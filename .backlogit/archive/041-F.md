---
archived_from: .backlogit/queue/041-F.md
archived_status: done
artifact_type: feature
commit: ac1c9c8aa6d837ce25433c57e281594388755dc9
created_at: 2026-07-01T23:40:56.6470658-07:00
custom_fields:
    harness_status: pending
    source_stash_id: 699FB5DC
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: '[follow-up from 040-F, gated on operator running the 040.002-T calibration harness] Implement the RUNTIME, HOST-RELATIVE OCR cap/scale algorithm using the portable cost model the harness produced (operator design 2026-06-30: OOM risk is relative to the host''s available memory). Add a function (e.g. in resource_probe or page_range) that, at runtime on ANY host: (1) probes available memory (resource_probe.available_ram_gb / recommended_docling_max_mb); (2) applies the calibrated cost model (base_mb + k_mb_per_mpx * page_megapixels * scale^2) to compute the max OCR pages-per-group that fit within a safe fraction of THIS host''s available memory; (3) for an oversized single page, computes the render scale that brings peak memory under budget. Then: replace the PROVISIONAL fixed OCR_MAX_BATCHED_PAGES=8 in page_range.py with this runtime-derived, memory-aware cap (so an 8 GB box caps lower than a 128 GB box, same binary); replace the cap//2 halving in batch_dispatch with the memory-derived downsizing; add the single-page scale-step retry in batch_dispatch.dispatch_batched_groups_with_retry using the derived scale (emit ocr_scale into the manifest via the 040.001-T knob) before conceding to heuristic. The calibrated COEFFICIENTS come from the harness run; the ALGORITHM is agent-buildable + unit-testable with synthetic (available_mb, page_megapixels, coefficients) inputs -> expected cap/scale, plus a fake-runner scale-step retry test. References: docs/decisions/ (calibration verdict from 040.002-T), src/docline/process/page_range.py, src/docline/process/batch_dispatch.py, src/docline/runtime/resource_probe.py. Source: 040-F / operator clarifications 2026-06-30.'
id: 041-F
priority: medium
status: archived
title: Runtime host-relative OCR memory cap and scale recovery (040-F follow-up)
updated_at: 2026-07-02T11:15:58.3907261-07:00
---

Apply the portable OCR peak-memory cost model from the 040.002-T calibration run (base_mb=1412.84, k_mb_per_mpx=15.4942, per_page_floor~207 MB, safe_fraction=0.6) at RUNTIME so the batched docling worker OCR cap is host-relative. Replace the provisional fixed OCR_MAX_BATCHED_PAGES=8 with a memory-derived cap (min of bitmap-area cap and per-page-floor cap), replace the blind cap//2 halving in dispatch with memory-derived downsizing, and add a single-page ocr_scale step-down retry (040.001-T knob) before conceding to heuristic. Plan: docs/plans/2026-07-01-runtime-host-relative-ocr-cap.md. Agent-buildable + unit-testable with synthetic (available_mb, page_megapixels, coefficients) inputs and a fake runner.