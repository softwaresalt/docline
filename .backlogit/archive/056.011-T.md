---
archived_from: .backlogit/queue/056.011-T.md
archived_status: done
artifact_type: task
commit: 2ccd6384ebac232a748cb6b8a0478586f1ad28d2
created_at: 2026-07-16T17:48:05.7526418-07:00
dependencies:
    - 056.008-T
id: 056.011-T
labels:
    - fetch
    - mcp
    - test-first
parent_id: 056-F
priority: medium
references:
    - docs/plans/2026-07-16-console-verbosity-progress-plan.md
status: archived
title: Forward progress callback through fetch library seam
updated_at: 2026-07-17T14:17:43.606475-07:00
---

Unit 2b. Add optional `progress` (default None) to _fetch_url, _execute_single_source, execute_source_configs, execute_elt_fetch (elt/execute.py) and execute_fetch (app.py); forward into crawl(progress=...). No function prints. Files: elt/execute.py, app.py + tests/elt/test_execute_fetch_progress.py.

<!-- BEGIN:acceptance-criteria -->
- All five functions (_fetch_url, _execute_single_source, execute_source_configs, execute_elt_fetch, execute_fetch) accept optional keyword `progress: Callable[[int, int | None, str], None] | None = None` (default None); name/default/type identical across the chain (P3, N6).
- Callback reaches `crawl()` (verified via stubbed crawl or fake source); non-crawl source branches ignore it (documented).
- Final staged-count transport (N-staged): after crawl+staging, `_fetch_url` emits ONE final count-only event `progress(staged_count, None, detail)` — its authoritative pages-with-bodies count — so the CLI/reporter can display the real staged total. NO result-schema change: `_execute_single_source` still collapses `staged_count` to `complete: bool`; `StagingJob`/`FetchResult` untouched (test asserts schemas unchanged).
- `progress` is a standalone function parameter and is NOT added to the `FetchRequest` Pydantic model (P2-2). Test asserts `--manifest` output and the terminal JSON result are unchanged.
- `progress=None` preserves current behavior.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: test-first. Depends on the crawl() callback (Unit 2a). Plan Unit 2b.
<!-- END:implementation-notes -->