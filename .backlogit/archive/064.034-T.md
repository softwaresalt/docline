---
artifact_type: task
created_at: 2026-08-29T03:40:15Z
dependencies:
    - 064.033-T
id: 064.034-T
labels:
    - mcp
    - stdio
    - security
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: done
title: Map §H8 external-engine reject to -32602 in the stdio transport
updated_at: 2026-08-29T21:00:25.1201216Z
---

Make 064.033-T green. Code domain only, <=1 file (src/docline/mcp/stdio.py). Map the adapter's typed ExternalEngineNotAllowedError (raised by the 064.032-T chokepoint) to a JSON-RPC -32602 invalid-params envelope in the single hardened dispatch, so both protocol eras inherit the wire reject by construction. Mirrors the H4 UnknownToolError -> -32602 mapping precedent established by 064.009-T: a SPECIFIC typed-error catch ordered BEFORE the generic -32603 internal-error handler, distinct from the ValidationError -> -32602 catch. The transport carries NO engine names of its own and does NOT re-declare the allow-list - engine policy lives solely in the adapter (064.032-T). Depends on 064.033-T; predecessor of 064.008-T. See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Plan Hardening §H8; 064.009-T H4 mapping).

<!-- BEGIN:acceptance-criteria -->
- src/docline/mcp/stdio.py catches ExternalEngineNotAllowedError raised out of server.call_tool()/server.process() and maps it to a -32602 invalid-params envelope, ordered BEFORE the generic -32603 handler and distinct from (does not disturb) the existing UnknownToolError -> -32602 (H4) and ValidationError -> -32602 catches.
- The mapping runs in the ONE hardened dispatch shared by both eras, so a modern (_meta, no initialize) tools/call and a legacy tools/call both yield -32602 for a non-allow-list pdf_engine on a default server (no modern pre-handshake bypass). The era classifier changes only negotiation/envelope shape, never which guards run (consistent with the 064.023-T guard-parity invariant).
- No-secret: the envelope surfaces the offending engine name only (reuses the adapter error message); no absolute paths / full-arguments echo (§H3) and no ambient credential value in the envelope or logs.
- The transport does NOT re-declare _MCP_LOCAL_PDF_ENGINES or the allow-list and adds no second policy site (prevents advertise/dispatch drift) - mapping only.
- Greens 064.033-T scenarios (a)/(b); the opt-in accept anchor (c) stays green; existing suites remain green.
- ruff check ., ruff format --check ., pyright src/ clean; typed only.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green (transport mapping) after 064.033-T. Single file: src/docline/mcp/stdio.py - add one except ExternalEngineNotAllowedError branch that builds a -32602 envelope, placed before the generic -32603 catch (mirrors the H4 UnknownToolError branch added by 064.009-T). No new constant, no allow-list, no engine names in the transport. Both eras funnel through this dispatch (064.022-T/064.023-T), so parity holds by construction. Depends on 064.033-T; feeds 064.008-T (the subprocess smoke harness ships in the executable after the gate is green). See plan §H8.
<!-- END:implementation-notes -->
