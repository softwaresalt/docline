---
artifact_type: task
commit: "3997945"
created_at: 2026-06-19T21:34:42.044246-07:00
custom_fields:
    source_stash_id: 19EC723D
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: low
    source_stash_text: '[Compound learning candidate from 2026-06-14 session] Engram CLI is invocable via `D:\Tools\engram.exe` regardless of whether the MCP tool surface exposes engram-prefixed tools to the agent. The CLI subcommands map 1:1 to MCP tools (`engram search` = unified_search, `engram map-code` = map_code, `engram impact` = impact_analysis, `engram query-graph` = query_graph, `engram symbols` = list_symbols, `engram query-memory` = query_memory, `engram daemon-status` = get_daemon_status, `engram workspace-status` = get_workspace_status, `engram sync` = sync_workspace, `engram index` = index_workspace, `engram bind` = set_workspace). PROTOCOL: at session start, agents should probe `engram daemon-status` + `engram workspace-status`; if reachable + indexed, use CLI for grounding before file-based tools (Get-ChildItem, Select-String, view). PARENT-PROCESS DIAGNOSTIC: when investigating "stale engram processes", use `Get-CimInstance Win32_Process -Filter "Name=''engram.exe''"` and inspect `ParentProcessId` + `CommandLine` to distinguish shims (parent=copilot.exe, cmdline ends in "shim") from daemons (cmdline contains "daemon --workspace <PATH>"). Different workspaces have independent cozo DBs — daemons for different workspaces NEVER contend. Zombie shims from prior sessions are harmless. ENVELOPE PATTERN: a transient "database is locked" error on the first call after daemon spawn is normal; retry once after 5 seconds before declaring ENGRAM_DEGRADED. Should be promoted to docs/compound/ as a durable learning. References: 2026-06-14 030-F session where engram was bypassed for entire shipment cycle; would have caught _read_pdf_docling_pages return-shape surprise (returns [] or [markdown], NOT per-page list) during T1+T2 design.'
id: 033.002-T
labels:
    - docs
    - compound
    - engram
parent_id: 033-F
priority: low
status: archived
title: Promote Engram CLI grounding learning to docs/compound
updated_at: 2026-06-19T21:45:19.2195091-07:00
---

