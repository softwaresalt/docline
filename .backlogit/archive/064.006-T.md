---
artifact_type: task
commit: 29cf11715ed7b8d638aca6d739fb3a0177c7c20f
created_at: 2026-08-27T08:05:55.5232823Z
dependencies:
    - 064.005-T
id: 064.006-T
labels:
    - mcp
    - stdio
    - tests
    - test-first
    - security
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: done
title: Author stdio MCP security gates H1-H3 harness (red)
updated_at: 2026-08-29T21:11:05.7636667Z
---

Author the failing security-gate harness for H1-H3 of the stdio MCP server in tests/parity/test_mcp_stdio.py (test-first / red). Tests domain only. Third of four dependency-linked harness tasks split from the original oversized T1. Covers workspace-root containment, untrusted-input bounds, and error-text non-disclosure. H4-H6 live in 064.007-T. See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Plan Hardening H1-H3).

<!-- BEGIN:acceptance-criteria -->
- H1: process with workspace_root='/' and 'C:\\' is rejected with -32602 and writes nothing outside the workspace; workspace_root is absent from the advertised process inputSchema (documented parity exception).
- H2: exercise the bounded binary frame read/drain contract (fixed-size chunk reads up to `MAX_FRAME_BYTES = 1 MiB` = `1_048_576` payload bytes scanning for the newline, then bounded draining of the oversized remainder, WITH a carry-over buffer that preserves post-newline bytes) - NOT a post-read length check on an unbounded readline(). EXACT boundary coverage (plan §H2 Selected numeric limit; `MAX_FRAME_BYTES` counts payload bytes BEFORE the `\n`, delimiter excluded): (i) EXACT-N ACCEPT - a frame of exactly `1_048_576` payload bytes followed by `\n` is accepted and dispatched normally (the maximal in-bounds frame is not rejected); (ii) N+1 REJECT/DRAIN/RESYNC - a frame whose payload reaches `1_048_576 + 1` bytes with no `\n` yet overflows: the oversized accumulation is discarded (not decoded), an error envelope is emitted, and the remainder is drained in bounded `CHUNK_SIZE` chunks to the next `\n`/EOF, after which the loop RESYNCHRONIZES and dispatches the NEXT valid frame; (iii) over-read guard - via an instrumented reader recording each read size, assert each boundary read requests AT MOST `min(CHUNK_SIZE, MAX_FRAME_BYTES - buffered_payload + 1)` so the reader observes at most the single crossing byte (buffered content <= `MAX_FRAME_BYTES + 1`, never a whole extra `CHUNK_SIZE` chunk). Additional vectors: oversized single line, deeply nested array, an unterminated/chunked oversized frame each degrade to an error envelope with bounded MEMORY (no whole-oversized-frame buffering) and the loop resynchronizes on the next valid frame, AND two complete frames arriving in a SINGLE chunk are BOTH dispatched (the reader retains the bytes after the first newline as carry-over and seeds the next frame from them, in both the normal path and after an oversized drain - no next-request drop). The H2 vectors (exact-N accept, N+1 reject/drain/resync + over-read guard, carry-over/framing) are one parametrized bounded-memory + framing guardrail scenario.
- H3: the error envelope AND the isError tool-result content contain no absolute-path substring.
- All new tests observed FAILING before implementation (red).
- Scenario budget: 3 (H1, H2 bounded-memory guardrail, H3) - within the <4 heuristic.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: test-first (write, verify red). Depends on 064.005-T. Do not implement production code (width isolation). H2 must assert EXACT-N acceptance (a `1_048_576`-payload-byte frame + `\n` dispatched) AND N+1 rejection/bounded-drain/resynchronization plus the over-read guard, and bounded memory for the unterminated/chunked-oversized case, against the fixed-chunk / `MAX_FRAME_BYTES = 1 MiB` (`1_048_576`) / bounded-drain contract in 064.002-T (plan §H2 Selected numeric limit) - not merely a post-read length check. The exact-N/N+1 rows are added IN-PLACE to the existing H2 parametrized scenario (no new scenario; the count stays 3). H1 asserts both runtime rejection and schema omission. Milestone model: H1 and H3 are greened by 064.009-T (stdio runtime guardrails); the H2 bounded-read scenario is greened by 064.002-T (which owns the bounded-read helper + its runtime enforcement).
<!-- END:implementation-notes -->