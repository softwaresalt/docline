---
artifact_type: task
created_at: 2026-08-27T15:09:45.5753391Z
dependencies:
    - 064.022-T
id: 064.023-T
labels:
    - mcp
    - protocol
    - dual-era
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: done
title: Implement dual-era routing with legacy retention
updated_at: 2026-08-29T20:59:04.2701332Z
---

Make 064.021-T green (dual-era routing completion + legacy retention + guardrail-parity verification). Code domain, <=2 files (src/docline/mcp/stdio.py + src/docline/mcp/server.py). Owns the LEGACY branch of the request-shape era classifier: an initialize request LATCHES a per-process legacy-era selection (initialize -> legacy), and requests LACKING a modern negotiation member (io.modelcontextprotocol/protocolVersion / clientCapabilities) - whether metadata-free (tools/call/tools/list with NO _meta) OR carrying ONLY ancillary _meta (e.g. _meta.progressToken) - received BEFORE that selection are REJECTED (error result), never served as legacy, while the same requests AFTER the latch are served legacy (a retained 2025-11-25 client's ancillary _meta stays on the legacy path) - so the process era is latched by initialize and keyed on the namespaced modern member, not selected by an unadorned request shape or by ancillary _meta presence. Completes dual-era routing on top of the modern-branch routing delivered by 064.022 (the modern branch stays request-stateless and its modern classification wins even after this latch). Keep the legacy handshake/ping path (from 064.002) intact; identity/capabilities/versions are read from the single describe_server() accessor introduced by 064.015, so initialize (064.002) and server/discover (064.022) cannot drift by construction. GUARDRAIL PARITY (blocking, VERIFY not wire): 064.022 already routes the modern branch through the SAME hardened dispatch path as legacy (guards-by-construction); THIS task VERIFIES that both eras funnel every tools/call/process through that one hardened path so H1 (workspace_root reject -32602), H3 (error non-disclosure), H4 (closed allow-list), H5 (stdout hygiene) apply before the modern result wrapper - the era classifier changes only negotiation + envelope shape, never which guards run. 064.021 scenario (c) already greened at 064.022; this task must not regress it. See plan Protocol Era Model / cycle-3 + cycle-4 + cycle-5 remediation.

<!-- BEGIN:acceptance-criteria -->
- Legacy branch of the era classifier: an initialize request LATCHES a per-process legacy-era selection (initialize -> legacy); a request whose _meta carries a namespaced modern negotiation member (io.modelcontextprotocol/protocolVersion, equivalently clientCapabilities) was already routed to modern semantics by 064.022 (request-stateless, and the modern member WINS EVEN AFTER this latch is set). Under the latch, a subsequent request LACKING a modern negotiation member - including one carrying ONLY ancillary _meta (e.g. _meta.progressToken) - is served LEGACY (a retained 2025-11-25 client's ancillary _meta stays on the legacy path, never routed to the modern validator and rejected). A request lacking a modern negotiation member received BEFORE the initialize latch is set is REJECTED (error result), never served as legacy - parametrized over both a metadata-free operation (NO _meta) AND an ancillary-_meta-only operation (no protocolVersion member). Both branches coexist under one classifier. This greens the 064.021-T scenario (b) retained-legacy ancillary-_meta-stays-legacy row (i-a), the modern-wins-after-latch row (i-b), and both pre-initialize reject rows once the latch lands here (cycle-16 round-1).
- The legacy initialize/notifications/initialized/ping path is retained and unchanged for legacy clients.
- No drift: initialize and server/discover both read identity/capabilities from the single describe_server() accessor (introduced by 064.015), and initialize.protocolVersion (legacy singular) is contained in server/discover's supportedVersions (modern plural) - a containment check, not a singular-vs-plural field equality - so the no-drift property holds by construction.
- Guardrail parity VERIFIED (guards were applied by the modern branch in 064.022, not first wired here): a modern (_meta, no initialize) tools/call process with workspace_root is rejected -32602 (H1); modern-path isError content sanitizes absolute paths (H3); modern-path stdout stays clean (H5); H4/H6/H7 hold on the modern path. The §H8 external-PDF-engine reject (-32602 for a non-allow-list pdf_engine on a default server) likewise holds on BOTH eras because it funnels through the SAME hardened dispatch (adapter chokepoint 064.032-T + transport mapping 064.034-T); proven dual-era by 064.033-T. Both eras share ONE hardened dispatch path; this task confirms parity is not regressed by adding the legacy branch.
- 064.021-T harness passes green (legacy-retention regression anchor stays green; era routing both directions; modern-path guard parity - already green from 064.022 - stays green).
- ruff check ., ruff format --check ., pyright src/ clean; typed errors only.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green after 064.022-T. Boundary with 064.022: 064.022 owns the MODERN branch (detect the namespaced io.modelcontextprotocol/protocolVersion member -> modern handlers) + server/discover + -32022; this task owns the LEGACY branch (initialize -> latch legacy) + the pre-initialize reject of operations lacking a modern negotiation member (metadata-free OR ancillary-_meta-only) + guard parity + no-drift verification. The modern era is request-stateless (never depends on prior session state); the legacy era is latched per-process by initialize, and requests lacking a modern negotiation member before that latch (whether metadata-free or ancillary-_meta-only) are rejected (pre-initialize operation test), so neither a malformed modern request missing its protocolVersion member nor a legacy client's ancillary _meta can fall through to legacy pre-latch and bypass _meta validation. Guard parity closes the cycle-3 adversarial-review P1 (a stateless/pre-handshake modern branch that skipped H1 would re-open the workspace_root P0). Width-isolated: code domain, <=2 files. Depends on 064.022-T; feeds the subprocess smoke harness (064.008-T).
<!-- END:implementation-notes -->