---
artifact_type: task
created_at: 2026-08-27T15:09:16.7747317Z
dependencies:
    - 064.020-T
id: 064.021-T
labels:
    - mcp
    - protocol
    - dual-era
    - tests
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: done
title: Author legacy-era retention and era-routing harness
updated_at: 2026-08-29T20:58:23.8947112Z
---

Author the RED legacy-era retention + era-routing harness (tests/parity/test_mcp_stdio.py). Tests domain only. Proves the legacy initialize handshake still works AND the server routes eras correctly from how the client opens (a namespaced modern _meta negotiation member - io.modelcontextprotocol/protocolVersion - vs initialize; ancillary legacy _meta such as _meta.progressToken stays legacy). Verifies red until 064.023-T. See plan Protocol Era Model / cycle-3 + cycle-16 round-1 remediation.

<!-- BEGIN:acceptance-criteria -->
- Scenario (a) [regression anchor - already green from 064.002 legacy base + 064.015 describe_server]: the legacy initialize handshake still returns capabilities + protocolVersion '2025-11-25' + serverInfo; notifications/initialized is silent; ping -> {} (legacy-only). This anchor asserts ONLY the legacy handshake surface (no cross-era comparison), so it is green at authoring and guards against era-routing regressions. The initialize-vs-discover no-drift check is NOT part of this green anchor (server/discover does not exist until successor 064.022) and lives in scenario (b) as a genuinely-red assertion.
- Scenario (b) [era routing green at 064.023; no-drift sub-assertion green at 064.022]: (i) era routing keyed on the namespaced modern negotiation member - a tools/call whose _meta carries the io.modelcontextprotocol/protocolVersion member (detected by KEY MEMBERSHIP, not truthiness) is served under modern semantics with NO prior initialize (modern branch from 064.022), while the same method with NO modern negotiation member after initialize is served under legacy semantics (legacy branch from 064.023). Retained-legacy compatibility rows (parametrized rows on this scenario, NOT new scenarios): (i-a) a post-initialize request carrying ONLY ancillary _meta (e.g. _meta.progressToken) with NO protocolVersion member STAYS on the legacy path - it is NOT routed to the modern _meta validator and rejected (proving ancillary legacy _meta does not trigger modern validation for a retained 2025-11-25 client); (i-b) modern-wins-after-latch - a protocolVersion-bearing request arriving AFTER an initialize latch is still served MODERN (the modern negotiation member takes precedence over the legacy latch); AND (ii) no-drift - initialize and server/discover report the SAME identity/capabilities from the single describe_server() source, AND initialize.protocolVersion (legacy singular) is CONTAINED IN server/discover's supportedVersions (modern plural) - a containment check, NOT a singular-vs-plural field equality (legacy InitializeResult carries the singular protocolVersion; modern DiscoverResult carries the plural supportedVersions, so the two fields can never be equal); AND (iii) pre-initialize reject (pre-initialize operation test) - a request lacking a modern negotiation member received BEFORE any initialize is REJECTED (error result), never served as legacy, parametrized over BOTH a metadata-free operation (tools/call/tools/list with NO _meta) AND an ancillary-_meta-only operation (carrying _meta.progressToken but NO protocolVersion member), proving the process era is latched by initialize and keyed on the namespaced modern member, not selected by an unadorned request shape or by ancillary _meta presence (so neither a malformed modern request missing its protocolVersion member nor a legacy client's ancillary _meta can fall through to legacy pre-latch and bypass validation - the ancillary-_meta-only request hits the pre-init reject, NOT the modern -32602 path). The no-drift sub-assertion greens at 064.022 (once server/discover first reads describe_server()); the era-routing sub-assertions - the bidirectional routing, the retained-legacy ancillary-_meta-stays-legacy row (i-a), the modern-wins-after-latch row (i-b), and both pre-initialize reject rows - green at 064.023 (legacy branch owns the per-process legacy latch + pre-init reject); the protocolVersion-keyed discriminator that keeps ancillary _meta OFF the modern path is delivered by 064.022 (its modern-branch detect step keys on the namespaced member, not any _meta presence); the scenario is fully green at 064.023. All parts are RED at authoring because neither server/discover nor the legacy routing branch (with its latch) exists yet.
- Scenario (c) [green at 064.022 - guard parity + modern CallToolResult wire shape are intrinsic to the modern branch]: modern-path guardrail parity AND CallToolResult wire shape (parametrized over fetch/process/export_schema on success, plus the fetch/process failure->isError mapping) - a modern (_meta, no initialize) tools/call enforces H1 (workspace_root reject -32602), H3 (absolute-path sanitization in isError), and H5 (clean stdout) IDENTICALLY to the legacy path, AND a modern (_meta-bearing) request with a malformed id (object/array/bool/null) - even one whose _meta carries an unsupported protocolVersion or whose method is unknown - is rejected with -32600 and id:null and NO modern wrapper (proving the shared request-shape guard, incl. MCP 2026-07-28 RequestId id-type validation, runs BEFORE _meta extraction / version negotiation / era routing / method dispatch, so a malformed id can never surface as -32022/-32602/-32601 or a wrapped result), AND a modern (_meta-bearing) payload that is itself MALFORMED and ALSO lacks an id (an object carrying valid _meta but a missing/non-string method and no id) is rejected with -32600 and id:null - NOT silently suppressed as a notification and NOT wrapped in a modern envelope - proving request-shape validation precedes BOTH _meta extraction / era routing AND the id-absent notification-suppression branch (an absent id rescues a malformed modern payload no more than a malformed legacy one; cycle-16 Finding A), AND every successful modern tools/call returns a result that wraps a standards-valid MCP CallToolResult (result.content is a non-empty ContentBlock[]; result.structuredContent mirrors the domain result when the adapter mirrors it) inside the resultType:'complete' envelope with serverInfo _meta; for fetch/process a validated-but-failed result maps to isError=true with sanitized error text in content (export_schema's only failure mode, non-empty arguments, is a -32602 envelope, not an isError result) - the SAME CallToolResult body the legacy harness 064.005-T asserts, differing only by the modern wrapper. This proves the guards apply pre-handshake, era routing cannot branch around them, and the modern era emits a standards-conformant wire shape (not a raw FetchResult/ProcessResult/str). This greens at 064.022 because 064.022's modern branch funnels tools/call through the SAME hardened dispatch AND reuses dispatch()'s inline CallToolResult shaping (guards-and-shape-by-construction), applying only the resultType wrapper on top - not at 064.023, and no build state exposes an unguarded or raw-serialized modern path.
- The genuinely-red scenarios (b)/(c) are observed RED at authoring; (a) is a legacy-only green regression anchor. Scenario (c) greens at 064.022 (modern guard funnel + shared result-shaping/resultType wrapper); scenario (b) is fully green at 064.023 (legacy branch), with its no-drift sub green at 064.022.
- No production code changed (tests only).
- Scenario budget: 3 (legacy-only green anchor; era-routing + no-drift + pre-initialize reject; modern-path guard parity + CallToolResult wire shape) - within the <4 heuristic. The modern wire-shape assertion strengthens the existing guard-parity scenario in-place (no new scenario added). The modern malformed-id request-shape case (cycle-9) is likewise added as a parametrized row to scenario (c) in-place - no new scenario; the count stays 3. The modern id-absent-malformed row (cycle-16 Finding A: _meta-bearing object, missing method, no id -> -32600 id:null, unwrapped, not suppressed) is likewise a parametrized row in scenario (c), in-place - no new scenario; the count stays 3. The cycle-16-round-1 era-routing rows (retained-legacy ancillary-_meta-stays-legacy (i-a), modern-wins-after-latch (i-b), and the ancillary-_meta-only pre-initialize reject) are likewise parametrized rows added to scenario (b) in-place - no new scenario; the count stays 3.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: red-only harness authored before 064.023-T. Basis: versioning.mdx 'Backward Compatibility with Initialization-Based Versions' - a dual-era server selects behavior from how the client opens. Scenario (a) is the legacy-only green regression anchor (server/discover does NOT exist until successor 064.022, so the initialize-vs-discover no-drift check cannot be green here and is placed in scenario (b) as a genuinely-red assertion). Scenario (c) closes the cycle-3 adversarial-review P1: because the modern surface is stateless/pre-handshake, a modern client could re-open the H1 workspace_root P0 if the modern branch skipped the guard - so guard parity is asserted here. Scenario (c) also asserts the modern CallToolResult wire shape (result.content is a ContentBlock[] under the resultType:'complete' wrapper), the modern-era counterpart to the legacy CallToolResult body asserted by 064.005-T; both are produced by the SAME result-shaping step (064.002 legacy body / 064.022 modern wrapper around that body). Width-isolated: tests domain only. Request-shape parity (cycle-9): the shared dispatch request-shape guard - including MCP 2026-07-28 RequestId id-type validation (object/array/bool/null id -> -32600 with id:null, never echoed) - runs BEFORE _meta extraction / era classification / method routing. The legacy/shared path is asserted in 064.005-T; the MODERN path is asserted HERE in scenario (c) via an added parametrized malformed-id row (modern _meta + malformed id, incl. an unsupported-version or unknown-method variant, still yields -32600 + id:null with NO modern wrapper), making the shape-before-_meta/era/method ordering test-bound rather than merely structural. Parametrized row in the existing scenario (c) - scenario budget stays 3. Depends on 064.020-T; greened by 064.023-T. §H8 cross-reference: the external-PDF-engine dual-era reject (a modern _meta tools/call process with a non-allow-list pdf_engine is rejected -32602 identically to legacy) is proven by the dedicated §H8 transport-mapping harness 064.033-T, kept OUT of this scenario to preserve the 3-scenario budget; the guard-parity invariant here (both eras funnel through one hardened dispatch) is what makes that §H8 parity hold by construction.
<!-- END:implementation-notes -->