---
artifact_type: task
created_at: 2026-08-27T15:09:03.9940554Z
dependencies:
    - 064.009-T
id: 064.020-T
labels:
    - mcp
    - protocol
    - dual-era
    - tests
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: done
title: Author dual-era discovery and modern-negotiation harness
updated_at: 2026-08-29T20:58:02.4892496Z
---

Author the RED dual-era discovery + modern-negotiation harness (tests/parity/test_mcp_stdio.py). Tests domain only. Covers MCP 2026-07-28 modern-era semantics verified against the official spec (server/discover MUST - and server/discover is itself routed through the per-request _meta validator, requiring BOTH protocolVersion AND clientCapabilities even pre-initialize, cycle-11; per-request _meta protocolVersion; -32022 UnsupportedProtocolVersion). Verifies red until 064.022-T. See plan Protocol Era Model / cycle-3 + cycle-11 remediation.

<!-- BEGIN:acceptance-criteria -->
- Scenario (a): server/discover - a MODERN, modern-only method routed through the SAME per-request _meta validator as every modern request (cycle-11), so it carries _meta with BOTH io.modelcontextprotocol/protocolVersion = '2026-07-28' AND io.modelcontextprotocol/clientCapabilities and is answerable with NO prior initialize (pre-handshake availability does NOT waive required _meta) - returns a DiscoverResult with supportedVersions (both eras, e.g. ['2026-07-28','2025-11-25']), capabilities, serverInfo in _meta, resultType:'complete', AND cache metadata ttlMs + cacheScope (DiscoverResult is a CacheableResult per the 2026-07-28 schema, so ttlMs/cacheScope are required on the discover result itself, not only on list results).
- Scenario (b): a well-formed modern request whose _meta carries BOTH io.modelcontextprotocol/protocolVersion = '2026-07-28' AND io.modelcontextprotocol/clientCapabilities is served statelessly (no handshake) and the result carries resultType:'complete' + serverInfo _meta (list results also ttlMs/cacheScope).
- Scenario (c) [parametrized modern-metadata rejection - one cohesive 'invalid modern _meta => reject' property, axes = which required _meta member is invalid X which modern method (INCLUDING server/discover)]: (i) an unsupported _meta protocolVersion returns -32022 with data.supported (list) + data.requested; (ii) a modern request with missing OR malformed _meta.io.modelcontextprotocol/clientCapabilities returns -32602 Invalid Params (validated only after version acceptance, so version negotiation takes precedence: an unsupported version yields -32022 even if clientCapabilities is also absent); (iii) a PRESENT-but-malformed-type protocolVersion (non-string/null value - the member KEY is present) is MODERN-classified by key membership (NOT truthiness) and rejected by the modern _meta validator (-32602 Invalid Params for a malformed member type; a well-typed but unsupported version string is the -32022 case in (i)), NEVER falling through to the legacy path - proving the era discriminator keys on protocolVersion KEY MEMBERSHIP, not on a valid/supported value (cycle-16 round-1). The property holds for EVERY modern method INCLUDING server/discover (cycle-11): a server/discover with an unsupported version -> -32022, and a server/discover with missing/malformed clientCapabilities -> -32602 - server/discover is NOT exempt from _meta validation even though it needs no prior initialize.
- All three scenarios authored and observed RED (no modern negotiation implemented yet).
- No production code changed (tests only).
- Scenario budget: 3 (discover shape incl. cache metadata + required _meta; modern stateless serve with both _meta members; parametrized invalid-_meta rejection across members X modern methods incl. server/discover) - within the <4 heuristic. The server/discover _meta coverage (cycle-11) is an added AXIS on the existing scenarios (a) and (c), not a new scenario - budget stays 3. The cycle-16-round-1 present-but-malformed-type protocolVersion row (key-membership discriminator proof) is likewise an added axis on scenario (c), not a new scenario - budget stays 3.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: red-only harness authored before 064.022-T. Authoritative basis: modelcontextprotocol/modelcontextprotocol docs/specification/2026-07-28 (server/discover.mdx - DiscoverResult is a CacheableResult so ttlMs/cacheScope are required; versioning.mdx - per-request _meta carries protocolVersion AND clientCapabilities; changelog.mdx - -32022). Cycle-11: server/discover is itself validated by the modern _meta validator (both members required, version-first) - it is NOT a bare pre-handshake call exempt from _meta; the harness therefore sends server/discover WITH valid _meta in scenario (a) and asserts server/discover rejection rows in scenario (c). Width-isolated: tests domain only. Depends on 064.009-T (transport base + guardrails exist); greened by 064.022-T.
<!-- END:implementation-notes -->