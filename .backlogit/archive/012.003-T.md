---
archived_from: .backlogit/archive/012.003-T.md
artifact_type: task
commit: 7d4579f2a9a7d525946eddd95fbc4fb2dbf40ea2
created_at: 2026-06-03T22:01:32.9866317-07:00
dependencies:
    - 012.002-T
id: 012.003-T
labels:
    - processing
    - segmentation
    - integration
    - stage-harvested
    - shipment-12
parent_id: 012-F
priority: medium
references:
    - docs/plans/2026-06-03-heading-aware-segmentation-plan.md
status: archived
title: Wire segment_markdown into build_output_document_parts and run quality gates
updated_at: 2026-06-03T23:35:56.6990191-07:00
---

Wire `segment_markdown` into `src/docline/process/output_contract.py`. Replace the PDF branch (which currently calls `read_pdf_pages` and falls back to `read_pdf`) with a single `read_pdf` + `segment_markdown` flow. Replace the DOCX branch (which currently calls `_chunk_text_blocks(blocks, 6000)`) with `"\n\n".join(blocks)` + `segment_markdown`. Remove `_chunk_text_blocks` and `_DOCX_SEGMENT_CHAR_LIMIT` (no other callers). Update `tests/elt/test_process_regression.py` fixture expectations only if observed part counts change. Run all 5 quality gates and confirm green.

<!-- BEGIN:AcceptanceCriteria -->
- `src/docline/process/output_contract.py` no longer imports `read_pdf_pages` and no longer defines `_chunk_text_blocks` or `_DOCX_SEGMENT_CHAR_LIMIT`
- PDF branch: `rendered = read_pdf(file_path); segment_bodies = segment_markdown(rendered)` (with empty-safety: if `rendered` is empty, fall back to `[""]`)
- DOCX branch: `blocks = read_docx_blocks(file_path); joined = "\n\n".join(b.strip() for b in blocks if b.strip()); segment_bodies = segment_markdown(joined) if joined else [""]`
- HTML/MD/TXT branches unchanged
- `OutputDocumentPart` dataclass and `build_output_document_parts` signature unchanged
- `_relative_output_path` unchanged (filenames remain `part-NNNN.md`)
- `ruff format --check .` clean
- `ruff check .` clean
- `pyright src/` clean
- `pytest` all green (including the previously-RED tests from 012.001-T now passing and any fixture updates in `tests/elt/test_process_regression.py`)
- `python -m build` succeeds
- `docline export-schema` output unchanged (BaseFrontmatter v1 contract preserved)
<!-- END:AcceptanceCriteria -->

<!-- BEGIN:ImplementationNotes -->
- Verify no other callers of `read_pdf_pages` exist before removing the import (a quick grep confirms it is only used in `output_contract.py`)
- If `tests/elt/test_process_regression.py` line ~320 monkeypatches `docline.app.build_output_document_parts`, that test does not depend on internal segmentation behavior; should still pass without fixture changes
- Capture observed part-count delta on the existing seed PDF and DOCX fixtures (used in tests/elt/) for inclusion in the closure document
<!-- END:ImplementationNotes -->
