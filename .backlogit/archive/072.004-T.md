---
artifact_type: task
created_at: 2026-09-13T01:22:28.9137499Z
dependencies:
    - 072.001-T
id: 072.004-T
labels:
    - security
    - elt
    - code
    - harness-ready
parent_id: 072-F
priority: high
references:
    - docs/plans/2026-09-12-source-key-credential-sanitization-plan.md
status: done
title: Extend sanitize_source_key() to github_repo/manifest_git/manifest_local same-sink variants
updated_at: 2026-09-13T17:36:52.3114466Z
---

PLAN Unit 1b (code; test-first). R8 same-sink coverage extension. DEPENDS ON 072.001-T (reuses its sanitize_source_key/sanitize_source_id and the _MAX_CREDENTIAL_DECODE_LAYERS bounded multi-layer decode primitive; do NOT duplicate them). Extend sanitize_source_key(config) in src/docline/elt/source_keys.py to the remaining same-sink URL-bearing variants confirmed by the cycle-5 source-fact audit (_execute_single_source routes ALL config types through build_source_key into metadata.source + the ERROR log; GitHubRepoSource and ManifestGitSource are fetched via _fetch_github at execute.py:246 and are URL-bearing). (1) GitHubRepoSource (github_repo:{repo_url}@{branch}:{path_glob}): sanitize config.repo_url via the same fail-closed sanitize_source() wrapper + the R8 multi-layer URL guard used for crawl urls, marker-gate config.branch and config.path_glob via sanitize_source_id, and recompose github_repo:{sanitized_repo_url}@{sanitized_branch}:{sanitized_path_glob}. This SUBSUMES the former 79BF0AEC github_repo deferral (reconciled/archived this cycle). (2) ManifestGitSource (manifest_git:{id}:{url}@{branch}): credential-redact config.id via sanitize_source_id, sanitize config.url via the fail-closed url wrapper + R8 guard, marker-gate config.branch, and recompose manifest_git:{sanitized_id}:{sanitized_url}@{sanitized_branch} (closes PR #195 Copilot thread PRRT_kwDOSsAX4c6h1D4t). (3) ManifestLocalSource (manifest_local:{id}:{path}:{includes}): credential-redact config.id via sanitize_source_id for uniform manifest-id handling; keep config.path and config.include BYTE-IDENTICAL (filesystem components; path-embedded-secret redaction stays deferred as 06A59B1D). LocalFileSource stays build_source_key(config) byte-identical. Do NOT expand _CREDENTIAL_PARAM_PREFIXES (06A59B1D deferred); do NOT modify _sanitize_url in staging.py (059-S blast radius held); make_job_id still hashes the RAW build_source_key(config) (determinism). Files: src/docline/elt/source_keys.py, tests/elt/test_source_keys.py. Size: S | Complexity: medium. Depends on 072.001-T. ACCEPTANCE CRITERIA: (1) github_repo config with a credentialed repo_url (userinfo token AND ?token=SECRET, single- and double-encoded) -> userinfo + SECRET ABSENT from the sanitized key, non-credential repo_url/branch/path_glob preserved; (2) manifest_git config with a credentialed url and a credential-bearing id (srcA?token=IDSECRET incl. %74oken/%2574oken encodings) -> IDSECRET/token ABSENT, credential-free branch preserved verbatim; (3) manifest_local config with a credential-bearing id -> credential redacted, path/includes byte-identical; (4) credential-free github_repo/manifest_git/manifest_local configs returned byte-identical except redacted markers (branch/path/glob verbatim); (5) make_job_id input (raw build_source_key(config)) unchanged by the helper for every variant (determinism); (6) [R8 Copilot PRRT_kwDOSsAX4c6h1V5K] github_repo config whose BRANCH carries a credential (e.g. branch main?token=SECRET, single- AND double-encoded) AND, as a separate case, whose PATH_GLOB carries a credential (e.g. **/*.md?token=SECRET) -> SECRET ABSENT from the sanitized key in each case, proving config.branch and config.path_glob are actually routed through sanitize_source_id and not left raw (an impl that sanitizes only repo_url MUST FAIL these); (7) manifest_git config whose BRANCH carries a credential (main?token=SECRET, single- AND double-encoded) -> SECRET ABSENT from the sanitized key, credential-free id/url preserved as specified in (2).