---
artifact_type: task
created_at: 2026-06-03T13:12:07.5290203-07:00
dependencies:
    - 011.001-T
    - 011.002-T
id: 011.003-T
labels:
    - closure
    - verification
    - ci
parent_id: 011-F
priority: medium
references:
    - docs/plans/2026-06-03-docline-ci-workflow-plan.md
    - .github/instructions/github-pr-automation.instructions.md
status: done
title: Verify CI runs on probe PR and write closure document
updated_at: 2026-06-03T13:20:41.9141426-07:00
---

Validate the CI workflow runs end-to-end on a real probe PR before the shipment closes, and capture the audit trail in a closure document.

Steps:
1. Open the implementation PR (containing F1 + F2) against `main`.
2. Confirm all five CI jobs run, complete, and report `success` on the probe PR.
3. If any gate fails on the probe PR, fix locally, push, and re-poll per `.github/instructions/github-pr-automation.instructions.md` § 2.3 (Ship's `fix-ci` skill handles this).
4. Run `actionlint` locally before push.
5. Write `docs/closure/011-S-docline-ci-workflow.md` capturing:
   - Workflow file path and final commit SHA
   - Probe PR number and run ID
   - Per-job result table (job name, conclusion, duration)
   - SHA pinning audit (each third-party `uses:` line with resolved SHA and intended semver)
   - Confirmation of zero `secrets.*` references (grep audit)
   - Confirmation of zero `pull_request_target` triggers (grep audit)

Depends on F1 (`011.001-T`) and F2 (`011.002-T`) being committed. Commit scope: `docs`.

<!-- BEGIN:AcceptanceCriteria -->
- Probe PR shows at least one successful CI run where all five jobs report `conclusion: success`
- `actionlint` reports zero issues on the final workflow file
- `docs/closure/011-S-docline-ci-workflow.md` exists and records per-job results, SHA pinning audit, and security control confirmations
- Zero `secrets.*` references confirmed via grep audit
- Zero `pull_request_target` triggers confirmed via grep audit
<!-- END:AcceptanceCriteria -->