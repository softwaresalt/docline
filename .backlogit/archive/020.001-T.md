---
artifact_type: task
created_at: 2026-06-07T13:05:21.9423719-07:00
id: 020.001-T
labels:
    - deps
    - markitdown
    - config
    - test-first
    - harness-ready
parent_id: 020-F
priority: high
references:
    - docs/plans/2026-06-07-pa4-calibration-closure-plan.md
    - pyproject.toml
status: done
title: U4 — Add markitdown[pdf] as required runtime dep (pyproject.toml)
updated_at: 2026-06-07T13:32:58.4370016-07:00
---



<!-- BEGIN:acceptance-criteria -->
- pyproject.toml project.dependencies array includes `markitdown[pdf]>=0.1.6,<0.2`.
- `pip install -e .` succeeds with the new dep.
- `python -c 'from markitdown import MarkItDown'` succeeds in the venv.
- Existing pytest suite (989 tests) still passes — no import regressions.
- CI install times on all 3 OS matrix targets stay within budget (no rollback trigger #1 fired).
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Add markitdown[pdf] as a required runtime dependency in pyproject.toml. Per plan-review P3#6 finding: pin to a minor-version range (>=0.1.6,<0.2) rather than open-ended >= to insulate against backend swaps in future markitdown versions.

Files:
- pyproject.toml (modified — add to project.dependencies array)

No new tests; existing imports verify on next CI install. Blocks U1 (markitdown baseline) since markitdown must be installable before U1 imports it.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Land U4 FIRST in build-feature order — U1 imports markitdown and will fail to compile until the dep is present.

Keep the version pin tight (`<0.2`) per plan-review P3#6: markitdown could swap backends in a future minor release (e.g., from pdfminer.six to Marker/LLM-based), silently changing our triage output shape. Re-evaluate when upgrading.

Do NOT remove pypdf — still required by readers/pdf.py, pdf_splitter.py, the per-page reader.pages[i] metadata access in pdf_triage.py, AND the U1 baseline_engine='pypdf' fallback path.
<!-- END:implementation-notes -->