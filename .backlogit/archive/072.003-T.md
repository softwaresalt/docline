---
artifact_type: task
created_at: 2026-09-12T20:07:26.6288103Z
dependencies:
    - 072.002-T
id: 072.003-T
labels:
    - security
    - elt
    - code
    - harness-ready
parent_id: 072-F
priority: high
references:
    - docs/plans/2026-09-12-source-key-credential-sanitization-plan.md
status: done
title: Wire _execute_single_source metadata + ERROR-log sinks through sanitizer (GREEN)
updated_at: 2026-09-13T17:36:37.118759Z
---

PLAN Unit 3 (code; GREEN). In _execute_single_source (src/docline/elt/execute.py), set metadata source to sanitize_source_key(config) and change the _log.exception argument from source_key to sanitize_source_key(config) (R8 typed-config contract; url sanitized fail-closed + id credential-redacted independent of URL detection with BOUNDED multi-layer decoded-name marker matching + fail-closed at the cap; helper TOTAL/non-throwing, so calling it during metadata build BEFORE the fetch try and inside the exception logger cannot crash _execute_single_source or mask a fetch failure; config is in scope alongside source_key = build_source_key(config)). KEEP job_id = make_job_id(source_key) on the RAW key (determinism invariant). UNCONDITIONALLY scrub credential-bearing data from the logged exception MESSAGE AND its exc_info traceback within this ERROR sink for ALL fetch-failure paths -- not merely the sanitized source_key argument, and not gated on a prior observed leak. This covers BOTH (a) the crawl path (an exception embedding the raw config.url) AND (b) the Git fetch paths: readers/github.py raises GitHubFetchError whose message embeds the raw repo_url (github.py:45-52) and branch-derived request URLs (github.py:74-77), which _log.exception renders via exc_info even after the source-key argument is sanitized. Route the exception through a total credential-scrubber (reuse the R8 sanitize primitives on the rendered message/traceback text) so no raw userinfo/credential-query value from config.url, repo_url, or branch reaches the log or metadata.json, while KEEPING useful safe diagnostics (error class, status/reason, credential-free host/path). do NOT drop exc_info (Unit 2 asserts exc_info is not None, so the traceback must remain present but credential-free). Bounded to this file/ERROR path (D6E758F5 scope). Files: src/docline/elt/execute.py. Size: XS | Complexity: medium. Depends on 072.002-T. ACCEPTANCE CRITERIA: (1) metadata.source and ERROR log both routed through sanitize_source_key; (2) make_job_id still receives the raw source_key (job_id unchanged vs pre-fix for identical config); (3) Unit 1+2 tests green including the R6 malformed-url/fail-closed regression and the R8 double-encoded / bounded multi-layer credential-name regression; (4) existing test_web_crawl_orchestrate_and_execute_share_job_key stays green; (5) [R8 Copilot PRRT_kwDOSsAX4c6h1V40] a Git fetch failure (github_repo AND manifest_git) whose GitHubFetchError message embeds a credential-bearing repo_url/url or branch-derived request URL leaves that credential value ABSENT from the FULL rendered caplog.text (message + exc_info traceback) AND from metadata.json, while exc_info is not None (traceback retained, credential-free) and a safe diagnostic (error class/reason) remains present; scrubbing is UNCONDITIONAL, not gated on a prior observed leak.
