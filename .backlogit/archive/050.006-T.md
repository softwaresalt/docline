---
artifact_type: task
created_at: 2026-07-05T17:57:11.642204-07:00
dependencies:
    - 050.005-T
id: 050.006-T
labels:
    - openapi
    - cli
    - mcp
    - docs
    - parity
parent_id: 050-F
priority: medium
status: done
title: T6 — CLI/MCP parity + docs + deferred follow-up stash
updated_at: 2026-07-05T19:36:05.5563304-07:00
---

Surface the OpenAPI ingestion path through both interfaces and document it. Wire the reader into `docline process` (compute-bound pass) and the MCP tool surface so a single spec produces identical output on both. Advertise the path in `--manifest` (JSON Schema tool definition). Add a README section describing OpenAPI/Swagger ingestion, the v1 scope boundary, and usage. Preserve dual-interface parity per the constitution. On completion, record the deferred-beyond-v1 items as a follow-up stash entry.

<!-- BEGIN:acceptance-criteria -->
- `docline process` ingests an OpenAPI spec end-to-end and emits the per-operation/per-schema docs.
- The MCP tool exposes the same capability; a parity test asserts CLI and MCP produce identical output for one spec.
- `docline --manifest` advertises the OpenAPI path in the JSON Schema tool definition.
- README has a new section documenting OpenAPI/Swagger ingestion, v1 scope (3.x, single-spec, per-operation, local $ref only), and usage.
- A follow-up stash entry is created capturing deferred-beyond-v1 items (external/split $ref, Swagger 2.0, API versioning/monikers, pagination/LRO conventions, security-scheme deep render, corpus-wide azure-rest-api-specs sweep).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
- Depends on T5 (assembled BaseDocument output).
- `.json` staged files are currently skipped in the process pass (config/docfx sidecars) — ensure OpenAPI specs reach the reader via the T1 content-sniff routing, not by relaxing the blanket `.json` skip.
- Dual-interface parity is a constitutional requirement — add/extend parity coverage (tests mirror CLI and MCP).
- README is a `docs`-class change; follow markdown + writing-style instructions.
- Reference: spike Decomposition T6 row + 'Deferred beyond v1' list (record as follow-up stash on harvest).
<!-- END:implementation-notes -->