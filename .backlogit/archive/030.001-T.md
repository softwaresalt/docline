---
artifact_type: task
created_at: 2026-06-14T00:33:00.2950107-07:00
id: 030.001-T
labels:
    - docling
    - worker
    - envelope
parent_id: 030-F
priority: medium
references:
    - src/docline/_tools/docling_worker.py
status: done
title: 'T1: Worker emits JSON envelope (schema v1)'
updated_at: 2026-06-14T00:52:31.4241467-07:00
---

Change `docline._tools.docling_worker.main()` to serialize `{"schema_version":1, "pages":[...], "page_count":N, "text":"\n\n".join(pages)}` to the output path via json.dumps(ensure_ascii=False) instead of writing the flat-joined markdown. Update module docstring to document the envelope. Preserve all existing exit codes (0/2/3/4/5/6). On json.dumps failure (extremely unlikely for str-only content), exit 6 with structured stderr diagnostic. Tests must cover: (a) happy path produces a parseable envelope with pages list matching `_read_pdf_docling_pages` output; (b) page_count integrity (len(pages) == page_count); (c) text field equals `"\n\n".join(pages)`; (d) ensure_ascii=False preserves non-ASCII content; (e) failure modes still hit the right exit codes. No consumer changes in this task — T2 handles that, so the worker output must be JSON BUT the existing markdown-blob expectation in pdf_batch/pdf_triage will fail until T2. Consider gating with a temporary env var or just accept the test-suite red phase until T2 ships, depending on TDD discipline. Plan reference: docs/plans/2026-06-14-docling-worker-contract-refresh-plan.md §T1.