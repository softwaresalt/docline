---
artifact_type: task
commit: d1362bd
created_at: 2026-06-18T18:05:13.42871-07:00
custom_fields:
    source_stash_id: F676E692
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: '[follow-up from 033-S] Batched-mode partial-crash recovery: pdf_triage and pdf_batch currently gate per-chunk usability on the WHOLE-batch subprocess returncode (use_batched_splice → subprocess_ok = batched_returncode==0). If the batched worker process dies partway (e.g. OS-killed after writing K valid chunk envelopes), ALL chunks are marked fallback, discarding the K successfully-written envelopes. Fix: in batched mode, gate each chunk on its OWN output (envelope exists + not error-envelope) regardless of the overall returncode; only treat the whole batch as failed when NO envelopes were produced. This makes batched mode resilient to partial crashes. Lower priority than 033-S root-cause since the cosmos failure was 100% (no partial success to recover). References: src/docline/process/pdf_triage.py post-pass loop, src/docline/process/pdf_batch.py _run_chunks_batched.'
id: 032.002-T
labels:
    - docling
    - perf
    - batched-worker
    - resilience
    - test-first
parent_id: 032-F
priority: medium
status: archived
title: 'Batched-mode partial-crash recovery: gate each chunk on its own envelope (033-S follow-up)'
updated_at: 2026-06-18T18:42:40.8382094-07:00
---



<!-- BEGIN:acceptance-criteria -->
- Batched mode gates each chunk on its OWN output envelope (envelope exists AND is not an error-envelope), independent of the overall subprocess returncode.
- The whole batch is treated as failed only when NO envelopes were produced.
- Test-first: simulate a partial crash (K valid envelopes written + nonzero batched returncode) and assert the K chunks are usable while the remainder fall back.
- Surfaces touched: src/docline/process/pdf_triage.py post-pass loop, src/docline/process/pdf_batch.py _run_chunks_batched.
<!-- END:acceptance-criteria -->