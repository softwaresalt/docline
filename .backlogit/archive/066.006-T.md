---
archived_from: .backlogit/queue/066.006-T.md
archived_status: done
artifact_type: task
commit: b89c902460700128fc71b0d33046b1210e2fb5d4
created_at: 2026-08-29T22:55:13.8397804Z
custom_fields:
    source_stash_id: 0A56B201
    source_stash_kind: bug
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: 'SSRF TOCTOU/DNS-rebinding on returned hostname (fetch/sitemap.py validate_sitemap_url): validation classifies resolved IPs then returns the original hostname URL; the HTTP client re-resolves at fetch time, so a TTL-0 attacker can present a public IP at validation and a private/CGNAT IP at fetch, bypassing all address classification. Pre-existing architectural limitation, out of scope for 056-S (width-isolated CGNAT fix). Fix: resolve the hostname once and pin the validated IP for the outbound TCP connection, while preserving the ORIGINAL hostname for the Host header, TLS SNI, and certificate verification (verify the cert against the hostname, never the pinned IP) so HTTPS is not weakened. Flagged by 056-S adversarial review (L2). Source: docs/closure/2026-08-29-056-s-cgnat-ssrf-closure.md. Grounded: src/docline/fetch/sitemap.py validate_sitemap_url.'
dependencies:
    - 066.005-T
    - 066.004-T
id: 066.006-T
parent_id: 066-F
priority: medium
status: archived
title: Route sitemap fetch through the public pinned sink (green)
updated_at: 2026-08-30T01:57:53.5776146Z
---

src (sitemap.py; reuse public http.fetch_page - do NOT export/consume private _Pinned* classes; no new http.py boundary required). Concrete contract (removes API ambiguity): keep validate_sitemap_url(url)->str as a SYNCHRONOUS SSRF preflight (scheme/host/literal-IP via the canonical predicate; no authoritative resolve-and-return-hostname handoff). Add one authoritative entry point: async def fetch_sitemap(url:str, *, timeout_seconds:float=30.0, max_redirects:int=5)->FetchResponse that calls validate_sitemap_url then delegates to http.fetch_page, so resolution+validation+connect+redirect-revalidation+proxy-suppression are atomic and the validated IP is pinned while Host/SNI/cert keep the hostname. This is the single sitemap fetch path; the TOCTOU-prone hostname-return workflow is removed by making validate_sitemap_url preflight-only. AC: A.T5 harness greens against fetch_sitemap; full pytest clean; HTTPS cert/SNI verify against hostname; sitemap.__all__ exports fetch_sitemap. Width: src only. Depends on A.T5 (066.005-T) and A.T4 (066.004-T). Source plan: docs/plans/2026-08-29-ssrf-classifier-pinning-plan.md