---
archived_from: .backlogit/queue/054.003-T.md
archived_status: done
artifact_type: task
created_at: 2026-07-06T11:12:36.275875-07:00
dependencies:
    - 054.001-T
id: 054.003-T
parent_id: 054-F
priority: high
references:
    - docs/decisions/2026-07-06-postgresql-online-docs-fetch-capability.md
    - src/docline/fetch/html_extract.py
    - src/docline/fetch/html_normalize.py
status: archived
title: Verify PostgreSQL /docs/current end-to-end (fetch → process → fidelity)
updated_at: 2026-07-06T13:55:03.1873085-07:00
---

Prove docline can process the PostgreSQL online docs end-to-end. Run a bounded crawl of `https://www.postgresql.org/docs/current/` (page cap, depth cap, section-scope to `/docs/current/`, respect robots), then `execute_process` the staged output, and assess fidelity: schema-valid WebFrontmatter, correct canonical URL, tables preserved, `<pre>` code rendered as fenced code, Note/Warning admonitions preserved, and cross-doc `.html`→`.md` links harvested as graph edges. Capture recorded HTML fixtures from a small representative set of pages so a deterministic test asserts the fetch→process contract without live network in CI; a gated/manual live run confirms the real path. Record gaps for T3.

<!-- BEGIN:acceptance-criteria -->
- Deterministic test drives fetch (mocked network via recorded fixtures) → process → asserts Markdown + frontmatter + edges.
- Fidelity report enumerates tables/code/admonitions/canonical-URL/cross-doc-edge outcomes on the PostgreSQL sample.
- Any fidelity gaps are itemized and handed to T3 (or fixed here if trivial).
- Section-scope keeps the crawl inside `/docs/current/`; `/docs/devel/` and disallowed paths are never fetched.
- Gates green.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Fixtures: save a handful of real `/docs/current/` pages (index TOC + a page with `<pre>` code + a page with a Note/Warning + a page with a reference table). Keep the bounded live run tiny (small max_pages) and clearly gated so CI stays offline. Reuse `tests/fetch/` patterns.
<!-- END:implementation-notes -->