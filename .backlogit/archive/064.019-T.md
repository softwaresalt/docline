---
artifact_type: task
created_at: 2026-08-27T15:08:47.4543655Z
dependencies:
    - 064.018-T
id: 064.019-T
labels:
    - mcp
    - parity
    - correctness
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: done
title: Correct fetch tool description to HTTP(S)-only
updated_at: 2026-08-29T20:56:56.1196622Z
---

Make 064.018-T green. Code domain only, 1 file (src/docline/app.py). Correct the shared fetch tool description in get_manifest (app.py:465-468 - the single shared literal, which get_mcp_manifest re-exposes to the MCP surface) to state HTTP(S)-only (e.g. 'Fetch a document from an HTTP(S) URL and stage it for processing.'), matching execute_fetch's rejection of non-HTTP(S) sources. This fixes the advertisement on BOTH the CLI --manifest and the MCP tools/list surfaces (both were equally wrong). server/discover does NOT advertise individual tool definitions or descriptions (its DiscoverResult carries supportedVersions, capabilities, identity, and cache metadata only), so it is out of scope for this text correction. No processing-behavior change. See plan Design (fetch advertising) / cycle-3 remediation.

<!-- BEGIN:acceptance-criteria -->
- The fetch tool description in get_manifest (app.py:467, the shared literal re-exposed by get_mcp_manifest) states HTTP(S)-only and no longer claims 'a URL or file path'.
- Both truthful surfaces are corrected: docline --manifest (CLI) and the MCP tools/list advertise the corrected description. server/discover is NOT a description surface (it exposes versions/capabilities/identity/cache metadata, not tool descriptions) and is not asserted here.
- execute_fetch processing behavior is UNCHANGED (this is a description-text correction only).
- 064.018-T parity harness passes green.
- ruff check ., ruff format --check ., pyright src/ clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green after 064.018-T. Correcting the SHARED literal in get_manifest() is preferred over an MCP-specific override because the CLI advertising is equally wrong (get_mcp_manifest returns get_manifest().tools, so the one edit fixes both surfaces). Single-line string change; keep it accurate to execute_fetch's accepted scheme set {http, https}. Width-isolated: code domain, 1 file. Depends on 064.018-T.
<!-- END:implementation-notes -->