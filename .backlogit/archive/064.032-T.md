---
artifact_type: task
created_at: 2026-08-29T03:40:05Z
dependencies:
    - 064.031-T
id: 064.032-T
labels:
    - mcp
    - stdio
    - security
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: done
title: Implement §H8 adapter external-engine gate (allow-list omit + dispatch deny)
updated_at: 2026-08-29T20:59:45.2571815Z
---

Make 064.031-T green. Code domain only, <=2 files (src/docline/mcp/server.py + src/docline/mcp/exceptions.py). Implement the complete §H8 ADAPTER-level policy on DoclineMcpServer so external, credential/network-bearing PDF engines are unavailable on the untrusted MCP surface by default (advertise-omit AND dispatch-deny) and available only under the explicit server-side opt-in. Uses a local-engine ALLOW-LIST (fail-closed: any engine not on the list requires opt-in, so a future external engine is denied by default). Introduces the single sources of truth reused by the transport mapping (064.034-T) and the startup resolver (064.036-T). Does NOT touch the shared ProcessRequest model or readers.pdf._SUPPORTED_LAYOUT_ENGINES - CLI behavior (docline process --pdf-engine mistral_ocr, docline --manifest) is UNCHANGED. Does NOT edit tests (064.031-T owns test/normalization). Depends on 064.031-T; predecessor of 064.033-T. See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Plan Hardening §H8; 064.015-T list_callable_tools; H4 typed-error pattern).

<!-- BEGIN:acceptance-criteria -->
- src/docline/mcp/server.py defines _MCP_LOCAL_PDF_ENGINES: frozenset[str] = frozenset({"auto", "docling", "heuristic"}) - the ALLOW-LIST of PDF engines permitted on the untrusted MCP surface WITHOUT opt-in (local / no external egress). Documented as an allow-list so any engine absent from it (present or future) is denied by default.
- src/docline/mcp/exceptions.py defines ExternalEngineNotAllowedError (a DoclineError subclass; co-located with the transport error types) raised when a client requests a non-allow-list PDF engine without the server opt-in. Its message names the offending engine ONLY (no arguments dict, no credential value).
- DoclineMcpServer.__init__ gains external_pdf_engines_enabled: bool = False (default DISABLED / fail-safe), stored on the instance as the SOLE toggle; NEVER derived from request data.
- Advertise gate: DoclineMcpServer.list_callable_tools() builds the process inputSchema pdf_engine enum filtered to _MCP_LOCAL_PDF_ENGINES when self._external_pdf_engines_enabled is False, and to the full shared enum when True - applied on the SAME MCP-specific schema-construction site that already omits workspace_root (§H1). Enum ordering follows the shared ProcessRequest enum, filtered.
- Dispatch deny (chokepoint): a single guard consulted by BOTH the call_tool "process" adapter AND the public process() method (the last hop before execute_process, so no adapter path bypasses it) raises ExternalEngineNotAllowedError when the resolved request pdf_engine is not in _MCP_LOCAL_PDF_ENGINES and self._external_pdf_engines_enabled is False. On an opt-in server the guard is a no-op (dispatch proceeds). ingest_local_dir remains absent from the call_tool allow-list (H4 / 064.015-T) - no second egress tool.
- list_tools(), get_manifest()/get_mcp_manifest() UNCHANGED (CLI/full-manifest still expose all four engines). No change to app.py, app_models.py, readers/pdf.py. The -32602 WIRE mapping of ExternalEngineNotAllowedError is delivered by 064.034-T (transport); this task raises the typed error only.
- Greens 064.031-T scenarios (a)/(b)/(c); existing suites remain green.
- ruff check ., ruff format --check ., pyright src/ clean; typed only, no bare except.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green (complete adapter security policy) after 064.031-T. Width/function budget: one allow-list constant, one typed-error class (exceptions.py), one __init__ parameter, one conditional in list_callable_tools()'s enum construction, and one shared guard helper consulted by the process dispatch path (<5 functions, 2 files). _MCP_LOCAL_PDF_ENGINES + external_pdf_engines_enabled + ExternalEngineNotAllowedError are the ONLY new sources of truth; the transport mapping (064.034-T) and startup resolver (064.036-T) reuse them without duplication. Put the guard where BOTH call_tool's process adapter and the public process() traverse it (a helper) so the belt-and-suspenders chokepoint holds even though the shared ProcessRequest stays permissive for CLI parity. Do NOT wire env/CLI here (064.036-T) and do NOT add the transport -32602 catch here (064.034-T). Depends on 064.031-T. See plan §H8.
<!-- END:implementation-notes -->
