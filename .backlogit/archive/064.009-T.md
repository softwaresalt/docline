---
artifact_type: task
commit: 29cf11715ed7b8d638aca6d739fb3a0177c7c20f
created_at: 2026-08-27T08:20:23.5712803Z
dependencies:
    - 064.002-T
id: 064.009-T
labels:
    - mcp
    - stdio
    - code
    - security
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: done
title: Implement stdio runtime guardrails H1/H3/H4/H5
updated_at: 2026-08-29T21:12:06.9831882Z
---

Implement the stdio runtime guardrails H1/H3/H4/H5 on the transport surface (src/docline/mcp/stdio.py) to turn the security harnesses 064.006-T (H1, H3) and 064.007-T (H4, H5) green. Code domain only. NO fetch-guard wiring: the §H6/§H7 shared-fetch SSRF/resource guards live in the shared fetch call path (delivered by 064.011-T/064.013-T) and enforce automatically once 064.002-T routes server.fetch, so they green at 064.002-T (via 064.014-T), not here. H2 bounded-read enforcement sits with its helper in 064.002-T. Split from 064.002-T so neither task breaches the 2-hour/<5-function rule. Depends on 064.002-T. See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Plan Hardening H1/H3/H4/H5; Milestone model).

<!-- BEGIN:acceptance-criteria -->
- H1: dispatcher-level runtime REJECT (-32602) of a client-supplied workspace_root (extra="forbid" does not catch a real model field, so an explicit pre-construction reject is required); server root pinned to process cwd; nothing written outside the workspace.
- H3: generic, non-reflective error text on envelope AND isError content (no absolute paths / tracebacks; sanitize ProcessResult.error / FetchResult.error / PathContainmentError text).
- H4: fail-closed unknown/unrouted tool names (incl. ingest_local_dir, dunders) -> -32602, never AttributeError. The transport does NOT re-declare or duplicate the tool allow-list: tool identity lives solely in the adapter's static map (064.015-T). server.call_tool() raises the concrete typed UnknownToolError (a DoclineError subclass, named in 064.015-T) for any name absent from the adapter allow-list, and the transport catches UnknownToolError SPECIFICALLY and maps it to a -32602 envelope. This catch is ordered BEFORE the generic -32603 internal-error handler (and distinct from the ValidationError -> -32602 catch) so an unknown tool never falls through to -32603 (mapping only, no second allow-list in stdio.py - prevents advertise/dispatch drift).
- H5: child-library stdout redirected during tool execution; protocol stdout carries only well-formed JSON-RPC frames.
- Greens the H1/H3 scenarios in 064.006-T and the H4/H5 scenarios in 064.007-T; existing suites remain green.
- ruff check ., ruff format --check ., pyright src/ clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green (stdio runtime guardrails: workspace_root reject, error sanitizer, typed unknown-tool-error -> -32602 mapping, stdout-redirect context manager - 4 concerns). H4 is implemented by mapping the adapter's typed unknown-tool error (raised by server.call_tool() from its single static allow-list in 064.015-T) to a -32602 envelope; do NOT duplicate the allow-list in stdio.py - the transport carries zero tool names. Depends on 064.002-T. Do NOT wire fetch guards here - they are shared-code (064.011-T/064.013-T) and auto-enforce via execute_fetch. H2 enforcement lives with the bounded-read helper in 064.002-T. §H8 cross-reference: the external-PDF-engine dispatch reject reuses THIS task's H4 typed-error -> -32602 mapping precedent (a specific typed-error catch ordered before the generic -32603) - 064.034-T adds an ExternalEngineNotAllowedError -> -32602 catch in the same stdio dispatch, and 064.032-T raises that typed error from the adapter chokepoint; no change to this task's own scope.
<!-- END:implementation-notes -->