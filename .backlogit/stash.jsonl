{"id":"7AA9FAA0","priority":"low","kind":"feature","text":"Add release / publish workflow (PyPI, GitHub Releases) once a 1.0 release is ready. Out of scope for 011-S which is CI-gate bootstrap only.","created_at":"2026-06-03T20:12:22.8897875Z"}
{"id":"4CA80776","priority":"low","kind":"task","text":"[Docling OCR tuning, plan-review F3 from 015-S] Enable `PdfPipelineOptions(do_ocr=True)` in `_read_pdf_docling_pages` to extract text from scanned/image-only PDFs that the current pipeline returns empty for. Defaults to docling's bundled OCR engine (likely Tesseract or EasyOCR). Lower priority because the current heuristic baseline also produces no text for scanned PDFs — enabling OCR is an enhancement, not a regression fix. Closure: docs/closure/015-S-post-g3-hygiene.md.","created_at":"2026-06-04T18:42:43.8263835Z"}
{"id":"5CFE4481","priority":"medium","kind":"task","text":"[review follow-up from 019-F] Per-page docling output limitation in process_pdf_triaged splice-back: when a flagged range covers N pages, the docling_worker returns a single concatenated markdown blob (one subprocess invocation per range). The orchestrator currently places that blob on the FIRST page of the range and leaves subsequent pages of the range as empty strings (still marked engine=\"docling\"). Downstream consumers expecting per-page content per the docling attribution may be surprised. Options: (1) modify docling_worker to emit per-page output via a JSON envelope (breaking change to the worker contract — coordinate with pdf_batch.py consumers); (2) run docling_worker once per page within a flagged range (slower but per-page correct); (3) document the limitation prominently and provide a helper that re-splits the blob heuristically (H1 boundaries) when downstream needs per-page. Compound learning candidate: \"single-subprocess range outputs require explicit per-page splitting protocol\".","created_at":"2026-06-06T23:41:51.6478317Z"}
{"id":"24920EFF","priority":"low","kind":"task","text":"[review follow-up from 019-F] Validate weights_path in fidelity_scorer.load_weights against workspace containment when the path is supplied by an untrusted source (e.g. an MCP server caller). Today the CLI is the only caller and operator-supplied paths are trusted, so this is low priority. Becomes relevant when ProcessRequest is exposed via the MCP server and pdf_mode=triage is invoked remotely with a caller-controlled weights JSON path. Mitigation: reuse safe_workspace_path / validate_workspace_relative_path from docline.paths to constrain the loaded file under the workspace root.","created_at":"2026-06-06T23:41:51.7159257Z"}
{"id":"DE3E7346","priority":"low","kind":"task","text":"[review follow-up from 019-F] Extract shared Pass 1-2 helper between process_pdf_triaged and triage_report_only in pdf_triage.py. Both functions currently duplicate the heuristic-extraction loop (~12 lines each) and the per-page scoring loop. Refactoring opportunity: introduce a private _heuristic_pass(path, reader) -> list[str] and _score_pass(heuristic_pages, reader, scorer) -> list[PageScore]. Reduces duplication and centralizes the per-page extraction error handling. Defer until a third caller appears or until a bug is found in one path but not the other.","created_at":"2026-06-06T23:41:51.7572599Z"}
{"id":"60E6157D","priority":"high","kind":"task","text":"[PA4 calibration blocker from 021-S] Improve the QA tripwire disagreement metric in pdf_triage._normalize_markdown(). The current implementation only strips trailing whitespace and collapses blank lines, which inflates the disagreement count by treating structurally-similar text (code fences wrapping the same content, different reading-order starting points, trivial whitespace/spacing variation) as full disagreements. Empirical evidence from PA4 cosmos run (2026-06-07): 32 of 33 sampled \"clean\" pages flagged as disagreements; manual inspection of 5 of those showed 4 were false positives (formatting/order differences with substantially the same content) and 1 was a genuine scorer miss (page 470 — a real markdown table that heuristic rendered as broken vertical text). Recommended fix: switch to lowercased + punctuation-stripped + tokenized Jaccard similarity (or normalized Levenshtein) with a configurable threshold (default ~0.7). Count as disagreement only when similarity is below the threshold. Without this fix, PA4 calibration cannot use qa_disagreements as a meaningful signal. Files: src/docline/process/pdf_triage.py:_normalize_markdown (replace), tests/process/test_pdf_triage_tripwire.py (add similarity-threshold scenarios), docs/closure/021-S-triage-then-repair.md (update calibration gate). Evidence: .elt/output/cosmos-tripwire/pa3-summary.json, .elt/output/cosmos-tripwire/splices/qa-*.md.","created_at":"2026-06-07T19:04:15.0040036Z"}
{"id":"1380BD85","priority":"high","kind":"task","text":"[PA4 calibration blocker from 021-S] Add a PDF-layout-structure signal to fidelity_scorer to detect mangled tables in heuristic output. The current scorer signals (char_density, non_ascii_ratio, long_unbroken_line, column_gap, table_char_density, image_heavy, form_fields) all score the HEURISTIC OUTPUT TEXT. They cannot detect the dominant scorer-miss failure mode: a real table in the source PDF that pypdf.extract_text() flattens into a single column of single words that looks like ordinary paragraph text. Empirical evidence: page 470 of azure-cosmos-db.pdf (Azure RBAC permissions table) rendered by heuristic as 'Action Description\\nnnections/write connection\\nMicrosoft.DBforMariaDB/...' (broken vertical text); docling renders it as a proper markdown table (4519 chars vs 2109 chars; 2.1x richer output). None of the existing signals fire on this. Recommended fix: add a signal that inspects the SOURCE pypdf.PageObject for structural cues: text-run X-coordinate clustering, column count via x-quantization, line density vs page area, presence of grid-like positioning. The signal looks at the PDF page object (not the extracted text) and fires when the heuristic-extracted text is suspiciously sparse relative to the layout complexity. Files: src/docline/process/fidelity_scorer.py (add new signal function + add to _SIGNAL_NAMES tuple + add default weight + integrate in score_page), tests/process/test_fidelity_scorer.py (add table-mangling test case with a fixture PDF). Recommended signal name: signal_layout_complexity. Once landed, re-run PA3+PA4 to verify page 470 (and similar table pages) now flag correctly.","created_at":"2026-06-07T19:04:15.0714378Z"}
