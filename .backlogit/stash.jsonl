{"id":"4C03AE14","priority":"low","kind":"task","text":"TOOLING: Fix Playwright MCP server browser default. The configured 'playwright' MCP server defaults to Chrome at the standard per-user install path (%LOCALAPPDATA%\\Google\\Chrome\\Application\\chrome.exe) which is NOT installed in this environment, so it fails at launch. Microsoft Edge IS installed at the standard system path (%ProgramFiles(x86)%\\Microsoft\\Edge\\Application\\msedge.exe). Validated working override 2026-09-08 (read-only probe of registry.terraform.io): @playwright/mcp with args --browser msedge --headless. Recommend updating .mcp.json playwright server to prefer msedge or add an Edge-backed fallback server (optionally driven by an env-configurable browser channel/executable path rather than a hardcoded location). Tooling/infra only, NOT a docline runtime dependency (feature 070-F / shipment 061-S is browserless, API-based; tests use recorded fixtures). Do NOT bundle with 061-S. Recorded per operator directive (capture MCP config change as a task, do not edit .mcp.json during Stage).","created_at":"2026-09-09T04:52:01.3352376Z"}
{"id":"6BF410E2","priority":"low","kind":"task","text":"DEFERRED SCOPE EXPANSION: Consolidate duplicated scripted-socket/HTTP-response test helpers (_ScriptedSocket, _http_response/_response, _create_connection_stub/_scripted_transport) that now exist with drifted semantics across tests/fetch/test_sitemap.py, tests/fetch/test_sitemap_pinned_sink.py, and tests/fetch/test_sitemap_resolution_count.py into one shared tests/fetch/ helper module.\n\nOut of scope for shipment 060-S per P-021 C1: the 069-F implementation plan (docs/plans/2026-08-29-sitemap-preflight-dedup-plan.md) deliberately decomposed B.T1-B.T7 as width-isolated, per-file tasks; cross-file test-infrastructure consolidation was never an authorized task in that plan and touches file boundaries beyond any single approved task's Files: scope.\n\nSource refs: task 069.001-T/069.002-T/069.004-T (feature 069-F, shipment 060-S); PR N/A (pre-PR capture); review-thread N/A (local review, no PR thread exists yet).\n\nRequires deliberation: yes (should Stage assess whether a shared conftest.py fixture or helper module is warranted, and whether it should also absorb similar existing per-file mocks in tests/fetch/test_ssrf_resolution.py etc.).\n\nKind: task. Provisional priority: low (test-hygiene only, no functional risk -- flagged independently by both Maintainability Reviewer and Python Reviewer during 060-S local adversarial review as P2/P2, with the Python Reviewer additionally noting drifted send()/sendall() semantics between copies as a latent DRY/regression-masking risk).","created_at":"2026-09-09T06:37:33.7768304Z"}
{"id":"D548B756","priority":"medium","kind":"task","text":"DEFERRED SCOPE EXPANSION: Harden the discovery-source registry's test-isolation and import-time-side-effect design (link_sources._sources module mutable state + crawl.py's module-import-time register(TfRegistrySource()) side effect, plus the unused @runtime_checkable on DiscoverySource). Out of scope per P-021 C1: fixing requires a registry redesign (e.g. explicit dependency injection into crawl(), or an isinstance() validation gate at registration) beyond completing the 11 authorized 070-F tasks; today's single-adapter usage is safe by construction and mitigated by autouse test fixtures. Source refs: shipment=061-S, feature=070-F, task=070.010-T, PR=N-A (pre-PR), thread=N-A (threadless, local review). requires_deliberation: true. Kind: task, provisional priority: medium.","created_at":"2026-09-09T16:51:06.5284757Z"}
{"id":"DFF8E8E1","priority":"low","kind":"task","text":"DEFERRED SCOPE EXPANSION: Harden TfRegistrySource's defensive JSON:API parsing beyond current test coverage: (a) no aclose() on the discovery async generator in crawl.py's early-return paths (relies on asyncio.run's implicit shutdown_asyncgens), (b) broad except Exception in _resolve_provider_version/discover_doc_urls could mask a programming bug as an ordinary adapter failure, (c) attrs_by_id keyed by item.get('id') can collide silently when multiple included entries omit id, (d) the continuation-page branch of _extract_docs_page does not filter by type=='provider-docs' the way the first-page branch's included-array filter does, (e) the off-host links.next guard assumes an absolute URL and would mis-classify a relative next link as off-host. Out of scope per P-021 C1: none of these are required by any of the 11 authorized 070-F tasks' acceptance criteria or fixtures; all are defense-in-depth hardening beyond the shipped, tested contract. Source refs: shipment=061-S, feature=070-F, task=070.006-T, PR=N-A (pre-PR), thread=N-A (threadless, local review). requires_deliberation: false. Kind: task, provisional priority: low.","created_at":"2026-09-09T16:51:14.6292975Z"}
{"id":"EF8503EC","priority":"low","kind":"task","text":"DEFERRED SCOPE EXPANSION: Prepare the discovery-source seam for a second concrete site adapter: (a) extract the duplicated fetch-plus-JSON-decode-plus-content-type-check boilerplate shared by TfRegistrySource._resolve_provider_version and discover_doc_urls into one helper so a second adapter cannot drift the I4 carve-out ordering, (b) move concrete-adapter imports and registration out of crawl.py's own module scope into a small dedicated registrations module so the core executor's diff footprint stays about crawl-loop concerns as adapters are added, (c) split _extract_docs_page's boolean-flag-driven branching into two named helpers for the two structurally unrelated JSON:API page shapes. Out of scope per P-021 C1: this is generalization work for a currently-hypothetical second adapter; the 11 authorized 070-F tasks scope exactly one adapter (Terraform Registry) and none of their acceptance criteria require this extensibility. Source refs: shipment=061-S, feature=070-F, task=070.010-T, PR=N-A (pre-PR), thread=N-A (threadless, local review). requires_deliberation: true. Kind: task, provisional priority: low.","created_at":"2026-09-09T16:51:22.2282637Z"}
{"id":"8E0B4FB5","priority":"low","kind":"task","text":"DEFERRED SCOPE EXPANSION: Add an independent pull-count bound in crawl.py's _seed_from_discovery_source, distinct from frontier.exhausted, for defense-in-depth against a future misbehaving DiscoverySource implementation that yields URLs which are consistently filtered out before frontier.admit() (duplicate, out-of-scope, or failing validate_crawl_url) -- such an implementation could drive the check-before-pull loop indefinitely since frontier.exhausted only flips on a successful admission. Out of scope per P-021 C1: the only currently-registered adapter (TfRegistrySource) cannot trigger this (its own paginated fetches are already budget/attempt-bounded via RemainingByteBudget), so this is speculative hardening against a not-yet-existing adapter, not a defect in the 11 authorized 070-F tasks. Source refs: shipment=061-S, feature=070-F, task=070.010-T, PR=N-A (pre-PR), thread=N-A (threadless, local review). requires_deliberation: false. Kind: task, provisional priority: low.","created_at":"2026-09-09T16:51:29.3946428Z"}
{"id":"0EE542D9","priority":"low","kind":"task","text":"DEFERRED SCOPE EXPANSION: Extend robots.txt policy enforcement to cover each of the TF registry adapter's individual internal API endpoints (version-lookup and provider-docs pagination requests), not only the start URL's own path -- a robots.txt that allows /providers/ but disallows /v2/ is not currently honored per-endpoint, since the discovery seed infers permission from a single start-URL check rather than consulting the cached robots policy for each API request via a callback/context passed to the source (Copilot review finding on PR 190, thread PRRT_kwDOSsAX4c6gw2AD). Out of scope per P-021 C1: this asks for STRICTER per-endpoint robots checking than the codebase's own existing precedent for auxiliary discovery fetches (mdBook toc-*.js script requests are not individually robots-checked either -- only user-facing discovered document URLs get a per-URL robots check via the main crawl loop, unchanged by this feature), and none of the 11 authorized 070-F tasks' acceptance criteria require this granularity; a split-policy robots.txt distinguishing API sub-paths from content sub-paths on the same host is also an exotic, unverified real-world scenario. Source refs: shipment=061-S, feature=070-F, task=070.010-T, PR=190, thread=PRRT_kwDOSsAX4c6gw2AD. requires_deliberation: true. Kind: task, provisional priority: low.","created_at":"2026-09-09T17:43:39.3438796Z"}
{"id":"1EDE36E7","priority":"medium","kind":"task","text":"DEFERRED SCOPE EXPANSION: pytest's tmp_path fixture is not pinned under a repo-local --basetemp anywhere in this repo (pyproject.toml [tool.pytest.ini_options].addopts is only '-v'; .github/workflows/ci.yml's pytest step passes no --basetemp), so any agent/CI invocation of bare pytest risks tmp_path resolving under the OS temp directory instead of the workspace tree, contrary to the NON-NEGOTIABLE CLI Workspace Containment principle (Constitution Principle IV). tests/scripts/test_hashicorp_dryrun_corpus.py's own module docstring asserts tmp_path 'is always rooted under the repo-local pytest --basetemp the operator/agent passes on the command line' -- an unenforced assumption, not a guarantee. Review-fix cycle 3 (062-S/PR #192) added several new --execute-mode tests using this fixture, increasing exposure to this pre-existing, repo-wide gap. Out of scope per P-021 C1: the fix belongs in pyproject.toml addopts (or an autouse conftest.py tmp_path redirect), a config surface outside the sentinel-claim destination-containment fix this cycle authorized. Source refs: task=071.007-T, feature=071-F, shipment=062-S, PR=192, thread=N/A (threadless -- local review finding, no GitHub review thread exists). requires_deliberation: true (Stage should decide between addopts --basetemp pinning vs. an autouse conftest.py fixture, and whether to retrofit CI). Kind: task, provisional priority: medium.","created_at":"2026-09-11T23:26:33.017671Z"}
{"id":"1E7CCBF7","priority":"low","kind":"bug","text":"DEFERRED SCOPE EXPANSION: scripts/hashicorp_mdx_normalize.py's --execute write pass (main() -> process_corpus -> _process_one_product_tree) reads source files via Path.read_text(encoding='utf-8') with no handling for UnicodeDecodeError (a ValueError subclass, not OSError); a non-UTF-8 source .mdx/.md file would surface as a raw Python traceback with exit code 1 instead of one of the module's documented EXIT_* codes, contradicting the module's own stated goal of never letting expected failure modes escape as a raw traceback. Flagged by independent Python Reviewer pass during 062-S/PR #192 review-fix cycle 3, explicitly noted as pre-existing and unrelated to this cycle's dest-claim/sentinel changes. Out of scope per P-021 C1: fixing requires touching process_corpus/_process_one_product_tree's read path, not the claim_destination()/_dest_precheck() contract surface this cycle authorized. Source refs: task=071.007-T, feature=071-F, shipment=062-S, PR=192, thread=N/A (threadless -- local review finding, no GitHub review thread exists). requires_deliberation: false. Kind: bug, provisional priority: low.","created_at":"2026-09-11T23:26:50.1962587Z"}
{"id":"E3129374","priority":"low","kind":"task","text":"DEFERRED SCOPE EXPANSION: two pre-existing, unmodified-this-cycle hardening gaps in scripts/hashicorp_mdx_normalize.py's guard_write_path(): (a) its containment check (candidate_resolved.relative_to(dest_resolved)) is a case-sensitive comparison, but Path.resolve() only normalizes casing for path segments that already exist on disk at resolve-time, so on the documented Windows target platform an operator supplying --dest/--report with differing casing on a not-yet-existing trailing segment could trigger a spurious ContainmentViolation or, more rarely, fail to catch a genuine escape; (b) guard_write_path re-resolves dest_root.resolve() from scratch on every per-file call inside a hot loop (potentially thousands of calls for a large corpus) even though dest_root never changes across the loop -- a deliberate TOCTOU-narrowing tradeoff per the function's own docstring, not an oversight, but repeated filesystem I/O against an unchanging root. Both flagged by independent Python Reviewer pass during 062-S/PR #192 review-fix cycle 3; neither is touched by this cycle's claim/release changes. Out of scope per P-021 C1. Source refs: task=071.007-T, feature=071-F, shipment=062-S, PR=192, thread=N/A (threadless -- local review finding, no GitHub review thread exists). requires_deliberation: false. Kind: task, provisional priority: low.","created_at":"2026-09-11T23:26:50.4495696Z"}
{"id":"13F7C7C3","priority":"low","kind":"task","text":"DEFERRED SCOPE EXPANSION: scripts/hashicorp_mdx_normalize.py's _dest_precheck() and claim_destination() independently implement overlapping '--dest is a non-directory / non-empty (accounting for a lone claim sentinel)' validation logic with separately-maintained message text -- an intentional, documented 'checked TWICE' belt-and-suspenders design (cheap early precheck + authoritative claim-time check), but the two implementations still duplicate business logic that could drift on a future touch. Review-fix cycle 3 (062-S/PR #192) already made both consistent in their sentinel-handling (fixing the P0/P1 exit-code-contract defect independently flagged by the Correctness/Concurrency/Python reviewer passes), but did not extract a single shared helper for the emptiness/sentinel classification itself. Relatedly, the same reviewer pass noted the sentinel-based claim subsystem (4-class exception hierarchy, ~90 lines of TOCTOU prose, double-checked pre-check + authoritative claim) is a large complexity increment relative to this script's own documented single-operator, one-exact-command usage model, and that mixed error-signaling idioms (str-return precheck vs. bespoke exception hierarchy vs. stdlib OSError/ContainmentViolation) now coexist in one already-large (871-line) file. Out of scope per P-021 C1: extracting a shared helper or simplifying the mechanism is a maintainability improvement, not required to complete the authorized existing-empty-dest change, and the sentinel design itself was explicit operator direction for this cycle. Flagged by independent Maintainability Reviewer pass during cycle 3's review gate. Source refs: task=071.007-T, feature=071-F, shipment=062-S, PR=192, thread=N/A (threadless -- local review finding, no GitHub review thread exists). requires_deliberation: false (advisory; only actionable if this disposable tool is ever promoted beyond requirements-evidence/single-run status). Kind: task, provisional priority: low.","created_at":"2026-09-11T23:26:50.696441Z"}
{"id":"ADE96404","priority":"low","kind":"task","text":"DEFERRED SCOPE EXPANSION: The ambient pipeline-topology CI check on PR #192 (062-S) reports BRANCH_MISMATCH because branch feat/github-markdown-extension does not match the naming convention the gate derives from shipment 062-S (e.g. feat/062-s-...); this is out of scope per P-021 C1 because fixing it would require renaming the already-open PR branch or reconfiguring the pipeline-topology gate's naming rules, neither of which is part of completing this cycle's authorized existing-empty-dest sentinel-claim fix. Confirmed pre-existing: the same failure occurred identically at the prior HEAD ebce8d5 (before this cycle's changes) and at 408dbba. Confirmed non-blocking: main has no branch protection rules (404 Branch not protected) and the CI workflow run's overall conclusion is success despite this job's individual failure conclusion, consistent with continue-on-error / advisory ambient status. Source refs: task=071.007-T, feature=071-F, shipment=062-S, PR=192, review-thread=N/A (ambient CI signal, not a GitHub review thread). requires_deliberation=true (branch-naming/topology-gate policy affects other in-flight work, not just this shipment).","created_at":"2026-09-11T23:33:06.7064129Z"}
{"id":"387E5F82","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: --execute does not reject a --dest resolved as equal to or nested under --source; such an invocation would create the sentinel and normalized output inside the declared read-only corpus. Flagged by Copilot shadow review on PR #192 (062-S), recurring across multiple review rounds including before this cycle's changes (first raised against ebce8d5-era code, e.g. review comment id 3993438433, again at eae28b0-era id 3994209569). Out of scope per P-021 C1: fixing requires adding a new dest-vs-source containment precondition to the execute-mode preflight, not touching the claim_destination()/_dest_precheck() emptiness/claim contract surface this cycle authorized. Source refs: task=071.007-T, feature=071-F, shipment=062-S, PR=192, review-thread=3994209569 (also 3993438433, resolved as part of this same deferred entry). requires_deliberation: true (containment-boundary policy question -- should dest-vs-source overlap be rejected outright, or is it an accepted operator-responsibility risk for a disposable tool). Kind: bug, provisional priority: medium (containment/safety-adjacent).","created_at":"2026-09-11T23:42:01.3897726Z"}
{"id":"C0E88586","priority":"low","kind":"bug","text":"DEFERRED SCOPE EXPANSION: protect_fenced_code's fence-opener scanner treats a backtick fence whose info string itself contains a backtick as a valid opener, per CommonMark this is invalid and should be treated as ordinary text; accepting it can mask the remainder of the document as an unterminated fence, hiding real MDX constructs from both normalization and the unresolved-construct execute gate (the restriction is specific to backtick fences, not tilde fences). Flagged by Copilot shadow review on PR #192 (062-S) at eae28b0-era review comment id 3994209535. Out of scope per P-021 C1: fixing requires touching the fence-opener regex/classification logic in scripts/_hashicorp_mdx/normalize.py, not the claim_destination()/_dest_precheck() contract surface this cycle authorized; pre-existing since the cycle-1/cycle-2 fence rewrites, untouched by this cycle's changes. Source refs: task=071.007-T, feature=071-F, shipment=062-S, PR=192, review-thread=3994209535. requires_deliberation: false. Kind: bug, provisional priority: low (narrow edge case, real corpus shows 0 unresolved constructs currently).","created_at":"2026-09-11T23:42:09.5751043Z"}
{"id":"7D71CBEA","priority":"low","kind":"bug","text":"DEFERRED SCOPE EXPANSION: guard_write_path()'s containment check (candidate_resolved.relative_to(dest_resolved)) succeeds when candidate_resolved == dest_resolved (Path.relative_to permits equal paths, returning an empty relative path), so a --report path resolving to exactly --dest itself (the directory root) passes the pre-claim/pre-write guard despite the documented strictly-inside contract. Execute mode then proceeds to write the corpus and only fails later when it tries to open the destination directory as the report file -- after corpus writes have already occurred, violating the fail-before-any-write behavior for a report path that was invalid from the start. Flagged by Copilot shadow review on PR #192 (062-S) at commit-20f91a4-era review comment id 3994338286. Out of scope per P-021 C1: this is a pre-existing gap in guard_write_path()'s core relative_to-based containment logic itself (unchanged since cycle 1/2), not the reserved-claim-sentinel-collision defect this cycle's own commit 20f91a4 fixed (that fix addressed a --report path equal to the NEW sentinel filename, a defect this cycle's own change introduced; this is a different, pre-existing edge case -- --report equal to --dest itself, which predates this cycle entirely and requires modifying guard_write_path()'s own core logic, not merely adding an adjacent special-case check next to it). Source refs: task=071.007-T, feature=071-F, shipment=062-S, PR=192, review-thread=3994338286. requires_deliberation: false. Kind: bug, provisional priority: low (narrow edge case: an operator would have to pass --report equal to --dest verbatim; real corpus dry-run and execute runs this cycle never exercise this path).","created_at":"2026-09-12T00:12:29.5135924Z"}
{"id":"48D6D05A","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: `.markdown` source files staged alongside a same-stem `.md` file collide at output: `_relative_output_path()` in src/docline/app.py maps both `foo.md` and `foo.markdown` to `foo.md`, and `write_markdown_output()` silently overwrites the first result, so a repository containing both extensions can lose one document while both still appear in manifests (Copilot review finding nt1k, PR 192, src/docline/app.py:51). Out of scope per P-021 C1: this is pre-existing production `.markdown` ingestion behavior in src/docline predating shipment 062-S / feature 071-F (071-F's authorized contract is a standalone disposable Python preprocessor -- scripts/hashicorp_mdx_normalize.py plus helper package and tests -- explicitly excluding production docline MDX/.markdown integration). Fixing this requires either detecting duplicate normalized output paths and failing clearly, or defining a collision-free output naming policy, in src/docline/app.py and related output-contract code -- a production-behavior change outside the temporary HashiCorp MDX normalizer contract already authorized for this cycle. Source refs: shipment=062-S, feature=071-F, task=N/A (not tied to a specific 071-F task), PR=192, thread=PRRT_kwDOSsAX4c6hnt1k. requires_deliberation: true. Kind: bug, provisional priority: medium.","created_at":"2026-09-12T04:09:11.9834109Z"}
{"id":"360FB708","priority":"high","kind":"bug","text":"DEFERRED SCOPE EXPANSION: production `.markdown` ingestion changes in src/docline/app.py (lines ~48-51) contradict the PR summary and committed plan, which state that this requirements-evidence tool (scripts/hashicorp_mdx_normalize.py) lives entirely outside src/docline and that production docline integration is out of scope (Copilot review finding nt3T, PR 192, src/docline/app.py:48). Out of scope per P-021 C1: shipment 062-S / feature 071-F's authorized contract is a standalone disposable Python preprocessor plus helper package and tests; splitting the pre-existing `.markdown` production feature into its own properly-scoped PR (or formally revising this PR's scope, backlog traceability, readiness evidence, and verification narrative to cover the production behavior change) is separate deliberation-worthy work, not a completion of the temporary HashiCorp MDX normalizer contract already authorized for this cycle. Source refs: shipment=062-S, feature=071-F, task=N/A (not tied to a specific 071-F task), PR=192, thread=PRRT_kwDOSsAX4c6hnt3T. requires_deliberation: true. Kind: bug, provisional priority: high.","created_at":"2026-09-12T04:10:25.6733447Z"}
{"id":"8FA344D0","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: a `.markdown` source is emitted as `.md` by `_relative_output_path()`, but the new branch in src/docline/process/cross_doc_links.py (~line 157) preserves `.markdown` in the link body and target metadata, so `this.markdown` linking to `other.markdown` produces `this.md` with a link to a nonexistent `other.markdown` output (Copilot review finding p8Ps, PR 192, src/docline/process/cross_doc_links.py:157). Out of scope per P-021 C1: this is pre-existing production cross-document-link rewriting behavior in src/docline predating shipment 062-S / feature 071-F, whose authorized contract is a standalone disposable Python preprocessor plus helper package and tests, explicitly excluding production docline MDX/.markdown integration. Fixing this requires rewriting emitted relative link targets to the corresponding `.md` output (while still preserving source identity for graph metadata where needed) and covering it end to end -- production link-rewriting behavior outside the temporary HashiCorp MDX normalizer contract already authorized for this cycle. Source refs: shipment=062-S, feature=071-F, task=N/A (not tied to a specific 071-F task), PR=192, thread=PRRT_kwDOSsAX4c6hp8Ps. requires_deliberation: true. Kind: bug, provisional priority: medium.","created_at":"2026-09-12T04:10:25.8163592Z"}
{"id":"87BFB31B","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: accepting `.markdown` in src/docline/process/output_contract.py (~line 291) creates a new output-path collision: `_relative_output_path()` maps both `guide.md` and `guide.markdown` to `guide.md`, and `write_markdown_output()` overwrites existing files (src/docline/process/output.py:19-21), so a repository containing both source files loses one document while both may still appear in manifests (Copilot review finding qAs7, PR 192, src/docline/process/output_contract.py:291). Out of scope per P-021 C1: this is pre-existing production output-contract behavior in src/docline predating shipment 062-S / feature 071-F, whose authorized contract is a standalone disposable Python preprocessor plus helper package and tests, explicitly excluding production docline MDX/.markdown integration. Fixing this requires detecting duplicate derived output paths and failing, or defining a collision-free mapping, before enabling the second extension -- the same underlying output-collision defect already flagged separately as finding nt1k (src/docline/app.py), confirming this is one pre-existing production-contract gap surfaced at two call sites, not two independent issues. Source refs: shipment=062-S, feature=071-F, task=N/A (not tied to a specific 071-F task), PR=192, thread=PRRT_kwDOSsAX4c6hqAs7. requires_deliberation: true. Kind: bug, provisional priority: medium.","created_at":"2026-09-12T04:10:25.9434463Z"}
{"id":"7F80C39E","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: fixing Copilot review finding qAsc (review-fix cycle 4, PR 192) removed an unsafe `if name in _KNOWN_HANDLED_TAGS: continue` skip from `classify_remaining_constructs()` in scripts/_hashicorp_mdx/normalize.py, so the classifier now correctly reports previously-hidden unresolved MDX construct residue. Re-running the real-corpus dry-run integration test (test_real_corpus_dry_run_zero_writes_and_coverage_report) after that fix surfaced 4 genuinely distinct, previously-invisible pipeline gaps against the live external corpus, none of which share qAsc's root cause (nested same-tag elements defeating a non-greedy regex) and none of which are required to complete the qAsc classification fix itself: (1) `<Note>` opening a list-item-indented block (boundary/v1.0.x/content/docs/workers/manage.mdx) is not matched by transform_callouts's line-anchored regex; (2) `<EnterpriseAlert inline />` embedded mid-sentence inside running list-item prose (multiple consul/v2.0.x files, e.g. api-docs/acl/index.mdx) is not recognized in that inline-embedded position; (3) a bare self-closing `<Warning/>` with no attributes or body (consul/v2.0.x/content/docs/monitor/telemetry/appdynamics.mdx) is not handled by the paired-tag Warning transform; (4) `<VideoEmbed url=\"...\">...</VideoEmbed>` (well-architected-framework/.../design-control-data-management-plane.mdx, 8 occurrences) is not matched by the VideoEmbed transform's expected attribute/shape. Out of scope per P-021 C1: none of these 4 gaps are required by the qAsc Copilot comment (which was specifically about the classifier's unsafe skip, already fixed and unit-tested with a synthetic nested-Note regression), and each requires separate investigation and a shape-specific pipeline fix in scripts/_hashicorp_mdx/normalize.py -- collectively a substantially larger body of work than the single-comment fix already authorized for this review-fix cycle. The real-corpus integration test's `unresolved_constructs == {}` assertion has been updated in this same commit to assert the actual, now honestly-reported baseline (EnterpriseAlert: 12, Note: 1, VideoEmbed: 8, Warning: 2) instead of a `{}` bar that was only ever passing because of the classifier bug just fixed; the test remains a regression guard against this documented baseline. Source refs: shipment=062-S, feature=071-F, task=N/A (self-discovered via qAsc fix side effect, not tied to a specific 071-F task), PR=192, thread=N/A (no review thread exists for this self-discovered finding; threadless capture per P-021 C2/C3 Step 4.4a). requires_deliberation: true. Kind: bug, provisional priority: medium.","created_at":"2026-09-12T04:10:26.134226Z"}
{"id":"F49E7D65","priority":"low","kind":"bug","text":"DEFERRED SCOPE EXPANSION: importing the `_hashicorp_mdx` helper package on a fresh checkout can create scripts/_hashicorp_mdx/__pycache__/*.pyc bytecode-cache files as a side effect of the Python interpreter's normal module-import machinery, which a Copilot review comment characterized as a violation of the tool's advertised dry-run \"zero writes\" guarantee (Copilot review finding, PR 192, scripts/hashicorp_mdx_normalize.py:152, from the re-review posted against commit 8dac2ee). Out of scope per P-021 C1: the already-authorized change for review-fix cycle 4 was the fix-and-reply-and-resolve pass over the 13 Copilot threads enumerated at the start of the cycle (9 in-scope fixes + 4 deferred), plus directly-caused consequences of those 9 fixes. This finding is not one of the original 13 and is not a direct consequence of any of the 9 fixes made in this cycle -- none of them touch Python import mechanics or bytecode caching. The tool's established \"zero writes\" dry-run contract (design doc SS2/9.1) was scoped to the tool's own designed output writes (--report, --dest), not to incidental interpreter-level bytecode caching of the helper package's own source files. Whether that established contract should be read to also cover this is a genuinely new scope question, not a completion of prior work, so it is deferred rather than silently fixed under the operator's one-cycle authorization for this pass. Fixing this would require wrapping the _hashicorp_mdx import with sys.dont_write_bytecode = True or relying on PYTHONDONTWRITEBYTECODE, plus a subprocess-based test that runs from a cache-free copy of the script tree and asserts no __pycache__ appears -- and a scoping decision on whether this even belongs in a disposable, one-time-use tool (design doc SS1: \"This tool is not, and will never become, a permanent part of docline\") versus being accepted residual behavior. Source refs: shipment=062-S, feature=071-F, task=N/A (pre-Ship-agent-task disposable-script feature), PR=192, thread=PRRT_kwDOSsAX4c6htGvx, comment=PRRC_kwDOSsAX4c7uIBea. requires_deliberation: true. Kind: bug, provisional priority: low (pre-existing CPython behavior, disposable-tool context, no data-integrity or containment impact; re-prioritization is Stage-only).","created_at":"2026-09-12T04:32:44.9988179Z"}
{"id":"F42911A1","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: guard_read_path/_iter_files_sorted validate each resolved leaf only against the overall --source root, so a symlink inside one selected product/version tree (e.g. vault/v2.x/some.mdx) can point across a version boundary to a DIFFERENT version tree under the same --source (e.g. vault/v1.20.x/index.mdx) and be silently accepted as containment-valid, letting non-latest-version content leak into the latest-version-only output selection without any containment violation being raised (Copilot review finding, follow-up round 5, PR 192, scripts/hashicorp_mdx_normalize.py:585, flagged as \"Previously missed -- in code that hasn't changed since the last review\"). Out of scope per P-021 C1: this is pre-existing containment-guard code that predates review-fix cycle 4 and was not touched by any of this cycle's fixes (p8PF/htL40 addressed escape OUTSIDE --source entirely, htcWC narrowed the TOCTOU window for that same overall-source guard -- neither authorized or implied validating against a per-product/version-specific root instead of the overall source root). Completing this would require plumbing the selected product/version root through to guard_read_path's call sites (or adding a second, narrower guard layer) to enforce version-selection integrity, a materially different guarantee (correct version scoping) than the already-authorized guarantee (no read/write escapes --source), and is a design change, not a completion of the exact temporary HashiCorp MDX normalizer contract already authorized this cycle. Source refs: shipment=062-S, feature=071-F, task=N/A (pre-Ship-agent-task disposable-script feature), PR=192, thread=N/A (no review thread was created for this finding -- it was posted only in the review summary body's \"Suppressed comments\" section with 0 new inline comments generated; threadless capture per P-021 C2/C3 Step 4.4a). requires_deliberation: true. Kind: bug, provisional priority: medium (data-integrity-adjacent -- could serve stale/non-latest content -- but requires a deliberate design decision on guard scoping, not an urgent security escape; re-prioritization is Stage-only).","created_at":"2026-09-12T05:36:56.3683723Z"}
{"id":"F144B331","priority":"low","kind":"bug","text":"DEFERRED SCOPE EXPANSION: the --report defense-in-depth collision recheck in main() (both occurrences, near lines 1125 and 1268) compares the resolved report path against `planned_dest_paths`, a set fully computed during the read-only preflight pass; if the --source corpus gains or renames a file between that preflight and the later --execute write pass, the write pass can create a NEW document at a path the stale preflight-era `planned_dest_paths` set never recorded, so this recheck misses the collision and the JSON report write can silently overwrite that newly-created document (Copilot review finding, follow-up round 6, PR 192, thread htp9L, scripts/hashicorp_mdx_normalize.py:1268). Out of scope per P-021 C1: none of this cycle's fixes (case-fold correctness for htTX0, the read-path TOCTOU narrowing for htcWC, the pyright cleanup for qAsu) touch this recheck's fundamental architecture -- they only changed HOW planned-path membership is compared (case-fold) or WHERE a resolved path is re-guarded before I/O (symlinks), never WHEN the `planned_dest_paths` snapshot itself is captured relative to the write pass. Closing this gap requires a materially different guarantee -- either re-scanning --source immediately before the final write to detect corpus mutation mid-run, or an explicit, deliberate decision to accept this as documented residual risk (the existing code already frames the surrounding checks as \"defense-in-depth only\" against a narrower two-check window, not against concurrent corpus mutation) -- which is a design question for Stage/deliberation, not a mechanical completion of any already-authorized fix. Source refs: shipment=062-S, feature=071-F, task=N/A (pre-Ship-agent-task disposable-script feature), PR=192, thread=PRRT_kwDOSsAX4c6htp9L, comment=PRRC_kwDOSsAX4c7uI2DM. requires_deliberation: true. Kind: bug, provisional priority: low (this is a single-operator-run, --source-corpus-controlled disposable tool per design doc SS1; concurrent external mutation of --source during a single synchronous run is a narrow threat model for this context, but re-prioritization is Stage-only).","created_at":"2026-09-12T05:48:19.3976682Z"}
{"id":"81815867","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: the blockquote-fence scanner in scripts/_hashicorp_mdx/normalize.py (around line 285, `_FENCE_OPEN_CORE_RE` and its associated opener/closer line matching) strips only leading whitespace before matching, so a standard CommonMark blockquote-fenced code block such as `> ```md` never matches the fence-open pattern (the `>` prefix, including nested `> > ` prefixes, is not recognized or retained); JSX-looking example content on subsequent `> ...` lines then reaches the transform pipeline and is silently rewritten, even though it is legitimately protected fenced-code content per CommonMark. The project's own existing blockquote-fence test uses a four-space-indented fence without any `>` prefix, so it does not exercise real blockquote fence syntax and did not catch this gap. Fixing requires parsing and retaining the blockquote-prefix structure (including nested prefixes) when recognizing opener/closer lines in the fence scanner, plus a new end-to-end `> ` + fenced-code regression test -- a materially different scanning contract than the fence maximality/backtracking fix this cycle already made to `_INLINE_CODE_RE` (finding p8PV/htp9L-adjacent). Flagged by Copilot review on PR #192 (062-S) at follow-up round 7, thread htvhC. Out of scope per P-021 C1: `git log -S \"_FENCE_OPEN_CORE_RE\"` confirms this regex/scanner was introduced in commit a898a7c (review-fix cycle 2), not by any commit in this cycle-4 session (8dac2ee onward); none of this cycle's authorized fixes (case-fold correctness, TOCTOU read-path narrowing, inline-code maximality, collision-check performance) touch blockquote-prefix parsing. Source refs: shipment=062-S, feature=071-F, task=N/A (pre-Ship-agent-task disposable-script feature), PR=192, thread=PRRT_kwDOSsAX4c6htvhC, comment=PRRC_kwDOSsAX4c7uI-Ax. requires_deliberation: true. Kind: bug, provisional priority: medium (JSX-inside-blockquote-fence is a plausible real-corpus pattern in HashiCorp docs, unlike some of the narrower edge cases already deferred, but still a design question for Stage/deliberation, not a mechanical completion of any already-authorized fix).","created_at":"2026-09-12T06:06:35.7230804Z"}
{"id":"19675EF9","priority":"low","kind":"bug","text":"DEFERRED SCOPE EXPANSION: main()'s execute preflight (around line 1151, the `process_corpus(source=source, dest=dest, execute=False, planned_dest_paths=planned_dest_paths)` preflight call and the parallel `--execute` write-pass call) only catches `ContainmentViolation` and `DestinationCollisionError`; an `OSError` raised while enumerating or reading the external `--source` corpus (for example, a permission change mid-run, or a file/directory disappearing between `source.iterdir()` calls) is not caught at all and escapes as a raw Python traceback with exit code 1, before any `--dest` claim is made, instead of one of the module's documented `EXIT_*` codes with an actionable message. The default dry-run handler (no `--execute`) has the identical gap for its own preflight call. Flagged by Copilot review on PR #192 (062-S) at follow-up round 8, thread ht1tX. Out of scope per P-021 C1: `git log -S \"except ContainmentViolation as exc:\"` confirms this narrow except-clause structure was already present at the original feature commit (c119c44) and both review-fix cycles 1 and 2 (7914294, a898a7c) -- pre-existing since before this review-fix cycle (cycle 4, 8dac2ee onward) even began; none of this cycle's authorized fixes (case-fold correctness, TOCTOU read-path narrowing, inline-code maximality, collision-check performance/correctness) touch main()'s top-level exception handling around process_corpus() calls. This is closely related to, but distinct from, stash entry 1E7CCBF7 (UnicodeDecodeError, a ValueError subclass not an OSError, during the actual per-file read/normalize step inside _process_one_product_tree, flagged during cycle 3) -- both point at the same underlying maintainability gap (main()'s exception handling around corpus processing is narrower than the module's own documented goal of never letting expected failure modes escape as a raw traceback), but cover different exception types at different call boundaries and should be tracked/fixed together if this is picked up. Source refs: shipment=062-S, feature=071-F, task=N/A (pre-Ship-agent-task disposable-script feature), PR=192, thread=PRRT_kwDOSsAX4c6ht1tX, comment=PRRC_kwDOSsAX4c7uJHAp. requires_deliberation: false (mechanical broadening of an except clause plus a new EXIT_* code, similar in shape to 1E7CCBF7, not a design question). Kind: bug, provisional priority: low (this is a single-operator, --source-corpus-controlled disposable tool per design doc SS1; a permission change or file disappearance mid-run during a single synchronous local run is a narrow threat model for this context, but re-prioritization is Stage-only).","created_at":"2026-09-12T06:16:10.3813961Z"}
{"id":"0F1A653C","priority":"high","kind":"bug","text":"DEFERRED SCOPE EXPANSION: Credential leak on the DEFAULT (non --execute) docline fetch path: orchestrate_fetch (src/docline/elt/orchestrate.py) calls create_staging_job(build_source_key(config)); create_staging_job (src/docline/fetch/staging.py:161) sets metadata.source = sanitize_source(source), a no-op on the web_crawl:/manifest_url: prefix, so the raw credentialed source_key (userinfo + ?token=...) is persisted to metadata.json AND printed to stdout (cli.py:381). Same leak class as D6E758F5 but a distinct function/call path (_execute_single_source is the only sink fixed under 072-F/063-S; this sink is NOT). Out of scope per P-021 C1: 063-S/072-F is strictly scoped to _execute_single_source per operator P-017 scope freeze; none of this cycle's authorized tasks (072.001-T sanitizer helper, 072.002-T redaction test, 072.003-T call-site wiring) touch orchestrate_fetch/create_staging_job, and closing this sink is materially distinct: create_staging_job receives an already-composed source_key STRING, not a typed SourceConfig, so the typed-config sanitizer contract adopted for 063-S (plan R3) cannot be applied verbatim and needs its own design decision. Source refs: shipment=063-S, feature=072-F, task=N/A (distinct un-tasked sink, not a 063-S work item), PR=N/A (surfaced by Stage adversarial multi-model review of 063-S staging artifacts 2026-09-12, not a PR review comment), thread=N/A, comment=N/A. requires_deliberation: true (string-only call site vs the typed-config contract). Kind: bug, provisional priority: high (default docline fetch path is a live credential sink; re-prioritization is Stage-only).","created_at":"2026-09-12T20:14:00.0000000Z"}
{"id":"06A59B1D","priority":"medium","kind":"task","text":"DEFERRED SCOPE EXPANSION: Expand credential redaction coverage: _CREDENTIAL_PARAM_PREFIXES (src/docline/fetch/staging.py) omits password/pwd/passwd/client_secret/refresh_token/code, and _sanitize_url does not redact path-embedded secrets. Out of scope per P-021 C1: expanding the shared _CREDENTIAL_PARAM_PREFIXES list also mutates the 059-S WARNING-path sanitizer, widening blast radius beyond D6E758F5; 063-S's authorized fix (072.001-T/072.002-T/072.003-T) reuses sanitize_source() WITHOUT altering _CREDENTIAL_PARAM_PREFIXES specifically to hold strict scope, so broadening the shared list is a materially different change requiring blast-radius deliberation before implementation. Source refs: shipment=063-S, feature=072-F, task=N/A (cross-cutting redaction-list change, not a 063-S work item), PR=N/A (surfaced by 063-S plan hardening plus Stage adversarial multi-model review 2026-09-12, not a PR review comment), thread=N/A, comment=N/A. requires_deliberation: true (blast radius across the 059-S WARNING path). Kind: task, provisional priority: medium (re-prioritization is Stage-only).","created_at":"2026-09-12T20:14:00.0000000Z"}
{"id":"27035E63","priority":"medium","kind":"task","text":"DEFERRED SCOPE EXPANSION: the operational-closure SKILL.md documents a generic closure-artifact output pattern (docs/closure/{YYYY-MM-DD}-{slug}-closure.md), but autoharness gate pipeline-topology's closure_complete predecessor-closure reader globs strictly for docs/closure/{shipment_id}-*-post-merge-closure.md with YAML frontmatter carrying compaction_status + closure_status. This undocumented, code-only naming/schema contract silently broke the very next shipment's pre_claim gate (063-S blocked on 062-S PREDECESSOR_CLOSURE_INCOMPLETE, closure_complete: null) even though 062-S's substantive closure work (compaction done, releasability READY) was fully complete -- it was only discoverable via the two prior 060-S/061-S artifacts' undocumented naming precedent, not from any written spec. Repaired for 062-S by adding docs/closure/062-S-071-F-post-merge-closure.md (a thin, additive, shipment-prefixed pointer artifact) without touching the existing narrative closure/runtime-verification docs. Out of scope per P-021 C1: this closure-evidence repair authorizes only adding the missing 062-S artifact, not changing the operational-closure SKILL.md template or the gate's glob/schema contract -- that is a design decision (update the skill's documented output pattern to match the gate, widen the gate to accept both patterns, or something else) requiring Stage deliberation, not a mechanical completion of this repair. Source refs: shipment=062-S, feature=071-F, task=N/A (post-hoc closure-evidence repair, not a shipment task), PR=N/A (pre-PR local finding), thread=N/A. requires_deliberation: true. Kind: task, provisional priority: medium (every future shipment's post-merge closure risks silently reproducing this same predecessor-closure gate failure until the contract is reconciled; re-prioritization is Stage-only).","created_at":"2026-09-13T05:25:12.9699665Z"}
{"id":"95BD0DC7","priority":"low","kind":"bug","text":"DEFERRED SCOPE EXPANSION: Exception __notes__ (PEP 678 add_note()) attached to a chained/wrapped exception are not scrubbed by the 072-F/063-S exception-chain scrubbing added in _clone_scrubbed_exception (src/docline/elt/execute.py); only the exception message and __cause__/__context__ chain are recursively cloned+scrubbed, so a credential embedded via .add_note(...) on any exception in the chain would leak into the ERROR log traceback unscrubbed. Out of scope per P-021 C1: closing this requires extending _clone_scrubbed_exception to also clone+scrub each __notes__ entry per exception in the chain, a materially additional code path beyond the reviewed C-1 fix (which only covers message/__cause__/__context__); currently ARCHITECTURALLY UNREACHABLE in this codebase (repo-wide search confirms zero call sites of add_note(/.add_note anywhere in src/docline as of this shipment), so there is no live exploitable leak today. Source refs: shipment=063-S, feature=072-F, task=N/A (post-review hardening finding, not a 063-S work item), PR=N/A (pre-PR, surfaced by post-remediation adversarial re-review of 063-S local branch, not a PR review comment), thread=N/A, comment=N/A. requires_deliberation: true (whether to harden proactively against a currently-unused Python 3.11+ feature). Kind: bug, provisional priority: low (verified currently unreachable; re-prioritization is Stage-only).","created_at":"2026-09-13T18:13:57.4397384Z"}
{"id":"709BDB53","priority":"low","kind":"bug","text":"DEFERRED SCOPE EXPANSION: ExceptionGroup/BaseExceptionGroup chains (PEP 654) are not traversed by the 072-F/063-S exception scrubbing added in _clone_scrubbed_exception (src/docline/elt/execute.py); the recursive scrub only walks __cause__/__context__ on a single exception object, not the .exceptions tuple of an ExceptionGroup, so a credential embedded in a sub-exception of a group would leak into the ERROR log traceback unscrubbed. Out of scope per P-021 C1: closing this requires a distinct traversal path (iterating/rebuilding via ExceptionGroup.derive(...)) beyond the reviewed C-1 fix scope; currently ARCHITECTURALLY UNREACHABLE in this codebase (repo-wide search confirms no TaskGroup/ExceptionGroup/asyncio.gather usage anywhere in src/docline as of this shipment; the crawl() path this shipment touches fetches sequentially), so there is no live exploitable leak today. Source refs: shipment=063-S, feature=072-F, task=N/A (post-review hardening finding, not a 063-S work item), PR=N/A (pre-PR, surfaced by post-remediation adversarial re-review of 063-S local branch, not a PR review comment), thread=N/A, comment=N/A. requires_deliberation: true (whether to harden proactively against a currently-unused Python 3.11+ concurrency pattern). Kind: bug, provisional priority: low (verified currently unreachable; re-prioritization is Stage-only).","created_at":"2026-09-13T18:14:04.933201Z"}
{"id":"6076A65E","priority":"low","kind":"bug","text":"DEFERRED SCOPE EXPANSION: Non-idempotent redaction passes in _scrub_exception_message (src/docline/elt/execute.py) can produce duplicated <redacted> sentinels (for example ...token=<redacted><redacted><redacted>) when the exact-substring-replace pass, the _HTTP_URL_RE full-URL pass, and the _redact_query_param_fragments scheme-less pass each independently re-match a value already redacted by an earlier pass in the same 3-pass pipeline. Confirmed by hand-trace: no credential byte is ever re-exposed at any stage (purely a log-quality/idempotency defect, not a security leak). Out of scope per P-021 C1: the 063-S authorized fix requires only that credentials be absent from logs/metadata, which this defect does not violate; making the 3-pass pipeline idempotent against its own <redacted> sentinel (e.g. skip re-matching a value that already equals or starts with <redacted>, or restructure the passes to run once against a single combined pattern) is a log-formatting quality improvement beyond the reviewed acceptance criteria. Source refs: shipment=063-S, feature=072-F, task=N/A (post-review hardening finding, not a 063-S work item), PR=N/A (pre-PR, surfaced by post-remediation adversarial re-review of 063-S local branch, not a PR review comment), thread=N/A, comment=N/A. requires_deliberation: false (mechanical idempotency fix, not a design question). Kind: bug, provisional priority: low (cosmetic log-quality defect only, no credential exposure; re-prioritization is Stage-only).","created_at":"2026-09-13T18:14:20.6589897Z"}
{"id":"4CEE1EA5","priority":"low","kind":"bug","text":"DEFERRED SCOPE EXPANSION: _is_url_shaped (src/docline/elt/source_keys.py) treats any string starting with a bare // prefix as URL-shaped regardless of whether a genuine authority/host follows, so a non-URL identifier that happens to start with // and contains exactly one @ (for example a value shaped like two-slash-prefix followed by comment-at-sign-author) is still routed through _strip_userinfo and gets its pre-@ segment stripped, corrupting a credential-free identifier. This is data corruption only, not a credential leak: build_source_key (the raw, unsanitized key used for job identity/determinism) is unaffected, only the human-readable sanitized key changes. Confirmed via direct execution during the 063-S final confirmation review (docs/closure/2026-09-13-sanitize-source-key-elt-error-paths-final-confirmation-review.md, finding U-3). Out of scope per P-021 C1: closing this requires tightening _is_url_shaped to require either a real scheme or a //-prefixed authority that also resembles a host token (contains a dot, or is followed by a port/path boundary) before triggering userinfo stripping, a materially additional design decision beyond the reviewed cycle-3 fix scope (which closed C-1/U-1/U-2, all credential-leak-class bugs; this is corruption-class and narrower). Source refs: shipment=063-S, feature=072-F, task=N/A (post-review hardening finding, not a 063-S work item), PR=N/A (pre-PR, surfaced by 063-S final confirmation adversarial re-review of the local branch, not a PR review comment), thread=N/A, comment=N/A. requires_deliberation: true (whether protocol-relative // identifiers are an intended supported shape for the affected fields). Kind: bug, provisional priority: low (data corruption on a narrow non-URL edge case, no credential exposure; re-prioritization is Stage-only).","created_at":"2026-09-13T19:21:25.777551Z"}
{"id":"1B5CEF80","priority":"low","kind":"task","text":"DEFERRED SCOPE EXPANSION: The \"currently architecturally unreachable\" characterization recorded on deferred stash entries 95BD0DC7 (R-4, exception __notes__ unscrubbed) and 709BDB53 (R-5, ExceptionGroup chains not traversed) is confirmed accurate for first-party call sites as of shipment 063-S/072-F (zero add_note(/TaskGroup/ExceptionGroup/gather usage repo-wide), but the qualifier should be understood narrowly as unreachable given docline own code today, not structurally impossible to trigger via any dependency. _clone_scrubbed_exception (src/docline/elt/execute.py) copies __notes__ verbatim and does not special-case BaseExceptionGroup.exceptions, so a future third-party dependency version bump that starts internally calling add_note() or raising an ExceptionGroup through a call site this shipment touches (the crawl runner, fetch_github_files, or a caller-supplied progress callback) would silently reopen this gap with no code change on the docline side. Confirmed via the 063-S final confirmation adversarial review (docs/closure/2026-09-13-sanitize-source-key-elt-error-paths-final-confirmation-review.md, finding M-1); not re-litigated as to whether R-4/R-5 should be fixed now, since that scope decision already stands. Out of scope per P-021 C1: this is a documentation-precision clarification to existing entries 95BD0DC7/709BDB53 acceptance language, not a code change, and re-prioritization/wording edits to existing entries are Stage-only. Source refs: shipment=063-S, feature=072-F, task=N/A (documentation-precision note, not a 063-S work item), PR=N/A (pre-PR, surfaced by 063-S final confirmation adversarial re-review, not a PR review comment), thread=N/A, comment=N/A. requires_deliberation: false (wording clarification only). Kind: task, provisional priority: low (advisory documentation note only, no code or security impact; re-prioritization is Stage-only).","created_at":"2026-09-13T19:21:36.8633915Z"}
{"id":"A6D7EEB9","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: `_strip_reversed_query_credentials`'s and `_remove_credential_query_params`'s query-token pre-pass in source_keys.py/execute.py only recognizes LITERAL `?`/`&`/`;` separators when splitting query components, so a percent-encoded separator (e.g. `%26` for `&`, `%3D` for `=`) still hides a nested credential-bearing key=value pair inside what appears to be a single, non-credential token -- for example `https://host/x?detail=1%26token%3DSECRET` reaches `sanitize_source()`/`_token_has_credential_name` as one `detail` token whose name is not credential-like, so the encoded `token=SECRET` pair survives re-encoding into both `metadata.source` and the ERROR log verbatim (Copilot review finding, PR #198, thread PRRT_kwDOSsAX4c6h76h8, comment PRRC_kwDOSsAX4c7ueCeW). Out of scope per P-021 C1: closing this requires a materially different mechanism than this cycles other three findings mechanical regex-quantifier/character-class extensions -- specifically, decoding candidate percent-encoded separator/assignment sequences WITHIN each already-split tokens value and re-scanning for a nested name=value pair, coordinated across at least two helper functions (`_split_query_component_preserving_separators` and `_token_has_credential_name`), without corrupting unrelated, legitimately percent-encoded non-credential values elsewhere in the same query string -- a design decision (full decode-and-rescan vs. typed urllib.parse.parse_qsl-based rearchitecture vs. another approach) requiring Stage deliberation, not a mechanical completion of this shipments already-authorized sanitizer helpers. Source refs: shipment=063-S, feature=072-F, task=N/A (PR-review finding, not a 063-S work item), PR=198, thread=PRRT_kwDOSsAX4c6h76h8, comment=PRRC_kwDOSsAX4c7ueCeW. requires_deliberation: true (decode-and-rescan architecture vs. parse_qsl-based rearchitecture is a genuine design choice). Kind: bug, provisional priority: medium (requires a deliberately percent-encoded query string, a plausible but non-default shape when copying URLs from browser address bars or config generators; re-prioritization is Stage-only).","created_at":"2026-09-13T21:27:15.35166Z"}
{"id":"96E6C3F2","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: `_HTTP_URL_RE`/`_QUERY_PARAM_TOKEN_RE` in execute.py (lines ~100 and ~392, `_scrub_exception_message`/`_sanitize_exception_text`) exclude `#` from their delimiter/character-class handling, so a fragment-only credential such as `/docs#token=SECRET` is never matched by either pattern and survives redaction verbatim in the ERROR log (Copilot review finding, PR #198, round 4, thread PRRT_kwDOSsAX4c6h8Ow0, comment PRRC_kwDOSsAX4c7ueggX). Out of scope for this cycle: PR #198s review-fix cycle count has reached the Ship agents non-negotiable circuit breaker limit of 3 cycles (round 1: 1 finding, round 2: 3 findings, round 3: 3 fixed + 1 deferred); per P-021 C4, reaching that limit does not authorize a 4th round of fixes regardless of whether an individual finding would otherwise pass the P-021 C1 mechanical-completion test, so this finding is deferred rather than fixed even though it may be a comparatively mechanical extension (adding # as a further bounded delimiter/boundary case, similar in kind to round 3s unterminated-quote/bracket fixes). Source refs: shipment=063-S, feature=072-F, task=N/A (PR-review finding, not a 063-S work item), PR=198, thread=PRRT_kwDOSsAX4c6h8Ow0, comment=PRRC_kwDOSsAX4c7ueggX. requires_deliberation: true (whether to reuse the fragment-aware source-key scanner referenced by the reviewer, or extend the existing regex alternation, is a design choice best made with fresh cycle budget). Kind: bug, provisional priority: medium (fragment-embedded credentials in URLs are a plausible real-world shape, but re-prioritization is Stage-only).","created_at":"2026-09-13T21:39:21.1128005Z"}
{"id":"7D7222E3","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: `_strip_reversed_query_credentials` (source_keys.py, lines ~180 and ~211) only inspects the OUTER query-string authority for a userinfo-bearing value; a nested URL supplied as a query VALUE (for example `https://host/x?redirect=https://user:SECRET@evil`) has a top-level key (`redirect`) that is not itself credential-like, so the pre-pass keeps the whole token, `sanitize_source()` percent-encodes the nested URL as an opaque string, and the final `sanitize_source_id()` only re-inspects the outer authority -- the nested credential survives verbatim into `metadata.source` (Copilot review finding, PR #198, round 4, thread PRRT_kwDOSsAX4c6h8Ow5, comment PRRC_kwDOSsAX4c7ueggi). Out of scope for this cycle: PR #198s review-fix cycle count has reached the Ship agents non-negotiable circuit breaker limit of 3 cycles; per P-021 C4, this bars a 4th round of fixes at this limit. Independently, per P-021 C1 this is also a materially different mechanism than a regex quantifier/character-class extension -- it requires recursively detecting and either scrubbing or fail-closed rejecting a nested URL-shaped value inside an already-split query token, without breaking legitimate non-URL query values that merely contain colons or slashes, coordinated across the query-splitting and authority-scrubbing helpers -- the same class of decode-and-rescan design question already flagged for stash entry A6D7EEB9 (percent-encoded separators), and arguably related enough that both should be triaged together. Source refs: shipment=063-S, feature=072-F, task=N/A (PR-review finding, not a 063-S work item), PR=198, thread=PRRT_kwDOSsAX4c6h8Ow5, comment=PRRC_kwDOSsAX4c7ueggi. requires_deliberation: true (recursive-scrub vs fail-closed-reject vs typed-parse rearchitecture, and whether to unify with A6D7EEB9s disposition, are genuine design choices). Kind: bug, provisional priority: medium (nested URL-as-query-value is a plausible shape, e.g. OAuth redirect_uri patterns; re-prioritization is Stage-only).","created_at":"2026-09-13T21:39:32.4423085Z"}
{"id":"E462E1F0","priority":"high","kind":"bug","text":"DEFERRED SCOPE EXPANSION (residual risk, C3(i) same-contract-surface completion): round-3s `_URL_SCHEME_RE` relaxation (`:/{1,2}` -> `:/*`, source_keys.py) fixed the reported malformed-scheme credential leaks but also widened `_is_url_shaped` to match ANY `scheme:value` string with zero slashes, including non-URL unrestricted identifiers that happen to contain a scheme-like colon prefix. For an identifier such as `release:owner@2026` (no real production source_key scheme is affected -- `manifest_url:`/`web_crawl:` both contain underscores, which fall outside `_URL_SCHEME_RE`s `[A-Za-z][A-Za-z0-9+.-]*` scheme-name character class and are NOT matched -- but a generic illustrative identifier shape used in the sanitizers own documented byte-for-byte-preservation contract IS affected), `_is_url_shaped` now returns True, `_contains_userinfo_marker`/userinfo-stripping fires on `owner@`, and `sanitize_source_id` returns `release:2026`, silently corrupting a credential-free value (Copilot review finding, PR #198, round 5, thread PRRT_kwDOSsAX4c6h8W8V, comment PRRC_kwDOSsAX4c7uesbY). Verified directly: release:owner@2026 -> release:2026, topic:general@v2 -> topic:v2, file:etc@secret -> file:secret (all corrupted); manifest_url:name@2026 and web_crawl:team@proj are correctly NOT url-shaped and pass through unaffected (the two schemes actually used by production source_key construction in this codebase). Per P-021 C1 this IS same-contract-surface (same `_URL_SCHEME_RE`/`_is_url_shaped` helper this cycles round-3 fix already modified -- narrowing zero-slash detection to genuine URL fields, or separating URL-shape detection from unrestricted-ID detection per the reviewers suggestion, would be a mechanical refinement of the exact regex just widened, not a materially different mechanism) and per C3(i) would normally be a MANDATORY in-scope fix, not a deferral candidate. It is deferred here SOLELY because PR #198s review-fix-cycle count has already reached the Ship agents non-negotiable circuit-breaker limit (3 cycles: round 1/2/3), and the operators DARK_MODE_ACTIVE contract lists both P-021 and the review-fix-cycle limit as independent stop conditions; per C3(ii) this residual-risk record is created precisely because deferring a same-contract-surface completion without one would itself be a P-021 violation. No production source_key scheme is exploitable today (manifest_url:/web_crawl: unaffected); the exposure is limited to the sanitizers documented general-purpose byte-for-byte-preservation guarantee for hypothetical non-underscore colon-prefixed identifiers, which the existing test suite regression guards (release:2026, topic:general, both without an @ sign) did not happen to exercise. Source refs: shipment=063-S, feature=072-F, task=N/A (PR-review finding, not a 063-S work item), PR=198, thread=PRRT_kwDOSsAX4c6h8W8V, comment=PRRC_kwDOSsAX4c7uesbY. requires_deliberation: true (whether to narrow to known URL schemes/fields, or split URL-shape detection from ID detection, is a design choice best made with fresh cycle budget, though the underlying necessity is not in question). Kind: bug, provisional priority: high (real byte-for-byte-preservation contract violation, verified reproducible, though currently inert against actual production source_key schemes; re-prioritization is Stage-only).","created_at":"2026-09-13T21:58:09.2971379Z"}
{"id":"E89DC095","priority":"high","kind":"bug","text":"DEFERRED SCOPE EXPANSION (same-contract-surface, C3(i)): `_URL_SCHEME_RE`s scheme-name character class (`[A-Za-z][A-Za-z0-9+.-]*`) excludes underscore, so a scheme name containing an underscore (e.g. `custom_scheme://user:pass@host`) is never recognized as URL-shaped by `_is_url_shaped`, `_sanitize_url_field` never routes it through `_strip_userinfo`/`sanitize_source_id`, and the raw credentialed value is returned unchanged. Because `validate_crawl_url` (src/docline/fetch/url_policy.py) only rejects unsupported schemes later in the fetch pipeline, ELT metadata/logging can already have persisted the raw unsanitized value before that later rejection occurs (Copilot review finding, PR #198, round 6, thread PRRT_kwDOSsAX4c6h8b_5, comment PRRC_kwDOSsAX4c7ue0VU). Verified directly: sanitize_source_id(\"custom_scheme://user:pass@host\") returns the value with credentials intact (is_url_shaped is False for this input). Per P-021 C1 this is same-contract-surface (the exact `_URL_SCHEME_RE` character class round-3s fix already modified) and per C3(i) would normally be a mandatory in-scope fix -- detecting authority after `://` independent of scheme-name character restrictions, or failing closed on an unrecognized-but-URL-shaped value, as the reviewer suggests. It is deferred here solely because PR #198s review-fix-cycle count has already reached the Ship agents non-negotiable circuit-breaker limit (3 cycles: round 1/2/3); per C3(ii) this residual-risk record is required because deferring a same-contract-surface completion without one would itself be a P-021 violation. Related to, but distinct from, stash entry E462E1F0 (same underscore-exclusion root cause, opposite failure mode: E462E1F0 is a false-positive/corruption case, this is a false-negative/leak case). No production docline source_key scheme is known to use an underscore-containing scheme name with a `://` authority form today (the two real schemes, manifest_url:/web_crawl:, use a bare colon, not `://`), but this is not independently verified against every possible caller/config the same way E462E1F0s blast radius was. Source refs: shipment=063-S, feature=072-F, task=N/A (PR-review finding, not a 063-S work item), PR=198, thread=PRRT_kwDOSsAX4c6h8b_5, comment=PRRC_kwDOSsAX4c7ue0VU. requires_deliberation: true (detect-authority-after-:// vs fail-closed-on-unrecognized-scheme vs another approach, best decided with fresh cycle budget). Kind: bug, provisional priority: high (verified reproducible credential leak in the same helper surface as this shipments core contract, even if not yet confirmed reachable via a real configured scheme; re-prioritization is Stage-only).","created_at":"2026-09-13T22:09:42.2928313Z"}
{"id":"BF028CAE","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: The known-scheme-allowlist fix for E462E1F0/E89DC095 (src/docline/elt/source_keys.py, _URL_SCHEME_RE / _has_authoritative_userinfo_context / _contains_userinfo_marker) widened _URL_SCHEME_RE's scheme-name character class to include underscore so that every one of docline's own compound source-key prefixes (web_crawl, github_repo, manifest_local, manifest_url, manifest_git, local_file) now itself parses as a URL scheme, where previously none did. If sanitize_source_id/_strip_userinfo were ever called on an already-prefixed compound key string containing a nested authority with credentials (for example a hypothetical prefix-colon-nested-URL-with-userinfo shape), _authority_span would locate the authority/userinfo boundary relative to the OUTER (prefix) colon rather than the nested URL's own scheme, truncating detection before the embedded credential and silently preserving it -- an adversarial-review finding (F2, docs/closure/2026-09-13-sanitize-source-key-elt-error-paths-e462e1f0-e89dc095-adversarial-review.md), majority confidence (2 of 3 reviewers), conservative severity CRITICAL. Independently verified NOT reachable via any current production call site: every existing caller sanitizes typed sub-fields (the .url string, the exception text) individually before any compound-prefix concatenation happens, and no call site re-feeds an already-prefixed compound key back through this module's sanitizer functions as a whole. Out of scope per P-021 C1: closing this requires re-architecting the authority-span algorithm to detect and skip past a recognized compound-prefix segment before locating the REAL nested scheme/authority (or otherwise separating outer-prefix parsing from inner-URL authority detection), a materially different mechanism than the known-scheme-allowlist extension this cycle's authorized E462E1F0/E89DC095 fix applied -- not a mechanical completion of that same regex/allowlist change. Source refs: shipment=063-S, feature=072-F, task=N/A (post-fix hardening finding, not a 063-S work item), PR=N/A (pre-PR local finding, surfaced by this cycle's mandated adversarial review of the uncommitted E462E1F0/E89DC095 diff, not a PR review comment), thread=N/A, comment=N/A. requires_deliberation: true (whether to re-architect authority-span detection to skip a recognized compound prefix, split URL-shape detection from ID-prefix detection entirely, or restrict the underscore-widened scheme match to only fire when no known compound-prefix separator precedes it, is a genuine design choice). Kind: bug, provisional priority: medium (real defense-in-depth gap, independently verified non-live against every current production call site; re-prioritization is Stage-only).","created_at":"2026-09-13T23:56:34.387793Z"}
{"id":"12925B3B","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: Copilot review round 8 (PR #198, thread PRRT_kwDOSsAX4c6h9PV6, comment PRRC_kwDOSsAX4c7ugDru, HEAD 48683e0) claims sanitize_source_id() only recognizes userinfo when the whole value starts with a URL-shaped scheme, and that a manifest ID such as an unrecognized-scheme-prefixed value with a strict \"://\" authority marker passes through unchanged, leaking the embedded userinfo credential, citing this file's lines 113, 199, and 313. Independently reproduced the exact literal example the comment provides (a 28-character unrecognized-scheme value in the form scheme-with-dash://user:pass@host) directly against the committed sanitize_source_id() implementation: the userinfo IS correctly stripped (output length 18, no \"@\", no \"pass\", \"://\" retained) because this is a strict two-slash authority marker, which _has_authoritative_userinfo_context treats as always authoritative regardless of scheme name -- so the literal reported reproduction does not confirm the claimed defect (a disputed/false-positive-style finding, similar in kind to the previously-disputed \"U-4\" claim in this shipment's review history). Lines 199 and 313 (cited as additional occurrences \"of the same file\") are inside docstring prose, not call sites, in the current HEAD. However, the comment's broader underlying concern -- that an UNRECOGNIZED scheme under a LOOSE (non-strict, zero/one/three-plus slash) authority match is deliberately NOT treated as authoritative, so a genuine credential embedded in an unrecognized-scheme, loose-match identifier (for example a bare \"unknownscheme:secret@host\" with zero slashes) is preserved unchanged rather than stripped -- is a real, already-documented, deliberate design tradeoff of the E462E1F0/E89DC095 known-scheme-allowlist fix itself (see the _has_authoritative_userinfo_context docstring and stash entry E462E1F0), not a new defect: it exists precisely because treating every unrecognized scheme as authoritative would reintroduce the E462E1F0 corruption regression on credential-free identifiers like release:owner@2026. Out of scope per P-021 C1 for this bounded cycle: distinguishing \"unrecognized scheme with a genuine credential\" from \"unrecognized scheme, non-credential identifier\" without a corpus of known network schemes requires a materially different mechanism (heuristic credential-shape detection, or an explicit fail-closed policy for all unrecognized schemes) than the allowlist-membership check the operator's E462E1F0/E89DC095 authorization covered, and was not raised as a blocking finding by the mandated 3-reviewer adversarial review of that same fix. Source refs: shipment=063-S, feature=072-F, task=N/A (post-fix PR-review finding, not a 063-S work item), PR=198, thread=PRRT_kwDOSsAX4c6h9PV6, comment=PRRC_kwDOSsAX4c7ugDru. requires_deliberation: true (whether to adopt a fail-closed policy for unrecognized schemes generally, add heuristic credential-shape detection, or explicitly accept the current documented tradeoff, is a design choice for Stage). Kind: bug, provisional priority: medium (the literal reported example does not reproduce; the underlying tradeoff is already documented and intentional, but the general unrecognized-scheme/loose-match credential-preservation gap remains a real, undecided design question; re-prioritization is Stage-only).","created_at":"2026-09-14T00:14:25.9924498Z"}
{"id":"9D44B6F3","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: _sanitize_url_field's leading-whitespace fail-closed guard (src/docline/elt/source_keys.py, `raw_url.lstrip()` at line ~216) only strips characters where `str.isspace()` is True; Unicode format-category (Cf) characters such as U+200B ZERO WIDTH SPACE, U+FEFF ZERO WIDTH NO-BREAK SPACE (BOM), or U+2060 WORD JOINER are NOT `str.isspace()`-true and are therefore never stripped, so a credentialed URL prefixed with one of these invisible characters defeats the `^`-anchored `_URL_SCHEME_RE` scheme check exactly like the leading-whitespace case the existing guard was built to close, and the raw credential-bearing value can be returned byte-for-byte unredacted. Flagged in PR #199's own body text as a follow-up finding, not a review-thread comment. Out of scope per P-021 C1: 063-S/072-F's authorized scope (072.001-T/072.002-T/072.003-T) is the reviewed leading-whitespace fail-closed guard itself; broadening it to strip or detect Unicode format characters is a materially different detection surface (Unicode category classification via `unicodedata.category`, not `str.isspace()`) requiring its own design decision, not a mechanical completion of the already-authorized fix. Source refs: shipment=063-S, feature=072-F, task=N/A (post-merge follow-up finding), PR=199, thread=N/A, comment=N/A. requires_deliberation: true (detection-surface design decision: which Unicode categories/code points to treat as format-character bypass vectors). Kind: bug, provisional priority: medium (a live bypass of the credential-redaction guard on the reviewed code path, narrowly scoped to this field/guard; re-prioritization is Stage-only).","created_at":"2026-09-14T03:47:32.6752423Z"}
{"id":"1C433464","priority":"medium","kind":"bug","text":"DEFERRED SCOPE EXPANSION: _contains_userinfo_marker's ambiguous-authority branch (src/docline/elt/source_keys.py, `if raw_authority.count(\"@\") != 1:` at line ~318) unconditionally treats more-than-one-`@` authorities as a credential marker (fail-closed, always redacts/sentinels), while the adjacent exactly-one-`@` branch is gated by `_has_authoritative_userinfo_context`'s known-scheme allowlist (the E462E1F0 regression guard) before treating a present userinfo segment as genuine credentials. This is a safe-direction (over-redaction, never under-redaction) asymmetry between the two branches -- an ambiguous-authority non-URL identifier containing two or more `@` characters (e.g. a manifest ID or path-like value) could be redacted/sentineled even when it is not actually URL userinfo, whereas the single-`@` case would correctly pass it through unredacted for an unrecognized scheme. Flagged in PR #199's own body text as a follow-up finding, not a review-thread comment. Out of scope per P-021 C1: 063-S/072-F's authorized scope did not touch `_contains_userinfo_marker`'s ambiguous-authority branch's own logic (it pre-dates this shipment, see the E462E1F0/round-3 fix history); reconciling the asymmetry (either gating the ambiguous-authority branch on the same allowlist, or documenting the asymmetry as an intentional fail-closed design choice) is a design decision, not a mechanical completion of any already-authorized fix. Source refs: shipment=063-S, feature=072-F, task=N/A (post-merge follow-up finding), PR=199, thread=N/A, comment=N/A. requires_deliberation: true (whether the asymmetry is intentional fail-closed design or should be reconciled with the allowlist). Kind: bug, provisional priority: medium (safe-direction over-redaction on an edge case, not a credential leak; re-prioritization is Stage-only).","created_at":"2026-09-14T03:47:49.7315928Z"}
{"id":"E7878B1B","priority":"low","kind":"task","text":"DEFERRED SCOPE EXPANSION: possible redundancy between `_remove_credential_query_params` and `_strip_reversed_query_credentials` (both in src/docline/elt/source_keys.py) inside `_sanitize_url_field`'s pipeline -- both functions independently tokenize and drop credential-named query/fragment tokens (one pre-pass before `sanitize_source`, one post-pass after the http(s):// prefix check), and it is not obvious from the current code structure alone whether both passes are still individually load-bearing for distinct input shapes or whether one now fully subsumes the other after the round-3/E462E1F0-era fixes layered on top. Flagged in PR #199's own body text as an advisory follow-up finding, not a review-thread comment. Out of scope per P-021 C1: 063-S/072-F's authorized scope added/wired the sanitizer call sites (072.001-T/072.002-T/072.003-T) without altering either existing function's internal logic or pass ordering; determining whether one pass can be safely removed requires tracing both functions' historical rationale and constructing input-shape coverage tests to prove equivalence, which is a design/verification exercise, not a mechanical completion of any already-authorized fix. Source refs: shipment=063-S, feature=072-F, task=N/A (post-merge follow-up finding), PR=199, thread=N/A, comment=N/A. requires_deliberation: false (mechanical code-reading/equivalence-proof exercise, not a design question, though it is advisory and non-urgent). Kind: task, provisional priority: low (advisory maintainability observation, no behavioral defect identified; re-prioritization is Stage-only).","created_at":"2026-09-14T03:47:50.2161058Z"}
{"id":"E0B6EE0D","priority":"low","kind":"task","text":"DEFERRED SCOPE EXPANSION: pre-existing cross-module private-symbol imports -- src/docline/elt/execute.py imports `_is_credential_name` and `_strip_reversed_query_credentials` directly from src/docline/elt/source_keys.py (both underscore-prefixed, module-private by convention), while src/docline/fetch/staging.py independently defines its own parallel `_is_credential_param` rather than sharing either. This cross-module private-symbol coupling is an encapsulation smell (execute.py depends on source_keys.py internals that carry no public-API stability contract) and a latent duplication risk (staging.py's separate implementation can drift from source_keys.py's without either failing loudly). Flagged in PR #199's own body text as an advisory follow-up finding, not a review-thread comment. Out of scope per P-021 C1: 063-S/072-F's authorized scope reused these existing private imports/functions as-is (072.001-T/072.002-T/072.003-T did not introduce this coupling, nor were they authorized to restructure module boundaries); promoting shared logic to a public/internal-but-documented shared module, or consolidating staging.py's parallel implementation, is an architectural decision requiring its own design review, not a mechanical completion of any already-authorized fix. Source refs: shipment=063-S, feature=072-F, task=N/A (post-merge follow-up finding), PR=199, thread=N/A, comment=N/A. requires_deliberation: false (mechanical refactor consideration once prioritized, though the choice of consolidation target is advisory). Kind: task, provisional priority: low (advisory maintainability observation; no behavioral defect identified; re-prioritization is Stage-only).","created_at":"2026-09-14T03:47:50.6681826Z"}
