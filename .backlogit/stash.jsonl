{"id":"7AA9FAA0","priority":"low","kind":"feature","text":"Add release / publish workflow (PyPI, GitHub Releases) once a 1.0 release is ready. Out of scope for 011-S which is CI-gate bootstrap only.","created_at":"2026-06-03T20:12:22.8897875Z"}
{"id":"4CA80776","priority":"low","kind":"task","text":"[Docling OCR tuning, plan-review F3 from 015-S] Enable `PdfPipelineOptions(do_ocr=True)` in `_read_pdf_docling_pages` to extract text from scanned/image-only PDFs that the current pipeline returns empty for. Defaults to docling's bundled OCR engine (likely Tesseract or EasyOCR). Lower priority because the current heuristic baseline also produces no text for scanned PDFs — enabling OCR is an enhancement, not a regression fix. Closure: docs/closure/015-S-post-g3-hygiene.md.","created_at":"2026-06-04T18:42:43.8263835Z"}
{"id":"A2A78AEE","priority":"high","kind":"task","text":"[Task, refined by 2026-06-05 RCA] Build scripts/load_test.py harness for full-corpus PDF load tests. Workflow per PDF: (1) call resource_probe.probe() once (stash 1D945AB5), log the ResourceBudget so the run is reproducible; (2) if PDF size or page count exceeds probe limits, call the splitter (stash F64683BC) to produce N chunks under `.elt/splits/{source_hash}/chunk-NNNN.pdf`; (3) for each PDF (or chunk), fork a child via subprocess.run([sys.executable, \"-m\", \"docline\", \"process\", ...]) so child OOM (docling rt_detr CPU allocator failure on hostile PDFs like azure-cosmos-db.pdf 109MB) reaps cleanly without taking down the harness; (4) wrap each child with psutil to sample peak RSS, elapsed time, exit code; (5) when probe.serialize_docling=True, hold to one docling child at a time with 10-30s reclaim pauses between calls; otherwise allow up to probe.recommended_concurrency concurrent children; (6) emit TSV row per child: {file, chunk, mb, pages, engine, exit, elapsed_s, peak_rss_mb, output_parts, fallback_reason, probe_available_gb, probe_concurrency, probe_serialize}; (7) on child non-zero exit, retry the same chunk with explicit --pdf-engine heuristic and record both rows; (8) after all chunks process, invoke the stitcher (stash D885CE79) to produce one logical document output. Runs in three tiers: small (≤10MB), medium (10-30MB), large (>30MB). Sets OMP_NUM_THREADS / MKL_NUM_THREADS / OPENBLAS_NUM_THREADS to budget.logical_cpus//4 in the child env. Output TSV is BOTH the regression baseline AND the empirical evidence that feeds the threshold spike (4B913619). **Operator constraint**: must be run from a plain PowerShell session, NEVER inside an agent tool call — the Copilot CLI co-hosted with docling triggered the 2026-06-04 paging spiral / hard reboot. Depends on shipment A (probe, gate, exception net, thread caps) AND shipment B (splitter, batch, stitch). See docs/memory/2026-06-05/rca-2026-06-04-load-test-system-oom.md Phase 1 of recommended approach.","created_at":"2026-06-06T01:50:58.7354775Z"}
