{"id":"7AA9FAA0","priority":"low","kind":"feature","text":"Add release / publish workflow (PyPI, GitHub Releases) once a 1.0 release is ready. Out of scope for 011-S which is CI-gate bootstrap only.","created_at":"2026-06-03T20:12:22.8897875Z"}
{"id":"ED74577A","priority":"medium","kind":"task","text":"Add cross-OS CI matrix (Windows + macOS) once Windows `tmp_path` PermissionError root cause is identified and fixed. Builds on 011-S (Linux-only CI).","created_at":"2026-06-03T20:12:22.9751642Z"}
{"id":"0AA8B223","priority":"low","kind":"task","text":"Investigate root cause of Windows `tmp_path` `PermissionError` teardown noise (176+ entries per pytest run). Deeper than the local-dev workaround documented in CONTRIBUTING.md from 011-S. Blocks the cross-OS CI matrix follow-up.","created_at":"2026-06-03T20:12:23.0207101Z"}
{"id":"90695245","priority":"high","kind":"feature","text":"[G3a] Replace page/char-based output splitting with heading-aware semantic segmentation. Current behavior (src/docline/process/output_contract.py): PDF splits per physical page via read_pdf_pages(); DOCX splits via _chunk_text_blocks(blocks, 6000)=greedy char bins. Both cut mid-section, breaking AST/Tree-sitter graphing. Proposed: new src/docline/process/segment.py that parses rendered markdown with markdown-it-py (already a dep) into an AST and splits at H1 boundaries (hard), sub-splits at H2 when a part exceeds max_chars (default 12k), and falls through to current char-bin behavior as the safety net only. Applies to both PDF (after extractor output) and DOCX (after read_docx_blocks). Web crawl already one-page-per-file — no change. Tests: add tests/process/test_segment.py covering H1-only, H1+H2, oversize H1, no-heading-fallback. Touches output_contract.py and one new module; no schema break (filenames still part-NNNN.md but content is semantically bounded). Source: operator-raised concern that current splits destroy chapter/section continuity needed for AST-based graphing.","created_at":"2026-06-04T02:11:35.0616439Z"}
{"id":"C5CA1740","priority":"high","kind":"feature","text":"[G3b] Add in-frontmatter referentiality + emit chunk anchors for AST/Tree-sitter graphing. Currently per-part .md files are structurally orphaned ΓÇö only manifest.json carries part order. _inject_chunk_anchors() exists in src/docline/process/assemble.py but emit_chunk_anchors=False by default and is never set True. Proposed additions under the BaseFrontmatter 'docline:' namespace (no v1 contract break ΓÇö additionalProperties: true): document_id (already in manifest), parent_document_id (same id for all parts of a source), part_index (1-based), total_parts, prev_part (relative path or null), next_part, section_title (H1 anchoring this part once G3a lands). Also: flip emit_chunk_anchors default to True in process pipeline so every .md ships chunk-NNNN anchors usable by graphtor's existing markdown_it/Tree-sitter parsers. Tests: tests/process/test_referentiality.py asserting part-0002.md has prev_part=part-0001.md, total_parts matches manifest count, anchor injection survives heading hierarchy. Re-export schema after change; update snapshot at d:/Source/GitHub/graphtor-docs/schemas/docline/base-frontmatter-v1.schema.json. Source: operator-raised concern that individual part files lack referentiality to enable graph-builder reconstruction.","created_at":"2026-06-04T02:11:50.6270176Z"}
{"id":"351170C9","priority":"high","kind":"feature","text":"[G3c] Wire docling as PDF engine + sidecar image extraction for DOCX/PDF to preserve tables, figures, code blocks, and layout structure in emitted markdown. Current behavior: pypdf returns flat text only (0 tables, 0 figures from ~50 .md from real PDFs); DOCX has _render_table() and emits GFM tables when <w:tbl> present, but 31 <w:drawing> elements per source DOCX produce 0 image references. Proposed (4 sub-pieces): (1) Add [project.optional-dependencies] pdf=['docling>=2,<3'] to pyproject.toml (no current optional-deps section ΓÇö establishing the pattern). (2) Default read_pdf_pages(layout_engine='auto') ΓÇö resolve to 'docling' when dependencies.pdf_available() else 'heuristic'. Expose CLI flag 'docline process --pdf-engine {auto,docling,heuristic}' and YAML config 'pdf_engine: auto' per source. _read_pdf_docling_pages already exists in src/docline/readers/pdf.py:471 ΓÇö just needs wiring. (3) DOCX image sidecar: walk <w:drawing>/<a:blip r:embed>, resolve via word/_rels/document.xml.rels, extract word/media/imageN.png to {output_root}/{job_id}/media/figure-NNNN.png, emit ![alt](media/figure-NNNN.png) at source position. (4) PDF image sidecar: configure docling PdfPipelineOptions(images_scale>0) and thread output through to same media/ dir. Manifest gains media_files: [...]. RISK: docline venv is Python 3.14.0; docling 2.97.0 supports >=3.10 but its transitive ML deps (torch, onnxruntime) may lack 3.14 wheels (smoke install hung silently for >5min, likely source-building). Plan must include: probe ecosystem compatibility, optionally pin pdf-extra to Python <3.14 in pyproject classifier, or document waiting for upstream wheels. Tests: tests/fixtures/sample-with-tables.docx (real <w:tbl>), sample-with-images.docx (<w:drawing>+embedded PNG), sample-multi-heading.pdf for docling layout. Source: operator-raised hard requirement that markdown formatting (tables, figures, lists) survive into output to enable AST/Tree-sitter graphing.","created_at":"2026-06-04T02:12:08.8987024Z"}
