---
title: "Shipment reconcile - 062-S pre"
shipment: "062-S"
mode: "pre"
expected_status: "done"
recommendation: "PROCEED"
---

## Summary

All manifest items for shipment `062-S` were found archived under
`.backlogit/archive/` (classified `pre-archived`) prior to any closure
mutation this session. The shipment record itself remains `active` in
`.backlogit/queue/062-S.md`. This is expected: PR #192 (merge commit
`d4a534e241c5daae592da12883ae0a522f9e762c`) already carried the covering
feature and all task completions/archival through to `main` as part of the
shipped code change; only the shipment record's own `active -> shipped`
closure and its own archival remain outstanding.

## Classifications

* `pre-archived` (12 of 12 manifest items, all declared `status: done`,
  none carrying `archived_status`/`archived_from` provenance — terminal
  relocation representation):
  `071-F`, `071.001-T`, `071.002-T`, `071.003-T`, `071.004-T`, `071.005-T`,
  `071.006-T`, `071.007-T`, `071.008-T`, `071.009-T`, `071.010-T`,
  `071.011-T`
* `matched`: none (no manifest item remains in `.backlogit/queue/`)
* `orphan`: none (queue scan found no file declaring `shipment_id: 062-S`
  outside the manifest)
* `missing`: none
* `status-mismatch`: none

## Shipment-Record-Status Classification

* Record status: `active` -> `record-consistent` (by definition; `active`
  is always consistent per the classification table — out of scope for the
  queued/blocked-only inconsistency cases).

## Evidence

* `backlogit shipment get 062-S` returned the 12-item manifest
  (`071-F` + `071.001-T`..`071.011-T`), record `status: active`.
* `Get-ChildItem .backlogit/queue -Filter "071*"` returned zero files.
* `Get-ChildItem .backlogit/archive -Filter "071*"` returned all 12 manifest
  items, each `status: done`.
* Queue-wide frontmatter scan for `shipment_id: 062-S` (or equivalent
  manifest-membership marker) outside the manifest found no orphans.
* Task-artifact filter applied per the record-status classification: all
  11 task items are `artifact_type: task`; `071-F` (`artifact_type:
  feature`) excluded from the task aggregate as required.

## Recommendation

PROCEED
