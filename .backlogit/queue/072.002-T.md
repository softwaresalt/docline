---
artifact_type: task
created_at: 2026-09-12T20:07:08.9052964Z
dependencies:
    - 072.001-T
id: 072.002-T
labels:
    - security
    - elt
    - tests
parent_id: 072-F
priority: high
references:
    - docs/plans/2026-09-12-source-key-credential-sanitization-plan.md
status: queued
title: Author failing redaction test for source_key leak (test-first RED)
updated_at: 2026-09-13T00:25:00.0000000Z
---

PLAN Unit 2 (tests; test-first RED). Update tests/elt/test_elt_real_execution.py::test_url_fetch_failure_logs_source_key_and_job_id to encode the redaction contract BEFORE the call-site is wired (Unit 3). Keep asserting job_id in record.message and exc_info is not None. Assert persisted metadata.source == sanitize_source_key(config) (R7 typed-config contract; url sanitized fail-closed + id credential-redacted independent of URL detection with decoded-name marker matching; helper TOTAL/non-throwing). Add a credential-bearing crawl URL (userinfo + ?token=SECRET) case asserting the literal 'SECRET'/userinfo substrings are ABSENT from caplog.text (FULL rendered record incl. traceback) AND from the written metadata.json text. Also add a ManifestUrlSource case whose id is a NON-URL-form credential-bearing string (srcA?token=IDSECRET, no scheme) and assert IDSECRET is ABSENT from both caplog.text and metadata.json. Also add a MALFORMED credentialed config whose URL would make _sanitize_url raise (https://host:notaport?token=SECRET) and assert _execute_single_source does NOT crash with a sanitizer ValueError (it proceeds to the normal fetch-failure path), the ERROR log records the fetch failure rather than a masking sanitizer error, and SECRET is ABSENT from both caplog.text and metadata.json (R6 totality/fail-closed regression). Also add a percent-encoded credential-name manifest id (srcA?%74oken=IDSECRET, opaque) and assert IDSECRET is ABSENT from both caplog.text and metadata.json (R7 encoded-name/decoded-detection regression: the raw marker scan would miss %74oken). The injected crawl failure MUST raise an exception whose message embeds the credentialed config.url (NOT a URL-free message like OSError('Network down')) so the traceback assertion is a genuine RED. Assert job_id == make_job_id(build_source_key(config)) recomputed independently from the raw credentialed key (determinism oracle; an impl hashing the SANITIZED key must FAIL this). Test is authored to FAIL (RED) against current code. Files: tests/elt/test_elt_real_execution.py. Size: S | Complexity: low. Depends on 072.001-T. ACCEPTANCE CRITERIA: (1) test imports/uses sanitize_source_key; (2) credential-absence asserted against caplog.text and metadata.json for a credentialed crawl url, a non-URL-form credential-bearing manifest id, a malformed credentialed url that would make _sanitize_url raise -- with no sanitizer ValueError escaping _execute_single_source (R6), AND a percent-encoded credential-name manifest id srcA?%74oken=IDSECRET (R7 decoded-name detection); (3) job_id asserted via independent raw-key make_job_id recomputation (not merely 'job_id in message') and exc_info is not None retained; (4) test is RED against current unwired code and will pass only after Unit 3.
