---
artifact_type: task
created_at: 2026-09-12T20:07:08.9052964Z
dependencies:
    - 072.001-T
id: 072.002-T
labels:
    - security
    - elt
    - tests
parent_id: 072-F
priority: high
references:
    - docs/plans/2026-09-12-source-key-credential-sanitization-plan.md
status: queued
title: Author failing redaction test for source_key leak (test-first RED)
updated_at: 2026-09-12T20:07:08.9052964Z
---

PLAN Unit 2 (tests; test-first RED). Update tests/elt/test_elt_real_execution.py::test_url_fetch_failure_logs_source_key_and_job_id to encode the redaction contract BEFORE the call-site is wired (Unit 3). Keep asserting job_id in record.message and exc_info is not None. Assert persisted metadata.source == sanitize_source_key(source_key). Add a credential-bearing crawl URL (userinfo + ?token=SECRET) case asserting the literal 'SECRET'/userinfo substrings are ABSENT from caplog.text (FULL rendered record incl. traceback) AND from the written metadata.json text; the injected crawl failure MUST raise an exception whose message embeds the credentialed config.url (NOT a URL-free message like OSError('Network down')) so the traceback assertion is a genuine RED. Assert job_id == make_job_id(build_source_key(config)) recomputed independently from the raw credentialed key (determinism oracle; an impl hashing the SANITIZED key must FAIL this -- the credential-free parity test cannot catch it). Test is authored to FAIL (RED) against current code. Files: tests/elt/test_elt_real_execution.py. Size: S | Complexity: low. Depends on 072.001-T. ACCEPTANCE CRITERIA: (1) test imports/uses sanitize_source_key; (2) credential-absence asserted against caplog.text and metadata.json; (3) job_id asserted via independent raw-key make_job_id recomputation (not merely 'job_id in message') and exc_info is not None retained; (4) test is RED against current unwired code and will pass only after Unit 3.