---
artifact_type: task
created_at: 2026-06-11T15:13:45.7312343-07:00
dependencies:
    - 028.002-T
id: 028.003-T
parent_id: 028-F
priority: medium
references:
    - docs/plans/2026-06-11-invert-triage-scoring-plan.md
status: queued
title: T3 — Wire --triage-pre-score CLI + MCP + ProcessRequest + pdf_triage short-circuit
updated_at: 2026-06-11T15:13:45.7312343-07:00
---

Wire the pre-triage scoring flag through the full surface:

1. ProcessRequest (app_models.py): add `triage_pre_score: bool = False` and `triage_pre_score_report_only: bool = False` fields. Both default off for backward compatibility with existing --pdf-mode triage runs.

2. CLI (cli.py): add `--triage-pre-score` and `--triage-pre-score-report-only` flags to the `process` subparser. Update docstrings.

3. MCP manifest: extend the `process` tool manifest schema to include the new bool flags.

4. pdf_triage.py main pass: when triage_pre_score=True, call pre_triage_score before heuristic extraction. Branch on classification:
   - route_to_docling: skip heuristic, add page to docling ranges immediately
   - route_to_heuristic: accept heuristic output without invoking post-extraction scorer
   - uncertain: continue through existing heuristic + post-extraction scoring path
5. triage_pre_score_report_only mode: short-circuit after pre-scoring all pages, emit per-page TSV with 5 signal scores + classification + reason, no docling.

TDD. Tests in tests/process/test_pdf_triage_pre_score.py (new) — 4 tests:
- --triage-pre-score=False (default) produces same output as today's --pdf-mode triage
- --triage-pre-score=True short-circuits docling-classified pages (verified via mock heuristic that asserts it's not called)
- --triage-pre-score-report-only emits TSV with correct columns, no docling subprocesses spawned
- ProcessRequest validates both flags as bool