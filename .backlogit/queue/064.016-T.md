---
artifact_type: task
created_at: 2026-08-27T15:07:50.3164314Z
dependencies:
    - 064.013-T
id: 064.016-T
labels:
    - fetch
    - security
    - dos
    - tests
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Author shared-fetch aggregate byte-accounting harness
updated_at: 2026-08-27T15:10:22.0852472Z
---

Author the RED unit harness (tests/fetch/) proving the aggregate MAX_TOTAL_FETCH_BYTES budget counts RAW WIRE BYTES, not decoded characters or a re-encode. Tests domain only. This isolates the byte-accuracy concern (and the FetchResponse shared-model blast radius) from the per-dimension caps (064.012/064.013). Verifies red until 064.017-T. See plan Plan Hardening H7 item 3 and cycle-3 remediation.

<!-- BEGIN:acceptance-criteria -->
- Scenario (a): a non-ASCII multibyte body (raw byte length > character length) accrues its RAW byte length toward MAX_TOTAL_FETCH_BYTES, not len(body) characters.
- Scenario (b): an invalid-byte body (where UTF-8 decode with errors=replace collapses bytes to U+FFFD) accrues its ORIGINAL raw byte length, not the replaced-character length.
- Scenario (c): a crawl whose cumulative RAW bytes exceed MAX_TOTAL_FETCH_BYTES is aborted even though the decoded character total stays under budget (the undercount-bypass attack).
- All three scenarios authored and observed RED (no body_byte_count field / char-based sum yet).
- No production code changed in this task (tests only).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: red-only harness authored before 064.017-T. Author into tests/fetch/. Fabricate responses with known raw byte lengths that diverge from character counts (multibyte UTF-8; invalid continuation bytes). Assert against the aggregate accumulator's byte total / abort behavior. Width-isolated: tests domain only. Depends on 064.013-T; greened by 064.017-T.
<!-- END:implementation-notes -->