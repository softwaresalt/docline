---
artifact_type: task
created_at: 2026-09-14T07:34:19.6141661Z
dependencies:
    - 073.001-T
    - 073.007-T
    - 073.009-T
id: 073.002-T
labels:
    - security
    - code
    - elt-staging
parent_id: 073-F
priority: high
references:
    - docs/plans/2026-09-14-elt-staging-credential-redaction-plan.md
status: queued
title: Thread typed sanitized key into create_staging_job
updated_at: 2026-09-14T07:36:08.1704078Z
---

Stream A / Unit A2 (code domain, test-first). Add keyword-only sanitized_source: str|None=None to create_staging_job (src/docline/fetch/staging.py), declared after `*` so it can never bind positionally (resolves position ambiguity). When provided, use it for metadata.source.

FAIL-CLOSED FALLBACK — RAISE, NOT SENTINEL (2026-09-15 operator contract, Finding 4): when sanitized_source is None AND source is a COMPOUND source key (starts with a known compound prefix: web_crawl:, manifest_url:, github_repo:, manifest_git:, manifest_local:, local_file:), RAISE a typed, non-leaking exception (a dedicated e.g. CredentialRedactionError) whose message / str() does NOT echo the raw source key or any credential substring. The whole-key `sanitized_source=None` sentinel-string fallback is REMOVED — omitting the sanitized key for a compound source is a programming error and MUST fail loudly without leaking, never silently substitute a redaction sentinel. Bare URL/path string compatibility MAY remain ONLY via the existing sanitize_source(source) branch, and ONLY because that branch preserves URL PATH components byte-for-byte while sanitizing structured userinfo + recognized credential query params correctly; it is retained solely for the existing bare-string positional test callers, which never pass compound keys.

Keep job_id=make_job_id(source) on the raw arg (determinism; H4 oracle residual accepted). Update orchestrate_fetch (src/docline/elt/orchestrate.py) to import sanitize_source_key and call create_staging_job(build_source_key(config), staging_dir, sanitized_source=sanitize_source_key(config)) — the production path ALWAYS supplies the sanitized key and never relies on the fallback.

TYPED-SANITIZER SCOPE (Finding 3): sanitize_source_key sanitizes URL fields (userinfo + recognized credential query-param names) and explicitly-secret typed config fields, and BYTE-PRESERVES non-credential provenance typed fields (branch, path_glob / include, manifest ID, local path).

Acceptance criteria: (1) 073.001-T passes; (2) job_id/cache_path byte-identical before/after for a fixed config (determinism); (3) existing staging + execute-path tests green, including bare-string positional callers in tests/fetch/test_staging.py (keyword-only proves positional compat); (4) omitting sanitized_source for a COMPOUND-key source RAISES the typed non-leaking exception — asserted: the exception type is raised AND its str() contains no raw key / credential substring — replacing the former sentinel behavior; (5) no cli.py change needed; (6) HONEST SUBSET/FINAL-GATE milestones — after this task the stream-A live-stdout test 073.007-T passes IN FULL (real cli.py:381 stdout free of userinfo + the existing recognized credential query param for all four URL source kinds, ordinary URL paths — e.g. `/authentication/overview`, `/tokenizer/config`, `/keys/rotation`, `/docs/100%25-off` — preserved byte-for-byte), and the USERINFO portion of the A2+B2 composition test 073.009-T passes; the NEW credential-query-name portion of 073.009-T stays red until 073.004-T (B2) lands, so FULL 073.009-T green requires BOTH A2 and B2. No stream-C/path task is required (operator contract: path-secret redaction REJECTED). Files: staging.py, orchestrate.py (2). Single production caller (orchestrate_fetch). Execution posture: test-first.

<!-- BEGIN:sizing -->
Size: S | Complexity: medium
<!-- END:sizing -->