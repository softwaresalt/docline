---
artifact_type: task
created_at: 2026-09-14T07:34:19.6141661Z
dependencies:
    - 073.001-T
    - 073.007-T
    - 073.009-T
    - 073.003-T
    - 073.008-T
id: 073.002-T
labels:
    - security
    - code
    - elt-staging
parent_id: 073-F
priority: high
references:
    - docs/plans/2026-09-14-elt-staging-credential-redaction-plan.md
status: queued
title: Thread typed sanitized key + exact-match credential query-name vocabulary (merged A2+B2)
updated_at: 2026-09-16T05:15:26.8251743Z
---

MERGED SINGLE PRODUCTION TASK (2026-09-15 final correction, P0 green-gate fix). This
is the ONLY production/code task in shipment 064-S. It implements BOTH former stream-A
(A2, typed sink wiring) AND former stream-B (B2, exact-match credential query-name
vocabulary + bounded query-NAME decode). Former task 073.004-T is RETIRED/merged into
this task. Rationale: the final live default-stdout composition gate 073.009-T only goes
FULLY green once BOTH userinfo redaction AND the expanded exact-match vocabulary land, so
two separate code tasks could never each produce a green suite (per-task-green /
test-first violation). A single merged production task makes 073.009-T — and every other
test task (073.001-T, 073.003-T, 073.007-T, 073.008-T) — green in ONE atomic verifiable
step. Width-isolation and the file-count granularity guideline yield here to the stronger
per-task-green (P-002/P-004) invariant; the change stays a single cohesive domain
(credential redaction across the shared sanitizer surface) spanning FOUR files —
src/docline/fetch/staging.py, src/docline/elt/orchestrate.py,
src/docline/elt/source_keys.py, and src/docline/elt/execute.py — as one atomic
verifiable green state. The four-file scope is an operator-mandated, documented
deviation from the <3-file guideline (same rationale as the A2+B2 merge): the live
WARNING/error composition gate 073.008-T and the live-stdout composition gate 073.009-T
can only each go green after the single production task lands, so the execute.py
error-output provenance-preservation change belongs in this same task, not a separate
one.

PART A — TYPED SINK WIRING (former A2). Add keyword-only `sanitized_source: str | None = None`
to create_staging_job (src/docline/fetch/staging.py), declared after `*` so it can never
bind positionally (resolves position ambiguity). When provided, use it for
metadata.source. FAIL-CLOSED FALLBACK — RAISE, NOT SENTINEL (Finding 4): when
sanitized_source is None AND source is a COMPOUND source key (starts with a known compound
prefix: web_crawl:, manifest_url:, github_repo:, manifest_git:, manifest_local:,
local_file:), RAISE a typed, non-leaking exception (e.g. CredentialRedactionError) whose
message / str() does NOT echo the raw source key or any credential substring. The whole-key
`sanitized_source=None` sentinel-string fallback is REMOVED. Bare-URL string
compatibility MAY remain ONLY via the existing sanitize_source(source) branch, and ONLY
for genuine bare http/https URL strings — where it strips structured userinfo + recognized
credential query-param NAMES while preserving the URL PATH components byte-for-byte;
retained solely for the existing bare-string positional test callers, which never pass
compound keys. LOCAL-PATH PRESERVATION (Finding P1, 2026-09-16 correction): compatibility
is BARE URL ONLY — NOT "URL/path". The operator contract designates local filesystem paths
and file:// inputs as PROVENANCE that MUST be preserved BYTE-FOR-BYTE, so this task MUST
change/remove the existing sanitize_source local-path redaction — the `file://` →
`<local-path-redacted>` branch AND the absolute/UNC/Windows-drive local-path →
`<local-path-redacted>` branch (src/docline/fetch/staging.py) — so a bare local-path or
file:// string input is returned BYTE-FOR-BYTE unchanged (only structured URL userinfo +
recognized credential query-param NAMES are ever removed). The existing local-path-redaction
assertions in tests/fetch/test_staging.py and tests/elt/test_source_keys.py MUST be updated
to the byte-preservation contract as part of keeping existing tests green (this is a
demonstrably-required characterization change, not new scope). Keep job_id=make_job_id(source) on
the raw arg (determinism; H4 oracle residual accepted). Update orchestrate_fetch
(src/docline/elt/orchestrate.py) to import sanitize_source_key and call
create_staging_job(build_source_key(config), staging_dir, sanitized_source=sanitize_source_key(config))
— the production path ALWAYS supplies the sanitized key and never relies on the fallback.

TYPED-SANITIZER PRESERVATION (Finding 3, src/docline/elt/source_keys.py). In
sanitize_source_key / _sanitize_url_field and the shared _remove_credential_query_params,
explicitly sanitize ONLY (a) URL fields (userinfo + recognized credential query-param NAMES)
and (b) explicitly-secret typed config fields, while BYTE-PRESERVING the non-credential typed
provenance fields: source `branch`, `path_glob` / `include` patterns, manifest `id`, and
LocalFileSource / ManifestLocalSource local filesystem `path`. LocalFileSource and
ManifestLocalSource carry NO URL credential to redact; their local path, include patterns,
and manifest ID appear UNCHANGED in the resulting key/metadata even when they contain
credential-looking text (e.g. `?token=`). This is the source_keys behavior 073.001-T asserts.

PART A″ — ERROR/LOG COMPOSITION PROVENANCE PRESERVATION (error-output provenance finding,
src/docline/elt/execute.py). The execute-path exception/log composition
(`_scrub_exception_message` / `_exception_scrub_replacements` / `_sanitize_exception_text`
/ `_redact_query_param_fragments` / `_strip_reversed_query_credentials`, emitted through the
`_log.exception(...)` WARNING/error sink) INDEPENDENTLY applies `sanitize_source_id` to
`config.id` (manifest ID) and `_sanitize_exception_text` (which chains `sanitize_source` →
`sanitize_source_id`) to `config.branch` and `config.path_glob`. Under the operator contract
those are PROVENANCE, not access credentials, and MUST be BYTE-PRESERVED in the composed
WARNING/error text — even when they contain credential-looking text (e.g. a branch
`feat/token`, a path_glob `**/*?token=*`, or a manifest id `tokenizer`). Change the composition
so it removes ONLY structured URL/typed credentials (URL userinfo + recognized credential
query-param NAMES on `config.url` / `config.repo_url` + explicitly-secret typed fields) while
leaving branch / path_glob / manifest ID byte-for-byte in the emitted text (NOTE — WE-sink
composition scope, Finding P1/073.008-T: the execute.py `_exception_scrub_replacements` WARNING/
error sink composes ONLY url/repo_url, branch, path_glob, and manifest id per source kind —
ManifestLocalSource composes ONLY config.id; LocalFileSource is not in the replacement set — so
local filesystem PATH and INCLUDE patterns are NOT emitted into this sink and their byte-
preservation is proven at the typed serialization surface by 073.001-T, NOT at the WARNING/
error sink) — e.g. drop branch / path_glob / manifest-ID from the credential-replacement set (route them through an identity/preservation path rather
than a credential-redacting sanitizer), and confine `_sanitize_exception_text` / the
`_HTTP_URL_RE` substitution / `_redact_query_param_fragments` to URL and typed-secret fields.
This is the WARNING/error composition behavior 073.008-T asserts at the real emission point.

PART B — EXACT-MATCH CREDENTIAL QUERY-NAME VOCABULARY + BOUNDED QUERY-NAME DECODE (former
B2). In src/docline/fetch/staging.py REPLACE the legacy case-insensitive `startswith`/prefix
heuristic in _is_credential_param with matching against an EXPLICIT, case-insensitive,
EXACT-match credential-name vocabulary. Individually enumerate the vocabulary constant
(_CREDENTIAL_PARAM_PREFIXES; rename to reflect exact-match semantics if useful) preserving the
existing recognized names by individual enumeration — token, access_token, key, api_key,
secret, auth, sig, signature, x-amz-credential, x-amz-signature, x-amz-security-token,
x-goog-signature — PLUS the individually-enumerated real-credential names previously caught
only by the `startswith("auth")` prefix, retained so exact-match keeps prior coverage:
auth_token, authorization — PLUS the intended additional legacy/cloud names refresh_token,
apikey, client_secret, x-goog-credential, awsaccesskeyid — PLUS the newly added password,
pwd, passwd, code. Match EVERY name by case-insensitive EXACT equality; NO prefix / startswith
/ delimiter-boundary matching (which caused the tokenizer / keynote / secretary / authorship /
signal and password_policy / pwd_length / code_version / passwd_file / refresh_token_ttl false
positives). NOTE (honest completeness): this vocabulary is NOT universally complete — arbitrary
or vendor-specific credential query-param names NOT enumerated here (and suffixed/prefixed
variants such as `token_v2`, that exact-match no longer catches) are INTENTIONALLY NOT redacted
absent explicit product support; that is an accepted residual (Risks/Caveats), not a silent gap.

BOUNDED DECODE — QUERY NAMES ONLY, BOTH PATHS (Finding 2): apply bounded percent-decoding to
the query-parameter NAME on BOTH the string sanitizer path (_is_credential_param /
sanitize_source) AND the typed sanitizer path (source_keys._remove_credential_query_params /
_is_credential_name) via the SHARED matcher; re-check the vocabulary after EACH unquote layer,
cap at 5 layers (_MAX_CREDENTIAL_DECODE_LAYERS), and fail closed (treat as credential) if the
name is still transforming past the cap. Do NOT decode URL paths or query-parameter VALUES. The
shared matcher also widens the typed/059-S WARNING path (intended, additive).

BENIGN QUERY BYTE-PRESERVATION — SURGICAL TOKEN REMOVAL, BOTH PATHS (Finding P2). The
credential-token removal on the string path MUST be SURGICAL: drop only credential-named tokens
from the RAW query string and preserve every benign token and its exact bytes — percent spelling
(`%20`, `%25`), `&` separators, and blank/valueless parameters (e.g. `flag`, `x=`) —
BYTE-FOR-BYTE. REPLACE the current `parse_qsl(...)`+`urlencode(...)` round-trip in `_sanitize_url`
(src/docline/fetch/staging.py), which NORMALIZES benign query bytes (`%20`→`+`, adds `=` to
valueless params, re-spells percent-encoding), with raw-token filtering consistent with the typed
path's `source_keys._remove_credential_query_params` (which already preserves surviving tokens
byte-for-byte). The typed path currently DELEGATES to `sanitize_source` inside `_sanitize_url_field`
and therefore INHERITS the string path's normalization before `_remove_credential_query_params`
runs, so fixing the string path's query filtering makes benign query parts byte-for-byte on BOTH
the string and typed (live default-stdout) paths.

Acceptance criteria (single atomic green state — the FULL 064-S test suite goes green when
this task completes): (1) 073.001-T passes (in-memory metadata.source + reconstructed
model_dump credential-free AND branch/path_glob/include/manifest-ID/local-path byte-preserved,
incl. LocalFileSource/ManifestLocalSource); (2) 073.003-T passes (exact-match new + case-varied
+ bounded-decode redaction; ALL benign near-matches preserved; enumerated legacy/cloud names
still redact); (3) 073.007-T passes IN FULL (real cli.py:381 stdout free of userinfo + existing
recognized credential query param for all four URL source kinds; ordinary paths incl. literal
`%25` preserved byte-for-byte); (4) 073.008-T passes IN FULL (live WARNING/error text drops the
FULL new credential vocabulary — password, pwd, passwd, client_secret, refresh_token, code,
apikey, x-goog-credential, awsaccesskeyid — across all four URL source kinds, AND the composed
WARNING/error text BYTE-PRESERVES branch, path_glob, and manifest ID while removing only
structured URL/typed credentials: execute.py provenance preservation — local filesystem path and
include patterns are NOT composed into this WARNING/error sink and are NOT asserted here; their
byte-preservation is proven at the typed serialization surface by 073.001-T); (5) 073.009-T passes IN FULL — the A2+B2
composition (userinfo AND EVERY recognized new credential query-param NAME — password, pwd,
passwd, client_secret, refresh_token, code, apikey, x-goog-credential, awsaccesskeyid —
including at least one percent-encoded query NAME)
is absent from real default-path stdout for all four URL-bearing source kinds, with benign
param + ordinary path preserved byte-for-byte; (6) job_id/cache_path byte-identical before/after
for a fixed config (determinism, H4 accepted); (7) omitting sanitized_source for a COMPOUND-key
source RAISES the typed non-leaking exception (type raised AND str() contains no raw key /
credential substring); (8) existing staging + execute-path + 059-S/063-S tests green, incl.
bare-string positional callers in tests/fetch/test_staging.py (keyword-only proves positional
compat); NO benign false positives (tokenizer/keynote/secretary/authorship/signal +
password_policy/pwd_length/client_secretary/codec/code_version/passwd_file/refresh_token_ttl and
case variants preserved); (9) no cli.py change needed; no path or query-VALUE decoding; no
stream-C/path task required (operator contract: path-secret redaction REJECTED);
(10) sanitize_source returns a bare local filesystem path (absolute/UNC/Windows-drive) and a
`file://` input BYTE-FOR-BYTE unchanged — the `<local-path-redacted>` sentinel is GONE (Finding
P1; compatibility is bare URL only, local paths are preserved provenance); proven by 073.003-T,
and the contradicting `<local-path-redacted>` assertions in tests/fetch/test_staging.py and
tests/elt/test_source_keys.py are updated to the byte-preservation contract; (11) benign query
parts are preserved BYTE-FOR-BYTE (representative percent spelling incl. `%20`/`%25`, `&`
separators, blank/valueless params) via surgical credential-token removal — NOT parse_qsl/urlencode
re-serialization — on both the string and live default-stdout paths (Finding P2); proven by
073.003-T (helper) and 073.009-T (live); (12) the bounded query-NAME decode preserves a benign
token that resolves to a stable benign value within the 5-layer cap (exact-cap benign boundary)
while an over-cap benign-looking name still transforming past the cap fails closed (accepted
false-positive residual); proven by 073.003-T. Files:
src/docline/fetch/staging.py, src/docline/elt/orchestrate.py, src/docline/elt/source_keys.py, src/docline/elt/execute.py (4).
Single production caller of create_staging_job (orchestrate_fetch). Execution posture: test-first
(all five test tasks must be red on HEAD, green after this task).

<!-- BEGIN:sizing -->
Size: M | Complexity: medium
<!-- END:sizing -->