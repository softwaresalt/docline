---
artifact_type: task
created_at: 2026-09-15T20:54:01.8987405Z
custom_fields:
    blocked_reason: 'Operator scope-correction (064-S/073-F): path-embedded secret redaction REJECTED. Docline responsibility narrowed to structured access credentials (URL userinfo + recognized credential query params + typed secret config fields); arbitrary path redaction and document-body scanning are out of scope (upstream DLP concern). Stream C retired non-destructively; not deleted.'
id: 073.006-T
labels:
    - security
    - code
    - elt-staging
    - rejected
    - superseded
parent_id: 073-F
priority: medium
references:
    - docs/plans/2026-09-14-elt-staging-credential-redaction-plan.md
status: blocked
title: Redact path-embedded secrets in _sanitize_url
updated_at: 2026-09-16T02:59:07.4878518Z
---

**RETIRED — OPERATOR DECISION 2026-09-15 (064-S/073-F): REJECTED, NOT EXECUTABLE.** Path-embedded secret redaction is REJECTED as out of scope: Docline redacts ONLY structured access credentials (URL userinfo + recognized credential query params + typed secret config fields) and MUST NOT scan/rewrite source-document content or redact ordinary URL paths (that is an upstream DLP concern). This task is removed from executable shipment 064-S (status=blocked via shipment return-blocked), its dependency edges (→073.004-T, →073.005-T, →073.007-T) are removed, and it is retained NON-DESTRUCTIVELY for traceability only. The grammar described below is HISTORICAL and MUST NOT be implemented; `_sanitize_url` MUST leave URL path components unchanged.

Stream C / Unit C2 (code domain, test-first). Implement the H2 + H2-C2 path-embedded secret grammar in src/docline/fetch/staging.py _sanitize_url (plus a small path-redaction helper) using DECODE-BEFORE-SEGMENTATION (deliberation Remediation cycle 2 / H2-C2, finding #1; iterative-validation refinement cycle 3 / finding F-02): (1) ITERATIVE malformed-escape validation -> fail closed to <path-redacted> on any invalid %-escape, validated BEFORE EACH decode layer AND re-validated on the freshly-decoded string before the next layer (NOT only a single pre-first-decode scan) so a nested escape whose malformedness is masked by an outer valid `%25` (e.g. `%252`->`%2` truncated after one layer; `%25ZZ`->`%ZZ` after one layer; plus first-layer `%2/`, `%ZZ`, trailing `%`) is caught at the layer it surfaces; (2) bounded iterative percent-decode (<=5 layers, same _MAX_CREDENTIAL_DECODE_LAYERS contract), each layer gated by the step-1 re-validation -> fail closed to <path-redacted> on over-cap (still transforming after 5 layers); (3) segment on "/" over the FULLY-DECODED path so encoded structural delimiters (%2F->/, %3D->=) are treated as segment/kv boundaries, retaining a byte-span map back to the ORIGINAL raw bytes; (4) per-segment marker detection via the SHARED credential-name matcher (reuses B2 expanded vocabulary + exact-match new markers as the single source of truth), redact marker=value values in place (preserving original raw marker-name + delimiter bytes, incl. %3D) and bare-marker-associated NON-EMPTY following segments (-> <redacted>); /token/ (empty adjacent value) stays unchanged (non-empty-adjacent-value rule, findings #1/#9); preserve benign segments and separators byte-for-byte (replacement-boundary safety); (5) fail closed to <path-redacted> on unparseable/unmappable input. Acceptance criteria: (1) C1 (073.005-T) passes incl. all H2-C2 encoded-delimiter/multilayer/malformed/over-cap/`/token/` cases and the nested per-layer malformed cases %252/%25ZZ (finding F-02); (2) existing sanitizer + 059-S/063-S tests green; (3) strictly additive (never redacts a benign path); (4) userinfo + query-param redaction unchanged; (5) the DIRECT path-embedded-secret fixture in live-stdout integration test 073.007-T goes green after this task lands — C2's _sanitize_url path redaction reaches the default-path stdout via sanitize_source_key -> _sanitize_url_field -> sanitize_source -> _sanitize_url (test-first: 073.006-T depends_on 073.007-T; finding F-04). Files: staging.py (1). Execution posture: test-first.

<!-- BEGIN:sizing -->
Size: M | Complexity: medium
<!-- END:sizing -->