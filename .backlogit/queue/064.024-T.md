---
artifact_type: task
created_at: 2026-08-27T23:59:00.0000000Z
dependencies:
    - 064.017-T
id: 064.024-T
labels:
    - fetch
    - security
    - dos
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Thread the aggregate byte budget through ancillary robots/TOC fetches
updated_at: 2026-08-29T06:30:00.0000000Z
---

Complete the aggregate MAX_TOTAL_FETCH_BYTES budget on the ancillary-fetch vector, split out of 064.017-T (cycle-6, PR #166 review: 064.017-T already changes fetch_page, crawl, and _fetch_with_retries — three functions — before any ancillary work, so folding the two ancillary functions in would breach the <5-function/2-hour envelope; the 064.017-T split guard is executed here). Code domain only, <=1 file (src/docline/fetch/crawl.py). This task owns the ancillary-fetch (_robots_allow / _discover_toc_links) budget threading + their two re-raise clauses AND the crawl() call-site edits that pass the seeded budget into both helpers, and takes over green-ownership of the 064.016-T scenario (c)(iii) ancillary sub-vector from 064.017-T. The core request-scoped RemainingByteBudget, the AggregateBudgetExceededError type, the fetch_page bounded-reader decrement/mid-read abort, the crawl() seed, the already-budget-aware _fetch_with_retries pass-through, and the crawl() main-loop + _fetch_with_retries re-raise clauses are delivered first by 064.017-T; this task extends the SAME request-scoped budget to the two ancillary fetch consumers — _robots_allow (which calls fetch_page DIRECTLY) and _discover_toc_links (which calls the already-budget-aware _fetch_with_retries, NOT fetch_page directly) — by threading the budget from crawl()'s two ancillary call sites, so all transfer for the request decrements one budget. Aggregate cap value: MAX_TOTAL_FETCH_BYTES = 512 MiB (536870912 bytes); see plan §H7 Selected numeric limits. Depends on 064.017-T. See plan H7 item 3 / cycle-6 remediation.

<!-- BEGIN:acceptance-criteria -->
- crawl() passes the SAME request-scoped remaining-byte budget it seeds (064.017-T) into BOTH ancillary helpers at their call sites (the _robots_allow call ~line 153 and the _discover_toc_links call ~line 247); _robots_allow forwards it to its DIRECT fetch_page call, and _discover_toc_links forwards it to its _fetch_with_retries call (already budget-aware from 064.017-T, which forwards to fetch_page) — so ancillary robots.txt / mdBook TOC transfer decrements the shared aggregate budget WHILE CHUNKS ARE READ (identically to main pages), and a hostile server cannot amplify transfer via oversized-but-under-cap robots.txt/TOC payloads outside the budget.
- An `except AggregateBudgetExceededError: raise` clause is added immediately before the broad `except (DoclineError, OSError)` handler in BOTH _robots_allow (~line 406, which has NO prior re-raise clause and needs one added) and _discover_toc_links (~line 465), mirroring the existing `except CrawlUrlRejectedError: raise` pattern, so the budget error propagates out of crawl() as a RAISE rather than being swallowed and recorded as a per-page skip (which would let the crawl continue over the remaining frontier with budget=0 and degrade a byte-abort into a max_pages x backoff time-exhaustion).
- 064.016-T scenario (c)(iii) — with SEPARATE robots and TOC variants: an ancillary robots.txt via _robots_allow (direct fetch_page) AND a mdBook TOC script via _discover_toc_links (via _fetch_with_retries) each decrement the shared request budget; crawl() RAISES the budget error in both — passes green here (both variants required, not either/or, so the distinct TOC-via-_fetch_with_retries path is independently exercised and not masked by the robots path). Scenarios (a), (b), (c)(i main-page mid-read abort), and (c)(ii retried-failure accrual) remain green from 064.017-T and MUST NOT regress.
- Boundary behavior (exact): the ancillary robots.txt / mdBook TOC fetches go through the SAME fetch_page bounded reader delivered by 064.013-T/064.017-T, inheriting its per-read cap of min(CHUNK_SIZE, per_response_remainder + 1, aggregate_remainder + 1); they decrement the SAME request-scoped budget seeded at 536870912 bytes. A total of exactly 536870912 entity-body bytes across ALL request fetch_page calls (main + retries + ancillary) is allowed; on the ancillary response that trips the budget the bounded reader buffers at most one additional (crossing) entity-body byte - never a full extra CHUNK_SIZE chunk of body - aborting mid-stream and raising AggregateBudgetExceededError, which crawl() re-raises. 064.016-T scenario (c)(iii) asserts this ancillary over-read guard (buffered content <= remaining allowance + 1).
- Existing tests/fetch suite remains green (the ancillary budget threading is additive; the budget still defaults to unbounded/None for standalone single fetch).
- ruff check ., ruff format --check ., pyright src/ clean; typed errors only; no bare except.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green after 064.017-T (it delivers the RemainingByteBudget/AggregateBudgetExceededError, fetch_page decrement, crawl() seed, the already-budget-aware _fetch_with_retries pass-through, and the crawl() main-loop/_fetch_with_retries re-raise). This task adds only: (1) crawl() passes the seeded budget into its two ancillary call sites — the _robots_allow(...) call (~line 153) and the _discover_toc_links(...) call (~line 247); (2) _robots_allow forwards the budget to its DIRECT fetch_page call (~line 400); (3) _discover_toc_links forwards the budget to its _fetch_with_retries call (~line 462), which is ALREADY budget-aware from 064.017-T (it is NOT a direct fetch_page call and _fetch_with_retries is not re-edited here); (4) add the two `except AggregateBudgetExceededError: raise` re-raise clauses immediately before the broad handlers in _robots_allow (~line 406, which has no prior re-raise) and _discover_toc_links (~line 465). Function/width budget (2-hour/<5-function guardrail, <=1 file: src/docline/fetch/crawl.py): 3 functions touched — crawl (the two ancillary call-site edits, since the request-scoped budget is local to crawl() and threading it into the helpers necessarily changes crawl()'s call sites), _robots_allow, and _discover_toc_links — with no new types and no new dataclass field, well within the <5-function envelope. Width-isolated: code domain, single file. Depends on 064.017-T; feeds the MCP untrusted-fetch end-to-end boundary harness 064.014-T (which asserts the aggregate cap end-to-end once ALL aggregate transfer, ancillary included, decrements the one budget).
<!-- END:implementation-notes -->
