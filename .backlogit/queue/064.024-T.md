---
artifact_type: task
created_at: 2026-08-27T23:59:00.0000000Z
dependencies:
    - 064.017-T
id: 064.024-T
labels:
    - fetch
    - security
    - dos
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Thread the aggregate byte budget through ancillary robots/TOC fetches
updated_at: 2026-08-27T23:59:00.0000000Z
---

Complete the aggregate MAX_TOTAL_FETCH_BYTES budget on the ancillary-fetch vector, split out of 064.017-T (cycle-6, PR #166 review: 064.017-T already changes fetch_page, crawl, and _fetch_with_retries — three functions — before any ancillary work, so folding the two ancillary functions in would breach the <5-function/2-hour envelope; the 064.017-T split guard is executed here). Code domain only, <=1 file (src/docline/fetch/crawl.py). This task owns the ancillary-fetch (_robots_allow / _discover_toc_links) budget threading + their two re-raise clauses, and takes over green-ownership of the 064.016-T scenario (c)(iii) ancillary sub-vector from 064.017-T. The core request-scoped RemainingByteBudget, the AggregateBudgetExceededError type, the fetch_page bounded-reader decrement/mid-read abort, the crawl() seed, and the crawl() main-loop + _fetch_with_retries re-raise clauses are delivered first by 064.017-T; this task extends the SAME request-scoped budget to the two ancillary fetch_page call sites so all transfer for the request decrements one budget. Aggregate cap value: MAX_TOTAL_FETCH_BYTES = 512 MiB (536870912 bytes); see plan §H7 Selected numeric limits. Depends on 064.017-T. See plan H7 item 3 / cycle-6 remediation.

<!-- BEGIN:acceptance-criteria -->
- _robots_allow and _discover_toc_links pass the SAME request-scoped remaining-byte budget seeded by crawl() (064.017-T) into their fetch_page calls, so ancillary robots.txt / mdBook TOC transfer decrements the shared aggregate budget WHILE CHUNKS ARE READ (identically to main pages), and a hostile server cannot amplify transfer via oversized-but-under-cap robots.txt/TOC payloads outside the budget.
- An `except AggregateBudgetExceededError: raise` clause is added immediately before the broad `except (DoclineError, OSError)` handler in BOTH _robots_allow (~line 406, which has NO prior re-raise clause and needs one added) and _discover_toc_links (~line 465), mirroring the existing `except CrawlUrlRejectedError: raise` pattern, so the budget error propagates out of crawl() as a RAISE rather than being swallowed and recorded as a per-page skip (which would let the crawl continue over the remaining frontier with budget=0 and degrade a byte-abort into a max_pages x backoff time-exhaustion).
- 064.016-T scenario (c)(iii) (an ancillary robots.txt via _robots_allow and/or a mdBook TOC script via _discover_toc_links decrements the shared request budget; crawl() RAISES the budget error) passes green here. Scenarios (a), (b), (c)(i main-page mid-read abort), and (c)(ii retried-failure accrual) remain green from 064.017-T and MUST NOT regress.
- Boundary behavior (exact): the ancillary reader decrements the SAME request-scoped budget seeded at 536870912 bytes; a total of exactly 536870912 raw wire bytes across ALL request fetch_page calls (main + retries + ancillary) is allowed; the read aborts mid-stream the instant the next chunk would push cumulative request raw bytes past 536870912, raising AggregateBudgetExceededError, which crawl() re-raises.
- Existing tests/fetch suite remains green (the ancillary budget threading is additive; the budget still defaults to unbounded/None for standalone single fetch).
- ruff check ., ruff format --check ., pyright src/ clean; typed errors only; no bare except.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green after 064.017-T (it delivers the RemainingByteBudget/AggregateBudgetExceededError, fetch_page decrement, crawl() seed, and the crawl()/_fetch_with_retries re-raise). This task adds only: (1) pass the crawl()-seeded budget into the _robots_allow fetch_page call and the _discover_toc_links fetch_page call; (2) add the two `except AggregateBudgetExceededError: raise` re-raise clauses (before each broad handler). Function/width budget (2-hour/<5-function guardrail, <=1 file: src/docline/fetch/crawl.py): 2 functions touched (_robots_allow, _discover_toc_links), no new types, no new dataclass field — well within the envelope. Width-isolated: code domain, single file. Depends on 064.017-T; feeds the MCP untrusted-fetch end-to-end boundary harness 064.014-T (which asserts the aggregate cap end-to-end once ALL aggregate transfer, ancillary included, decrements the one budget).
<!-- END:implementation-notes -->
