---
artifact_type: task
created_at: 2026-06-14T00:33:01.1998189-07:00
dependencies:
    - 030.001-T
id: 030.002-T
labels:
    - docling
    - pdf-batch
    - pdf-triage
    - fidelity
parent_id: 030-F
priority: medium
references:
    - src/docline/process/pdf_batch.py
    - src/docline/process/pdf_triage.py
status: queued
title: 'T2: Consumers parse envelope; per-page splice-back is content-correct'
updated_at: 2026-06-14T00:33:01.1998189-07:00
---

Update `src/docline/process/pdf_batch.py` `_process_one_chunk`: after subprocess exit 0, parse output file as JSON; populate ChunkResult.markdown with envelope["text"] (preserves existing stitching behavior); add `chunk_pages: tuple[str, ...]` field on ChunkResult carrying envelope["pages"] for downstream consumers. Update `src/docline/process/pdf_triage.py` splice-back loop (currently lines 470-483): parse envelope JSON; iterate envelope["pages"] and assign each to final_pages[start+i]; on length mismatch (envelope["pages"] length != range length), log warning and fall back to old "first-page-gets-blob, rest empty" with engine_per_page[i] set to "docling-collapsed" so the attribution mismatch is visible. Defensive JSONDecodeError catch should fall back to treating the body as text and using the existing single-blob path so a partial T1 rollout cannot brick production. Tests: (a) happy-path per-page assignment; (b) length-mismatch warning + fallback path; (c) JSONDecodeError defensive fallback; (d) chunk_pages field populated on ChunkResult; (e) existing stitching behavior unchanged via envelope["text"]. Plan reference: §T2.