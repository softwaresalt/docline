---
artifact_type: task
created_at: 2026-08-29T03:40:20.0000000Z
dependencies:
    - 064.003-T
id: 064.035-T
labels:
    - mcp
    - stdio
    - security
    - config
    - tests
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Author §H8 server-side opt-in startup config harness
---

Author the RED §H8 startup opt-in config harness (tests/parity/test_mcp_stdio.py). Tests domain only. Proves the external-PDF-engine opt-in is a SERVER-SIDE, startup-only, fail-closed control resolved by the docline-mcp entry point (src/docline/mcp/__main__.py main()) and threaded into a fresh DoclineMcpServer instance - never derived from request data, never mutating the module-level SERVER singleton. Tests the REAL main() wiring (not just a parser helper) so advertise and dispatch observe the same instance-local setting. PR #166 post-decomposition cycle-3 finding: the opt-in must be the ONLY way to enable the external engine, and a client must never be able to enable it. Verifies red until 064.036-T. See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Plan Hardening §H8; 064.003-T entry point).

<!-- BEGIN:acceptance-criteria -->
- Scenario (a) [fail-closed env resolution; green@064.036 (unset case is a green anchor)]: with no env var and no CLI flag, main() constructs a DoclineMcpServer with external_pdf_engines_enabled=False (DISABLED). With DOCLINE_MCP_ALLOW_EXTERNAL_PDF_ENGINES set to exactly "1" (raw, unpadded), the constructed server is ENABLED. With any OTHER value the server is DISABLED (fail-closed - ONLY the raw exact opt-in token "1" enables; resolution is raw-value equality with NO .strip()/trim). Disabled rows MUST include "0", "false", "true", "yes", "" (empty), "   " (whitespace-only), AND the leading/trailing-whitespace- and newline-padded "1" discriminators " 1", "1 ", " 1 ", "1\n" (these green ONLY under raw == "1" and stay RED under any .strip()/trim impl - the TDD guard for 064.036-T's exact-token contract). Parametrized rows; the default-disabled/unset row is a green regression anchor, the enabling and non-"1" (incl. padded-"1") rows are red until 064.036.
- Scenario (b) [CLI flag resolution; green@064.036]: launching main() with the docline-mcp --allow-external-pdf-engine flag constructs an ENABLED server; without it (and without the env token) the server is DISABLED. Env token and flag are independent enablers (either enables; absence of both disables).
- Scenario (c) [instance-local wiring + no-secret; green@064.036]: main() constructs a FRESH DoclineMcpServer(external_pdf_engines_enabled=<resolved>) and passes THAT instance to serve(server=...); it does NOT mutate or enable the import-time module-level SERVER default (assert SERVER.external stays False). No-secret: neither the resolved opt-in value nor any ambient credential env value (AZURE_AI_FOUNDRY_KEY / AZURE_AI_FOUNDRY_ENDPOINT / MISTRAL_API_KEY) is emitted to any captured log/stdout line during resolution/startup.
- Scenarios red at authoring except the noted green anchors (unset->disabled; SERVER-unmutated). No production code changed (tests only). The opt-in is resolved EXACTLY ONCE at startup and never re-read from request data (a client-supplied field cannot flip it - covered end-to-end by 064.031-T/064.033-T).
- Scenario budget: 3 (env resolution incl. fail-closed; CLI flag; instance-local wiring + no-secret) - within the <4 heuristic.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: red-only startup-config harness authored before 064.036-T. Width-isolated: tests domain only (tests/parity/test_mcp_stdio.py). Drive main() with monkeypatched os.environ and argv, and a patched serve() that captures the server instance it receives (assert external_pdf_engines_enabled + that it is a distinct instance from the module SERVER). Use caplog / captured stdout for the no-secret assertion. Do NOT make real paid calls. Depends on 064.003-T (the __main__.py entry point + serve() wiring exist). Greened by 064.036-T. See plan §H8.
<!-- END:implementation-notes -->
