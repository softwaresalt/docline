---
artifact_type: task
created_at: 2026-06-13T00:34:33.5077587-07:00
dependencies:
    - 029.001-T
id: 029.002-T
parent_id: 029-F
priority: medium
references:
    - docs/plans/2026-06-13-mistral-ocr-replace-adi-plan.md
status: queued
title: T2 — [mistral] extra + read_pdf_mistral reader module
updated_at: 2026-06-13T00:34:33.5077587-07:00
---

Add Mistral OCR support — optional [mistral] extra + reader module.

1. pyproject.toml: add `mistral = ["mistralai>=1,<2"]` to [project.optional-dependencies]
2. src/docline/dependencies.py: add `mistral_available() -> bool` helper mirroring `adi_available` shape (uses importlib.util.find_spec)
3. New src/docline/readers/mistral.py:
   - Public API: `read_pdf_mistral(path: Path, api_key: str | None = None, model: str = "mistral-ocr-latest") -> str`
   - `MistralCredentialError(DoclineError)` raised when api_key not resolvable from arg or MISTRAL_API_KEY env var
   - Lazy SDK import via `require_extra("mistralai", extra="mistral")` raising DependencyUnavailableError when [mistral] not installed
   - Uses mistralai.Mistral() client; calls client.ocr.process() with model + document_url or file
   - Returns markdown string (concatenated across pages if response is per-page)
   - Per-call telemetry: logs page count + wall time + projected cost (cost per page = $0.001 for mistral-ocr-latest)
   - Defensive error handling: catches mistralai exceptions and re-wraps as DoclineError

TDD. Tests in tests/readers/test_mistral_extractor.py (NEW) — 8-10 tests minimum:
- read_pdf_mistral happy path returns markdown (with FakeMistralClient fixture)
- MistralCredentialError raised when MISTRAL_API_KEY unset and api_key=None
- MistralCredentialError raised when MISTRAL_API_KEY="" (empty string)
- DependencyUnavailableError raised when [mistral] not installed (simulated via sys.modules manipulation)
- Custom model parameter passed through to client.ocr.process
- Per-call telemetry logged with expected fields (pages, wall_s, projected_cost_usd)
- Transient SDK error re-wrapped as DoclineError with path context
- File-not-found surfaces as FileNotFoundError (not wrapped)
- Determinism: two invocations against same fake client return same result

Also tests/process/test_mistral_extractor_optional.py (NEW) — 3-4 tests verifying:
- import docline succeeds without [mistral] installed
- pyproject.toml [mistral] extra structure
- read_pdf_mistral raises DependencyUnavailableError with helpful message when SDK absent

Stop condition: scope cap is 250 LOC change to src/docline/readers/mistral.py. If reader requires complex chunking or pagination logic, factor into helpers but keep public API single-function.