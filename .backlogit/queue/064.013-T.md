---
artifact_type: task
created_at: 2026-08-27T08:45:15.0000000Z
dependencies:
    - 064.012-T
id: 064.013-T
labels:
    - fetch
    - security
    - dos
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Implement shared-fetch page + per-response byte caps
updated_at: 2026-08-27T09:00:00.0000000Z
---

Implement the per-dimension resource-exhaustion caps on the SHARED fetch code to turn 064.012-T green. Code domain only. Two minimal edits: (1) app_models.py hard max_pages upper bound; (2) fetch/http.py streamed MAX_RESPONSE_BYTES read replacing the unbounded response.read() (initial + redirect). Hardens BOTH interfaces (CLI docline fetch and the MCP fetch tool) - a cross-interface behavior change (see plan Rollback/Risks). The aggregate MAX_TOTAL_FETCH_BYTES budget + raw-byte retention are a separate width-isolated pair (064.016-T/064.017-T). See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Plan Hardening H7).

<!-- BEGIN:acceptance-criteria -->
- FetchRequest.max_pages gains a hard upper bound (Field(..., le=MAX_PAGES_LIMIT)); an over-limit value raises ValidationError (surfaces as -32602 on the MCP boundary). Bound sized above legitimate CLI crawl use.
- fetch/http.py reads the body in bounded chunks up to MAX_RESPONSE_BYTES and aborts once exceeded (typed error), replacing the unbounded response.read(); applies to the initial response AND every redirect hop's final response.
- 064.012-T harness passes green (max_pages cap + per-response streamed cap).
- The existing tests/fetch suite remains green under the new bounds (or is deliberately updated for the new limit, documented in the commit). Typed errors only; no bare except.
- ruff check ., ruff format --check ., pyright src/ clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green (shared-code hardening) after 064.012-T. Cycle-3 re-scope: this task is now <=2 files (app_models.py + fetch/http.py) for the per-dimension caps; the aggregate budget and FetchResponse.body_byte_count retention moved to the byte-accurate pair 064.016-T/064.017-T so the shared-model (FetchResponse) blast radius and non-ASCII/invalid-byte accounting are isolated. Each edit is minimal (one Field bound, one streamed read) and stays within the ~2-hour envelope. Width-isolated to the fetch/resource-cap surface (single skill domain: code). Choose cap constants conservatively (well above real crawl sizes); document them near the constants. Depends on 064.012-T.
<!-- END:implementation-notes -->
