---
artifact_type: task
created_at: 2026-08-27T08:45:15.0000000Z
dependencies:
    - 064.012-T
id: 064.013-T
labels:
    - fetch
    - security
    - dos
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Implement shared-fetch page + response-byte + aggregate caps
updated_at: 2026-08-27T09:00:00.0000000Z
---

Implement the resource-exhaustion caps on the SHARED fetch code to turn 064.012-T green. Code domain only. Three minimal edits for ONE cohesive resource-cap concern: (1) app_models.py hard max_pages upper bound; (2) fetch/http.py streamed MAX_RESPONSE_BYTES read replacing the unbounded response.read() (initial + redirect); (3) fetch/crawl.py aggregate MAX_TOTAL_FETCH_BYTES accumulator that aborts the crawl. Hardens BOTH interfaces (CLI docline fetch and the MCP fetch tool) - a cross-interface behavior change (see plan Rollback/Risks). See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Plan Hardening H7).

<!-- BEGIN:acceptance-criteria -->
- FetchRequest.max_pages gains a hard upper bound (Field(..., le=MAX_PAGES_LIMIT)); an over-limit value raises ValidationError (surfaces as -32602 on the MCP boundary). Bound sized above legitimate CLI crawl use.
- fetch/http.py reads the body in bounded chunks up to MAX_RESPONSE_BYTES and aborts once exceeded (typed error), replacing the unbounded response.read(); applies to the initial response AND every redirect hop's final response.
- fetch/crawl.py enforces a hard aggregate MAX_TOTAL_FETCH_BYTES budget across all pages of a crawl and aborts the crawl once the running total is exceeded (bounds the max_pages x MAX_RESPONSE_BYTES product and disk-staging). The aggregate-budget abort MUST propagate out of the crawl loop and MUST NOT be swallowed by the per-page retry except (DoclineError, OSError) handler.
- 064.012-T harness passes green.
- The existing tests/fetch suite remains green under the new bounds (or is deliberately updated for the new limit, documented in the commit). Typed errors only; no bare except.
- ruff check ., ruff format --check ., pyright src/ clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green (shared-code hardening) after 064.012-T. This task intentionally touches 3 files (app_models.py + fetch/http.py + fetch/crawl.py) for one cohesive resource-cap concern; each edit is minimal (one Field bound, one streamed read, one loop accumulator) and stays within the ~2-hour envelope. Width-isolated to the fetch/resource-cap surface (single skill domain: code). Choose cap constants conservatively (well above real crawl sizes); document them near the constants. Depends on 064.012-T.
<!-- END:implementation-notes -->
