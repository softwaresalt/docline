---
artifact_type: task
created_at: 2026-07-06T11:12:35.5893614-07:00
id: 054.001-T
parent_id: 054-F
priority: high
references:
    - docs/decisions/2026-07-06-postgresql-online-docs-fetch-capability.md
    - src/docline/app.py
    - src/docline/fetch/crawl.py
    - src/docline/fetch/staging.py
status: queued
title: Wire execute_fetch web-crawl orchestration + staging persistence
updated_at: 2026-07-06T11:12:54.8634144-07:00
---

Replace the `execute_fetch` stub in app.py with real orchestration that composes the existing tested primitives: run `asyncio.run(crawl(start_url, config))`, persist each non-skipped CrawlResult body to `{staging}/<shard>/<job_id>/files/<slug>.html`, write a complete `metadata.json` (StagingJob with complete=True) and a crawl-manifest (staged file → source URL / canonical URL / discovery order) that `execute_process`'s `_load_crawl_manifest`/`_ordered_staged_files` consume, and return `FetchResult(source, staged_path, success=True)`. Map `FetchRequest.depth` → `CrawlConfig.max_depth`; apply a bounded default `max_pages`. Keep CLI `fetch` and MCP `fetch` on the same `execute_fetch` code path (structural parity).

<!-- BEGIN:acceptance-criteria -->
- TDD: failing tests first (mock `crawl`/`fetch_page`; no live network).
- Staged layout matches what `execute_process` walks: `metadata.json` (complete StagingJob) + sibling `files/` + crawl-manifest.
- `FetchResult.success=True` with a real `staged_path`; failure paths return `success=False` with a typed error (no bare except).
- `FetchRequest.depth`→`CrawlConfig.max_depth`; bounded `max_pages` default enforced.
- Robots/URL-policy/SSRF guards remain in force (crawl defaults `respect_robots=True`).
- CLI and MCP fetch produce identical staged output.
- Gates green: ruff check, pyright src/, pytest, ruff format --check.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
`create_staging_job` returns a StagingJob model but writes NO files — this task adds the disk-persistence bridge (files/ + metadata.json + crawl-manifest). Confirm the exact crawl-manifest schema `_load_crawl_manifest` expects before writing it. `crawl()` is async → wrap with `asyncio.run` inside the sync `execute_fetch`. Slugify URLs for stable `files/*.html` names.
<!-- END:implementation-notes -->