---
artifact_type: task
created_at: 2026-07-05T23:47:22.4483623-07:00
dependencies:
    - 053.001-T
id: 053.002-T
labels:
    - openapi
    - reader
    - render
    - cross-link
parent_id: 053-F
priority: medium
status: queued
title: T2 — cross-file schema doc-path mapping + link emission
updated_at: 2026-07-05T23:47:22.4483623-07:00
---

Cross-file schema doc-path mapping + renderer link emission. Give the reader a corpus/bundle view that maps a resolved external (target file, pointer) to the schema doc relative_path that target file produces, so an operation's external schema ref becomes a correct relative Markdown link to that schema doc. Extend render.py `_schema_type_summary` to emit links for resolved external refs (not just `#/components/schemas/`). Reuse the existing cross_doc_links harvester for edge extraction (no new contract surface).

<!-- BEGIN:acceptance-criteria -->
- A two-file fixture (operations file + external definitions file) renders the operation doc with a Markdown link to the external schema's doc.
- The link target resolves (relative path) to the produced schema doc (no dangling).
- resolve_cross_doc_links harvests the operation->schema edge with the expected shape.
- Example refs are not linked.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
- Depends on T1 (resolver).
- reader.py needs a directory/bundle pass so it knows which files produce which schema docs; compute the cross-file relative href accordingly.
<!-- END:implementation-notes -->