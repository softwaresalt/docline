---
artifact_type: task
created_at: 2026-07-01T23:41:25.959299-07:00
dependencies:
    - 041.002-T
id: 041.001-T
labels:
    - feature
    - process
    - ocr
    - memory
    - runtime
    - test-first
parent_id: 041-F
priority: medium
status: queued
title: T2 — Derive OCR cap at runtime and wire into batched grouping
updated_at: 2026-07-01T23:42:17.5189074-07:00
---

Compute the per-host ocr_max_pages from resource_probe.probe() + ocr_budget and pass it into group_by_page_count_ocr_aware and dispatch_batched_groups_with_retry at the two call sites (pdf_batch.py ~286, pdf_triage.py ~572). Keep OCR_MAX_BATCHED_PAGES=8 as ceiling / degraded-probe fallback.

<!-- BEGIN:acceptance-criteria -->
- A helper derives ocr_max_pages from probe().available_ram_gb (-> available_mb) and ocr_budget.max_ocr_pages_per_group using a representative page_megapixels.
- Representative mpx from the chunk/range first-page mediabox (like the harness page_megapixels), or a conservative default when unavailable.
- pdf_batch.py (~286) and pdf_triage.py (~572) pass ocr_max_pages=<derived> to group_by_page_count_ocr_aware and dispatch_batched_groups_with_retry.
- Degraded/unavailable probe -> cap falls back to OCR_MAX_BATCHED_PAGES (8); result clamped to <= 8 ceiling.
- Tests: derived cap larger on a high-RAM fake budget, smaller on a low-RAM fake budget; degraded probe -> fallback 8.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Inject the probe (default_probe seam already used in pdf_batch.py) so tests pass a fake ResourceBudget. Reuse existing page-count/text probes; do not read the mediabox twice per chunk. Depends on T1 (041.002-T).
<!-- END:implementation-notes -->