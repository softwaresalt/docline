---
artifact_type: task
created_at: 2026-06-03T13:12:07.1107625-07:00
id: 011.001-T
labels:
    - ci
    - ops
    - workflow
parent_id: 011-F
priority: medium
references:
    - docs/plans/2026-06-03-docline-ci-workflow-plan.md
    - .github/instructions/ci-security.instructions.md
    - .github/instructions/workflows.instructions.md
status: queued
title: Add `.github/workflows/ci.yml` with five gate jobs on ubuntu-latest
updated_at: 2026-06-03T13:12:07.1107625-07:00
---

Author `.github/workflows/ci.yml` running five jobs (`lint`, `format`, `typecheck`, `test`, `build`) on `ubuntu-latest`. Each job: checkout (no persisted creds), `astral-sh/setup-uv` with cache, `actions/setup-python` pinned to 3.12, `uv sync --locked --all-extras --dev`, then the gate command using `uv run`.

Gate commands:
- `lint`: `uv run ruff check .`
- `format`: `uv run ruff format --check .`
- `typecheck`: `uv run pyright src/`
- `test`: `uv run pytest`
- `build`: `uv run python -m build`

Triggers: `pull_request` (all branches targeting `main`) and `push` to `main`. NO `pull_request_target`.

Workflow-level configuration: `name: CI`; `permissions: contents: read`; `concurrency: { group: 'ci-${{ github.workflow }}-${{ github.ref }}', cancel-in-progress: true }`. No secrets references.

SHA pinning (resolve at implementation time, write semver as trailing comment):
- `actions/checkout` (v4)
- `actions/setup-python` (v5)
- `astral-sh/setup-uv` (v6)

See `docs/plans/2026-06-03-docline-ci-workflow-plan.md` § F1 and the hardening table for full controls. Commit scope: `ops`.

<!-- BEGIN:AcceptanceCriteria -->
- `.github/workflows/ci.yml` exists and parses as valid YAML
- `actionlint` reports zero issues
- All five jobs declared and reference the gate commands unmodified
- All third-party `uses:` references pinned to full commit SHA with trailing semver comment
- Workflow-level `permissions: contents: read`; no job grants write
- `persist-credentials: false` on every `actions/checkout`
- Workflow-level `concurrency` with `cancel-in-progress: true`
- Triggers limited to `pull_request` + `push` to `main`; no `pull_request_target`
- Zero `secrets.*` references
<!-- END:AcceptanceCriteria -->