---
artifact_type: task
created_at: 2026-08-27T08:45:20Z
dependencies:
    - 064.026-T
id: 064.014-T
labels:
    - mcp
    - stdio
    - fetch
    - security
    - tests
    - test-first
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Author MCP untrusted-fetch end-to-end boundary harness (red)
updated_at: 2026-08-27T08:45:20Z
---

Author the failing MCP untrusted-fetch end-to-end boundary harness in tests/parity/test_mcp_stdio.py (test-first / red). Tests domain only. Proves the shared-fetch SSRF-by-resolution (H6) and resource caps (H7, incl. the byte-accurate aggregate budget) hold through the FULL tools/call fetch boundary (stdin JSON -> dispatch -> server.fetch -> execute_fetch), not just at the unit level. Authored red because the dispatch loop does not exist yet; all three scenarios green at 064.002-T once the dispatch loop routes server.fetch (the already-hardened shared guards from 064.011-T/064.013-T/064.017-T/064.024-T/064.026-T auto-enforce via execute_fetch; NO boundary wiring in 064.009-T is required). See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Plan Hardening H6/H7; Milestone model).

<!-- BEGIN:acceptance-criteria -->
- H6 end-to-end: a tools/call fetch to a public hostname that resolves to loopback/private is rejected end-to-end (mocked resolution), returning an isError result / policy rejection - not a successful fetch.
- H7 page cap end-to-end: a tools/call fetch with max_pages above the hard cap is rejected with -32602 at the boundary.
- H7 byte/aggregate cap end-to-end: a tools/call fetch whose response body exceeds MAX_RESPONSE_BYTES = 10 MiB (10485760 bytes, including on a redirect) OR whose crawl exceeds the aggregate MAX_TOTAL_FETCH_BYTES = 512 MiB (536870912 bytes) budget is aborted end-to-end (plan §H7 Selected numeric limits).
- All new tests observed FAILING before implementation (red): the stdio dispatch loop is not yet present.
- Scenario budget: 3 (SSRF hostname, max_pages -32602, response/aggregate byte cap) - within the <4 heuristic.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: test-first (write, verify red). Tests domain only (no production code - width isolation). Mock DNS resolution and use a fake oversized/redirecting/multi-page HTTP response so the tests are hermetic. This is the boundary integration counterpart to the shared-fetch unit harnesses 064.010-T/064.012-T/064.016-T/064.025-T. Milestone model: because the shared-fetch guards (064.011-T/064.013-T/064.017-T/064.024-T/064.026-T) enforce inside execute_fetch, ALL three scenarios green at 064.002-T (the transport loop) the moment it routes server.fetch - no boundary wiring in 064.009-T is required. Depends on 064.026-T (linear chain: the full aggregate budget plus the request-amplification bound are in place); is the last shared-fetch harness authored before the adapter/transport impls 064.015-T/064.002-T. The request-amplification depth over-limit (-32602) and fetch-attempt cap are proven at the unit level in 064.025-T; this boundary harness keeps its 3-scenario budget (SSRF, max_pages -32602, byte/aggregate cap).
<!-- END:implementation-notes -->
