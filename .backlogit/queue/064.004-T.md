---
artifact_type: task
created_at: 2026-08-27T07:46:04.823021Z
dependencies:
    - 064.036-T
id: 064.004-T
labels:
    - mcp
    - stdio
    - docs
    - architecture
    - stage-darkfactory
parent_id: 064-F
priority: medium
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Document docline-mcp stdio usage in README and the transport domain map in docs/ARCHITECTURE.md
updated_at: 2026-08-29T04:30:00.0000000Z
---

Document operator/agent usage of the local stdio MCP server. Docs domain only (two files: README.md and docs/ARCHITECTURE.md). Add a README section on running docline-mcp and a SELF-CONTAINED client MCP configuration example wiring docline-mcp over stdio. The example MUST be grounded in a concrete, verifiable configuration format — the documented GitHub Copilot / VS Code local MCP config shape (`.vscode/mcp.json` with a top-level `servers` map whose entries are stdio servers of the form `{ "type": "stdio", "command": ..., "args": [...] }`), matching the MCP stdio transport spec (`basic/transports/stdio.mdx`, already cited in the plan). Do NOT tell the author to mirror or reuse the repository's `.mcp.json`: that file is git-ignored (see `.gitignore`) and is NOT a tracked, verifiable repository source — the example must stand on its own in the README, not point at an untracked file. Do NOT add a separate design-doc transport note (the deliberation already documents the transport surface — avoid duplication). ALSO document the §H8 external-PDF-engine posture (delivered by 064.031-036): by DEFAULT the MCP server denies external, credential/network-bearing PDF engines (mistral_ocr) — omitted from the advertised process schema and rejected -32602 at dispatch — and enables them ONLY when the operator starts docline-mcp with the DOCLINE_MCP_ALLOW_EXTERNAL_PDF_ENGINES=1 env var or the --allow-external-pdf-engine flag; include a clear warning that enabling delegates paid external OCR calls + upload of workspace PDFs to the (still-untrusted) connected client, so it should be enabled only for trusted local clients. Depends on 064.036-T (the opt-in wiring must exist before it is documented). ALSO create docs/ARCHITECTURE.md (the file does not yet exist) as a concise TOP-LEVEL domain map and dependency-direction record for the whole tool — NOT an 064-F-only design note — per .github/instructions/architecture-doc.instructions.md progressive disclosure. It must show the two entry interfaces resolving through the shared application façade: docline-mcp console script → docline.mcp.__main__ → docline.mcp.stdio transport → DoclineMcpServer adapter (docline.mcp.server) → shared docline.app façade/contracts → fetch / process / readers / schema; and docline CLI → docline.cli → docline.app + docline.elt.orchestrate → the same fetch / process / readers / schema domains. State the dependency-direction invariants explicitly: the shared/core domains (app, fetch, process, readers, schema) do NOT import back into the mcp or cli interface packages; shared-fetch hardening (§H6/§H7 in fetch/) applies to BOTH the CLI and the MCP surface; the §H8 external-PDF-engine opt-in policy is MCP-boundary-specific (server.py + stdio.py + __main__.py) and does NOT change CLI behavior. Keep protocol/design rationale OUT of docs/ARCHITECTURE.md (it stays in the deliberation/plan) — the doc carries boundaries and direction only, no duplicated history. See docs/plans/2026-08-27-mcp-stdio-server-plan.md (T4; Plan Hardening §H8).

<!-- BEGIN:acceptance-criteria -->
- README gains a 'Running the local stdio MCP server' section covering docline-mcp and python -m docline.mcp.
- A SELF-CONTAINED client MCP configuration example wiring docline-mcp over stdio is included INLINE in the README (full config block, not a pointer to an external/untracked file), using the documented GitHub Copilot / VS Code `.vscode/mcp.json` `servers` stdio entry shape (`type: "stdio"`, `command`, `args`) so its shape is verifiable against a real, documented format.
- The example does NOT claim to reuse or mirror a repository `.mcp.json` (that file is git-ignored and untracked); no reference implies committing a `.mcp.json` to the repo.
- No duplicate design-doc transport note is added (folded into the existing deliberation).
- The README documents the §H8 default-deny of external PDF engines on the MCP surface, the exact server-side opt-in (DOCLINE_MCP_ALLOW_EXTERNAL_PDF_ENGINES=1 env var / --allow-external-pdf-engine flag), that a client can NEVER enable it via request fields, and a paid-call + workspace-PDF-upload warning for the enabled state (enable only for trusted local clients). CLI --pdf-engine mistral_ocr is unaffected.
- docs/ARCHITECTURE.md is created with a TOP-LEVEL domain map and explicit dependency direction covering the new stdio transport (src/docline/mcp/stdio.py), the executable bootstrap (src/docline/mcp/__main__.py + the docline-mcp console script), and the DoclineMcpServer adapter (src/docline/mcp/server.py), showing BOTH the MCP and CLI interfaces resolving through the shared docline.app façade to fetch/process/readers/schema.
- docs/ARCHITECTURE.md states the dependency-direction invariants: core/shared domains (app, fetch, process, readers, schema) do not import the mcp or cli packages; shared-fetch hardening is cross-interface (CLI + MCP); the §H8 external-engine opt-in is MCP-boundary-only and leaves CLI behavior unchanged.
- docs/ARCHITECTURE.md carries boundaries and dependency direction only — no duplicated protocol/design rationale (that stays in the deliberation and plan).
- Both docs render under the repo markdown rules: README.md keeps exactly one H1 with no skipped heading levels; docs/ARCHITECTURE.md has exactly one H1 (or YAML frontmatter with a title: field and no H1) and no skipped heading levels.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: docs-only; no code changes. Ground the example in the documented VS Code / GitHub Copilot local MCP config format (`.vscode/mcp.json`, top-level `servers`, stdio entry `type`/`command`/`args`) and the MCP stdio transport spec — a verifiable shape — rather than the repo's git-ignored `.mcp.json`. Keep the example fully self-contained in the README so a reader can copy it without reading any untracked file. Two files, single docs domain (README.md + docs/ARCHITECTURE.md), within the <3-file / 2-hour width envelope. docs/ARCHITECTURE.md is net-new; keep it a concise top-level map (a small diagram or table plus a dependency-direction list), NOT a whole-codebase manual and NOT an 064-F-only note. Ground the dependency arrows in the real imports (`docline.mcp.server` and `docline.cli` both import `docline.app`; the core packages never import the interface packages).
<!-- END:implementation-notes -->