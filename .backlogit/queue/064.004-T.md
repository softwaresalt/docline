---
artifact_type: task
created_at: 2026-08-27T07:46:04.823021Z
dependencies:
    - 064.003-T
id: 064.004-T
labels:
    - mcp
    - stdio
    - docs
    - stage-darkfactory
parent_id: 064-F
priority: medium
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Document docline-mcp stdio usage in README with a client MCP config example
updated_at: 2026-08-27T23:59:00.0000000Z
---

Document operator/agent usage of the local stdio MCP server. Docs domain only. Add a README section on running docline-mcp and a SELF-CONTAINED client MCP configuration example wiring docline-mcp over stdio. The example MUST be grounded in a concrete, verifiable configuration format — the documented GitHub Copilot / VS Code local MCP config shape (`.vscode/mcp.json` with a top-level `servers` map whose entries are stdio servers of the form `{ "type": "stdio", "command": ..., "args": [...] }`), matching the MCP stdio transport spec (`basic/transports/stdio.mdx`, already cited in the plan). Do NOT tell the author to mirror or reuse the repository's `.mcp.json`: that file is git-ignored (see `.gitignore`) and is NOT a tracked, verifiable repository source — the example must stand on its own in the README, not point at an untracked file. Do NOT add a separate design-doc transport note (the deliberation already documents the transport surface — avoid duplication). See docs/plans/2026-08-27-mcp-stdio-server-plan.md (T4).

<!-- BEGIN:acceptance-criteria -->
- README gains a 'Running the local stdio MCP server' section covering docline-mcp and python -m docline.mcp.
- A SELF-CONTAINED client MCP configuration example wiring docline-mcp over stdio is included INLINE in the README (full config block, not a pointer to an external/untracked file), using the documented GitHub Copilot / VS Code `.vscode/mcp.json` `servers` stdio entry shape (`type: "stdio"`, `command`, `args`) so its shape is verifiable against a real, documented format.
- The example does NOT claim to reuse or mirror a repository `.mcp.json` (that file is git-ignored and untracked); no reference implies committing a `.mcp.json` to the repo.
- No duplicate design-doc transport note is added (folded into the existing deliberation).
- Markdown passes the repo markdownlint config (single H1, heading increment).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: docs-only; no code changes. Ground the example in the documented VS Code / GitHub Copilot local MCP config format (`.vscode/mcp.json`, top-level `servers`, stdio entry `type`/`command`/`args`) and the MCP stdio transport spec — a verifiable shape — rather than the repo's git-ignored `.mcp.json`. Keep the example fully self-contained in the README so a reader can copy it without reading any untracked file.
<!-- END:implementation-notes -->