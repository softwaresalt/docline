---
artifact_type: task
created_at: 2026-09-15T21:20:00.0000000Z
id: 073.007-T
labels:
    - security
    - test
    - integration
    - elt-staging
parent_id: 073-F
priority: high
references:
    - docs/plans/2026-09-14-elt-staging-credential-redaction-plan.md
status: queued
title: Integration test - live default-path fetch stdout is credential-free
updated_at: 2026-09-15T22:10:00.0000000Z
---

Stream A / Unit AI (test domain, characterization-first). Live-sink integration test for the STREAM-A userinfo subset. Invoke the LIVE `docline fetch` default (non-`--execute`) CLI entrypoint (via the project CLI test runner or a subprocess) over a credentialed config of each URL-bearing source kind (WebCrawlSource, ManifestUrlSource, GitHubRepoSource, ManifestGitSource) and assert the REAL stdout JSON emitted at cli.py:381 contains no URL userinfo and no EXISTING recognized credential query param (e.g. `?token=`) for all four kinds.

SUBSET / FINAL-GATE HONESTY (Finding 5): this task asserts ONLY the stream-A subset (userinfo + already-recognized credential query params + benign-path preservation) and is FULLY satisfied by 073.002-T (A2) alone. The NEW credential query-param names (password/pwd/passwd/client_secret/refresh_token/code) are NOT asserted here — their live-stdout composition with A2 is proven by 073.009-T after BOTH A2 and B2 land.

BENIGN-PATH PRESERVATION (operator contract, 064-S/073-F — path-secret redaction REJECTED; the prior finding-F-04 path-embedded-secret fixture and its 073.006-T coupling are REMOVED): include fixtures whose credentialed URLs (userinfo + `?token=`) carry ORDINARY paths — `/authentication/overview`, `/tokenizer/config`, `/keys/rotation`, `/docs/100%25-off` — and assert the emitted metadata.source PATH component is preserved BYTE-FOR-BYTE (only URL userinfo and the recognized query credential are removed; the path, including the literal `%25` in `/docs/100%25-off`, is unchanged — no path decoding, no percent-decoding, no path redaction).

Acceptance criteria: (1) test invokes the real CLI fetch entrypoint (not orchestrate_fetch directly) and captures actual stdout; (2) FAILS on current HEAD (proves the live stdout leak — userinfo + existing recognized query param for all four URL source kinds); (3) ALL assertions (userinfo + existing-recognized-query absence for all four kinds, AND benign-path byte-for-byte preservation) go green after 073.002-T lands and require NO further task (test-first: 073.002-T depends_on this task); (4) NO new-credential-name assertion here (that composition is 073.009-T); (5) benign paths (`/authentication/overview`, `/tokenizer/config`, `/keys/rotation`, `/docs/100%25-off`) emitted UNCHANGED — no path decoding/redaction; (6) no path-secret or document-content assertion; (7) no production code changed. Execution posture: characterization-first / test-first. Files: one test file under tests/.

<!-- BEGIN:sizing -->
Size: S | Complexity: low
<!-- END:sizing -->