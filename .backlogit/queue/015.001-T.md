---
artifact_type: task
created_at: 2026-06-04T09:53:29.2609354-07:00
id: 015.001-T
labels:
    - processing
    - docling
    - sidecars
    - tdd-red
    - stage-harvested
    - shipment-14
parent_id: 015-F
priority: medium
references:
    - docs/plans/2026-06-04-G3c-docling-sidecars-plan.md
status: queued
title: Write failing tests for engine resolution + DOCX image walk + sidecar manifest (TDD RED)
updated_at: 2026-06-04T09:53:29.2609354-07:00
---

Create RED tests across 4 files: `tests/readers/test_pdf_engine_resolution.py` (6 tests), `tests/readers/test_docx_image_extraction.py` (7 tests), `tests/process/test_media_sidecars_in_manifest.py` (5 tests), `tests/test_cli_process.py` (3 tests). All tests fail (ImportError or AttributeError or argparse failure) before any implementation lands. Per plan-review F4, create `tests/test_cli_process.py` fresh if not present.

<!-- BEGIN:AcceptanceCriteria -->
- All 4 test files exist with all 21 named scenarios
- All tests fail (RED) — no green tests
- DOCX image-extraction fixtures are byte-built inline (one PNG, one JPEG, one with two images, one with unresolvable embed id, one with no images)
- Test for `defusedxml` usage verifies the secure parser is called
<!-- END:AcceptanceCriteria -->

<!-- BEGIN:ImplementationNotes -->
Reuse `_write_staging_job` / `_make_minimal_docx` / `_make_pdf_bytes_multi_page` helpers from `tests/elt/test_process_regression.py` and `tests/process/test_referentiality.py` patterns. For DOCX-with-images fixture: construct `[Content_Types].xml`, `_rels/.rels`, `word/_rels/document.xml.rels` with `<Relationship Id="rId1" Type=".../image" Target="media/image1.png"/>`, `word/document.xml` with `<w:drawing><a:graphic><a:graphicData><pic:pic><pic:blipFill><a:blip r:embed="rId1"/></pic:blipFill></pic:pic></a:graphicData></a:graphic></w:drawing>` inside a paragraph, and `word/media/image1.png` as a minimal valid PNG (8-byte sig + IHDR + IDAT + IEND).
<!-- END:ImplementationNotes -->