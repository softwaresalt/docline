---
artifact_type: task
created_at: 2026-09-14T07:34:30.2444086Z
dependencies:
    - 073.003-T
    - 073.008-T
    - 073.009-T
id: 073.004-T
labels:
    - security
    - code
    - elt-staging
parent_id: 073-F
priority: medium
references:
    - docs/plans/2026-09-14-elt-staging-credential-redaction-plan.md
status: queued
title: Expand credential query-param name vocabulary and matching
updated_at: 2026-09-15T21:20:00Z
---

Stream B / Unit B2 (code domain, test-first). QUERY-PARAMETER credential-NAME matching ONLY; path-embedded secret redaction (former stream C / 073.006-T) is REJECTED/retired per the operator contract and NOT in executable scope at all.

REPLACE STARTSWITH WITH EXPLICIT VOCABULARY (Finding 1): in src/docline/fetch/staging.py REPLACE the legacy case-insensitive `startswith`/prefix heuristic in _is_credential_param with matching against an EXPLICIT, case-insensitive, EXACT-match credential-name vocabulary. Individually enumerate the supported legacy/cloud variants in the vocabulary constant (_CREDENTIAL_PARAM_PREFIXES; rename to reflect exact-match semantics if useful): token, access_token, auth_token, refresh_token, api_key, apikey, key, secret, client_secret, auth, authorization, sig, signature, x-amz-credential, x-amz-signature, x-amz-security-token, x-goog-signature — PLUS the newly added password, pwd, passwd, code. Match EVERY name by case-insensitive EXACT equality; NO prefix / startswith / delimiter-boundary matching (which caused the tokenizer / keynote / secretary / authorship / signal and password_policy / pwd_length / code_version / passwd_file / refresh_token_ttl false positives).

BOUNDED DECODE — QUERY NAMES ONLY, BOTH PATHS (Finding 2): apply bounded percent-decoding to the query-parameter NAME on BOTH the string sanitizer path (_is_credential_param / sanitize_source) AND the typed sanitizer path (source_keys._remove_credential_query_params) via the SHARED matcher; re-check the vocabulary after EACH unquote layer, cap at 5 layers (_MAX_CREDENTIAL_DECODE_LAYERS), and fail closed (treat as credential) if the name is still transforming past the cap. Do NOT decode URL paths or query-parameter VALUES.

The shared _is_credential_param also widens the typed/059-S WARNING path via source_keys._remove_credential_query_params (intended, additive); the live WARNING new-name behavior is asserted by integration test 073.008-T (test-first dependency).

Acceptance criteria: (1) 073.003-T passes; (2) live-WARNING integration test 073.008-T passes for the new names; (3) existing sanitizer + 059-S/063-S tests green, and the enumerated legacy/cloud names still redact by exact match (no coverage regression for real credential names); (4) NO benign false positives — tokenizer/keynote/secretary/authorship/signal + password_policy/pwd_length/client_secretary/codec/code_version/passwd_file/refresh_token_ttl and case variants preserved; (5) bounded query-name decode active on BOTH string and typed paths, fail-closed over-cap; no path or query-VALUE decoding; (6) HONEST SUBSET/FINAL-GATE — after this task the NEW-credential-query-name portion of the A2+B2 composition test 073.009-T passes; FULL 073.009-T green additionally requires 073.002-T (A2, userinfo). Files: staging.py (1) plus the shared matcher reused by source_keys.py; functions: the credential-name vocabulary (data) + _is_credential_param. Execution posture: test-first.

<!-- BEGIN:sizing -->
Size: S | Complexity: medium
<!-- END:sizing -->