---
artifact_type: feature
created_at: 2026-06-29T18:38:09.2524655-07:00
custom_fields:
    harness_status: pending
id: 038-F
labels:
    - bug
    - process
    - ocr
    - memory
    - docling
status: queued
title: Bound batched docling worker OCR memory and isolate OOM blast radius
updated_at: 2026-06-29T18:38:09.2524655-07:00
---

Fix the batched docling worker OCR OOM crash (access violation 0xC0000005) observed during the 036.002-T cosmos sweep (mg1, 2026-06-27). A batched group crashed while RapidOCR ran (std::bad_alloc on the onnxruntime Clip node, numpy _ArrayMemoryError), and the whole group fell back to heuristic (subprocess_fallback_count=1), contaminating wall-clock.

Root cause (grounded in code):
- Concern (1) "conditional-OCR gating not applied on the batched path" is REFUTED. Both batched paths already forward per-range/per-chunk do_ocr (pdf_triage.py:570 via _range_needs_ocr; pdf_batch.py:284 via _chunk_needs_ocr), and 036-S tests pin it (test_conditional_ocr.py). page_needs_ocr flags a page only when text <200 chars AND it has embedded images, so a native-text Azure page that is mostly a screenshot with a short caption legitimately trips the gate.
- Concern (2) is the REAL defect: group_by_page_count (MAX_BATCHED_PAGES=40) bounds page count, not OCR/bitmap memory, and a hard OCR bad_alloc kills the entire worker subprocess so EVERY range/chunk in that group falls back to heuristic. One OCR page drags down up to 40 pages of OCR-free docling work.

Chosen fix direction: make batched grouping OCR-aware so OCR-flagged ranges/chunks are isolated under a tighter per-group cap. This bounds per-group bitmap memory (OCR is what loads bitmaps) and limits an OCR OOM blast radius to OCR work only, leaving OCR-free ranges in their own group to survive. Rejected: per-page DPI/downscale in the worker (touches docling internals, riskier); relying on serialize_docling (only triggers on low-RAM hosts; the OOM occurred on an ample-RAM box where batched mode was active).

Files: src/docline/process/page_range.py, src/docline/process/pdf_triage.py, src/docline/process/pdf_batch.py. Refs: docs/decisions/2026-06-28-merge-gap-tuning-verdict.md; .elt/output/results.txt ~lines 1448-1582. Source stash: D44A61E4.

<!-- BEGIN:dod -->
- New OCR-aware grouping unit tests pass in tests/process/test_page_range.py (red-first).
- Integration tests in tests/process/test_bounded_subbatching.py (and/or test_conditional_ocr.py) prove: (a) OCR-flagged ranges are isolated from OCR-free ranges across groups; (b) a simulated worker non-zero exit on an OCR group forces only that OCR range to heuristic while OCR-free ranges still receive docling output.
- Both batched dispatch paths (pdf_triage.py splice + pdf_batch.py chunks) use the OCR-aware grouping.
- Existing 036-S/037-S tests (conditional OCR, bounded sub-batching) still pass.
- Quality gates green: ruff check ., pyright src/, pytest, ruff format --check .
<!-- END:dod -->

<!-- BEGIN:goals -->
- Bound per-group bitmap/OCR memory in the batched docling worker so OCR rendering cannot exhaust memory within the MAX_BATCHED_PAGES page budget.
- Limit the blast radius of an OCR OOM hard-crash so only OCR-flagged ranges/chunks fall back to heuristic, never OCR-free docling ranges in the same dispatch.
- Preserve existing throughput for OCR-free corpora (no extra subprocess spawns when no range needs OCR).
- Add regression coverage that reproduces the group-crash-forces-whole-group-to-heuristic failure and proves OCR-free ranges survive.
<!-- END:goals -->