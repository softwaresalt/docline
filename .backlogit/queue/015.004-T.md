---
artifact_type: task
created_at: 2026-06-04T09:54:07.2926531-07:00
dependencies:
    - 015.003-T
id: 015.004-T
labels:
    - processing
    - cli
    - integration
    - stage-harvested
    - shipment-14
parent_id: 015-F
priority: medium
references:
    - docs/plans/2026-06-04-G3c-docling-sidecars-plan.md
status: queued
title: Wire pdf_engine through CLI + ProcessRequest + output_contract + manifest media_files (GREEN-3)
updated_at: 2026-06-04T09:54:07.2926531-07:00
---

Wire `pdf_engine` end-to-end. Add `pdf_engine: Literal["auto","docling","heuristic"] = "auto"` to `ProcessRequest` in `src/docline/app_models.py`. Add `--pdf-engine` argparse argument to the `process` subcommand in `src/docline/cli.py` (default `auto`). Extend `build_output_document_parts(..., layout_engine: str = "auto")` and route to `read_pdf(layout_engine=layout_engine)`. Extend `OutputDocumentPart` with `media_files: tuple[str, ...] = ()`. In `execute_process`: build a `CountingPictureSink` per source rooted at `{job_root}/{source_basename}/media/`, call `build_output_document_parts(file_path, rel_in_files, layout_engine=request.pdf_engine, picture_sink=sink)`, populate `OutputDocumentPart.media_files` from the sink's emitted references (cast to tuple of paths relative to `{job_id}/`). Add `media_files: list[str]` to the per-source manifest entry (use `list(document_part.media_files)`). Per plan-review F2: when any media is extracted for a source, force the multi-part output layout (`{source_basename}/part-0001.md` + `{source_basename}/media/figure-NNNN.png`) regardless of segment count so markdown `![](media/figure-0001.png)` resolves as a sibling. All 5 manifest-sidecar tests + 3 CLI tests pass. Full quality gates green.

<!-- BEGIN:AcceptanceCriteria -->
- `ProcessRequest.pdf_engine: Literal["auto","docling","heuristic"] = "auto"` accepted; invalid value raises validation error
- CLI `docline process --pdf-engine docling` accepted; `--pdf-engine bogus` exits non-zero; default `auto` when omitted
- `build_output_document_parts(..., layout_engine="auto", picture_sink=None)` signature
- `OutputDocumentPart.media_files: tuple[str, ...] = ()` (additive frozen-dataclass field)
- DOCX branch with media forces multi-part output layout (`{source}/part-0001.md` + `{source}/media/...`)
- DOCX branch with no media uses existing single-part `{source}.md` layout
- Manifest entries gain `media_files: list[str]` (empty list when no media)
- `_build_markdown_with_frontmatter` is NOT modified to attach `media_files` to `docline:` namespace — `media_files` lives ONLY on the manifest entry (per plan-review F5)
- All 5 manifest-sidecar tests + 3 CLI tests pass
- All 5 quality gates green (ruff format, ruff check, pyright, pytest, build)
- `docline export-schema` byte-identical to origin/main
<!-- END:AcceptanceCriteria -->

<!-- BEGIN:ImplementationNotes -->
In `execute_process`, before the per-part loop, instantiate the `CountingPictureSink` rooted at `{job_root}/{source_basename}/media/`. After `build_output_document_parts` returns, the parts already carry the `media_files` field populated. Build manifest entries with `list(part.media_files)`. The forced multi-part layout (F2 refinement): inside `build_output_document_parts` DOCX branch, if `picture_sink` emitted any media, set `part_count = max(part_count, 2)` BEFORE computing `_relative_output_path` — but that changes part filenames mid-build. Cleaner: have `build_output_document_parts` peek at media-extraction outcome first, then pass `force_multipart=True` to `_relative_output_path` when media is present. Update `_relative_output_path` signature to accept `force_multipart: bool = False`.
<!-- END:ImplementationNotes -->