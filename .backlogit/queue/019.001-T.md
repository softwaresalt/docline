---
artifact_type: task
created_at: 2026-06-06T15:45:01.6991045-07:00
id: 019.001-T
labels:
    - pdf
    - triage
    - scorer
    - test-first
parent_id: 019-F
priority: high
references:
    - docs/plans/2026-06-06-triage-then-repair-plan.md
    - docs/scratch/2026-06-06-fidelity-scorer-poc.py
status: queued
title: U1 — Fidelity scorer module
updated_at: 2026-06-06T15:45:22.7020312-07:00
---



<!-- BEGIN:acceptance-criteria -->
- All public APIs strictly typed.
- `PageScore` is `@dataclass(frozen=True)`.
- `score_page(page_index, text, page_metadata=None)` returns a populated `PageScore` with all 7 signals + aggregate + `needs_docling` decision.
- Each signal function returns a float in `[0.0, 1.0]`.
- Weight override loads from JSON file path when provided; falls back to module defaults when absent.
- RED tests written first and fail; GREEN after implementation.
- Test scenarios: (1) each signal on a 1-line trigger and 1-line non-trigger input; (2) combiner: clean prose → no flag; each of five POC failure modes → flagged with correct `reason`; (3) `page_metadata=None` falls through gracefully (no metadata-dependent signals fire); (4) weight override via JSON loads and is applied.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Lift the proof-of-concept scorer at `docs/scratch/2026-06-06-fidelity-scorer-poc.py` into `src/docline/process/fidelity_scorer.py` with production hardening. Provides seven pure-function fidelity signals (char_density, non_ascii_ratio, long_unbroken_line, column_gap, table_char_density, image_heavy, form_fields), a weighted combiner `score_page()`, and frozen `PageScore` dataclass results. Signal weights externalized via JSON loader so they can be tuned without code changes.

Files:
- `src/docline/process/fidelity_scorer.py` (new, ~250 LOC)
- `tests/process/test_fidelity_scorer.py` (new)

Exception lineage: `FidelityScorerError(DoclineError)` — import from `docline.schema.models` per existing pattern.

Plan review P3: explicit import path for `DoclineError` confirmed at `src/docline/schema/models.py:9`.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Follow Constitution II Test-First: write `tests/process/test_fidelity_scorer.py` first, confirm RED, then implement the module.

Keep signal functions pure — no I/O, no module-level mutable state. Weight loader caches the parsed JSON on first call.

The POC at `docs/scratch/2026-06-06-fidelity-scorer-poc.py` is the reference implementation — lift it largely as-is, then add the typed exception, the JSON weight loader, and explicit type annotations on all returns.
<!-- END:implementation-notes -->