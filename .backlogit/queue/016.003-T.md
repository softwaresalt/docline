---
artifact_type: task
created_at: 2026-06-04T11:32:20.1713145-07:00
dependencies:
    - 016.002-T
id: 016.003-T
labels:
    - processing
    - readers
    - docling
    - tdd-green
    - stage-harvested
    - shipment-15
parent_id: 016-F
priority: medium
references:
    - docs/plans/2026-06-04-post-g3-hygiene-plan.md
    - docs/decisions/2026-06-04-post-g3-hygiene-plan-review.md
status: active
title: Wire picture_sink through read_pdf + docling tuning + picture routing (GREEN-2)
updated_at: 2026-06-04T11:33:22.2328999-07:00
---

Add `picture_sink: PictureSink | None = None` kwarg to `read_pdf` and `read_pdf_pages` in `src/docline/readers/pdf.py`. Forward it to `_read_pdf_docling_pages` (when the resolved engine is docling). Extend `_read_pdf_docling_pages` to configure `PdfPipelineOptions(do_table_structure=True, table_structure_options=TableStructureOptions(do_cell_matching=True), generate_picture_images=picture_sink is not None, images_scale=2.0)`. When `picture_sink` is provided AND docling exposes rendered pictures, iterate `result.document.pictures` and emit each picture's PNG bytes through `picture_sink.emit(mime='image/png', data=png_bytes)`. If docling's picture attribute API differs from the assumption, log a `_log.warning` and skip picture emission (markdown still flows). In `src/docline/process/output_contract.py`, thread `picture_sink` through to `read_pdf` for the PDF branch (DOCX branch already passes it). All 3 docling-side tests pass (skip-gated on docling availability). Full quality gates green.