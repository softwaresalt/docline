---
artifact_type: task
created_at: 2026-08-27T09:00:00.0000000Z
dependencies:
    - 064.014-T
id: 064.015-T
labels:
    - mcp
    - stdio
    - adapter
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Implement DoclineMcpServer.call_tool allow-list + list_callable_tools()
updated_at: 2026-08-27T09:00:00.0000000Z
---

Implement the adapter callable surface on DoclineMcpServer (src/docline/mcp/server.py) so the MCP transport has a single, guarded tool-dispatch source. Code domain only (<=2 files: src/docline/mcp/server.py). Extracted from 064.002-T so the transport task stays at <=4 functions, and so the list_tools()/list_callable_tools() invariant is owned in one place (Architecture/Scope cycle-2b finding). Greens the adapter-level assertions in 064.001-T. Depends on 064.014-T (the full red harness chain is authored first). See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Design "Advertised set == callable set"; T-adapter).

<!-- BEGIN:acceptance-criteria -->
- DoclineMcpServer.call_tool(name, arguments) dispatches via a static {name: bound_method} allow-list (fetch, process, export_schema) - NO getattr(server, name) (avoids dunder/attribute dispatch injection); unknown names raise a typed error the transport maps to -32602 (fail-closed).
- DoclineMcpServer.list_callable_tools() returns the callable allow-list manifest: ingest_local_dir EXCLUDED; the process inputSchema OMITS workspace_root; built from the same allow-list call_tool dispatches (advertised == callable).
- The legacy list_tools() is left UNCHANGED (full four-tool shared manifest) so tests/parity/test_manifest_parity.py stays green with no edit.
- Documented adapter invariant: list_tools() is the manifest-parity accessor ONLY and MUST NOT be used as an MCP advertise source; list_callable_tools() is the sole MCP advertise source. Assert every tool in list_callable_tools() is dispatchable via call_tool (no advertise-but-uncallable gap on the MCP surface).
- Greens the adapter-level assertions in 064.001-T (list_callable_tools parity, dispatchability, list_tools-unchanged, invariant); existing suites remain green.
- ruff check ., ruff format --check ., pyright src/ clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green (adapter surface) after the full red harness chain. Width-isolated to the adapter module (server.py). Two new methods (call_tool, list_callable_tools) + optionally a small build-time schema helper (<=4 functions, 1 file). Keep ONE static allow-list as the single source of truth shared by call_tool and list_callable_tools. The transport loop (064.002-T) consumes these methods; do NOT put transport logic here. Depends on 064.014-T; predecessor of 064.002-T.
<!-- END:implementation-notes -->
