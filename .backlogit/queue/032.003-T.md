---
artifact_type: task
created_at: 2026-06-18T18:05:14.3303175-07:00
custom_fields:
    blocked_reason: 'RESOLVED 2026-06-23: deliberation complete (docs/decisions/2026-06-23-bounded-sub-batching-deliberation.md) and both dependencies satisfied (032.001-T probe done; 032.002-T envelope gating done). The original "docling unavailable to the agent" premise was disproven during the 6E6754D4 investigation (docling runs in .venv). Staged into shipment 037-S.'
    source_stash_id: 1182227F
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: '[follow-up from 033-S — perf recovery] Bounded sub-batching for the docling batched worker. ROOT CAUSE (confirmed 2026-06-17): 032-S batched mode ran ALL ranges in ONE subprocess; on the cosmos corpus (86 ranges / 1818 pages) it exhausted memory and was killed → 86/86 docling fallback. Single-chunk worker works fine (diagnostic: 30-page splice → 54685 chars success). The per-chunk subprocess design exists specifically to reclaim torch memory between chunks (worker docstring). 033-S reverted use_batched_worker default to False (per-chunk = memory-safe), giving up the modest batched perf win (~5% cold-start dedup). RECOVERY DESIGN: instead of all-or-nothing batched vs per-chunk, sub-batch the manifest into bounded GROUPS capped by cumulative page count (T4 data: 30 pages ≈ 2GB RSS for ONE conversion, but batched mode ACCUMULATES across conversions in one process, so cap conservatively — suggest MAX_BATCHED_PAGES ≈ 32-48). Spawn a fresh worker subprocess per group (reclaims memory between groups) while amortizing model load WITHIN a group. cosmos 1818 pages / 40-cap ≈ 45 subprocesses vs 86 per-chunk vs 1 unsafe-giant. Needs: (a) group-by-page-count helper in pdf_triage + pdf_batch; (b) per-group --batch invocation; (c) combine with stash F676E692 (gate per-chunk on own envelope so a group crash doesn''t discard the group''s successful chunks); (d) empirical re-validation on cosmos (memory stays bounded, docling actually runs, wall-clock vs per-chunk). This is a reviewed design decision — recommend a deliberation before building. References: docs/decisions/2026-06-14-docling-batch-size-probe.md, .elt/output/cosmos-triage-022/pa3-summary.json.'
dependencies:
    - 032.001-T
    - 032.002-T
id: 032.003-T
labels:
    - docling
    - perf
    - batched-worker
    - deliberated
parent_id: 032-F
priority: medium
status: review
title: Bounded sub-batching for docling batched worker (033-S perf recovery)
updated_at: 2026-06-23T18:19:35.9713237-07:00
---



<!-- BEGIN:acceptance-criteria -->
- group-by-cumulative-page-count helper added (shared or mirrored in pdf_triage + pdf_batch); manifest split into bounded GROUPS capped by MAX_BATCHED_PAGES=40.
- A fresh --batch worker subprocess is spawned per group; model load amortized WITHIN a group, torch memory reclaimed BETWEEN groups.
- Per-range/per-chunk do_ocr (036-S) is preserved into each group's manifest.
- Combines with 032.002-T per-chunk envelope gating: a mid-group crash does not discard the group's successfully-written chunk envelopes.
- Unit tests (existing fake-runner harness): N items + cap C produce ceil-packed groups in doc order; a >cap single item is its own group; per-group manifest carries do_ocr; engages only when batched mode active.
- ruff, ruff format, pyright (no new errors), pytest green.
- RUNTIME-VERIFICATION GATE (deferred, manual): cosmos re-validation — peak RSS bounded, docling actually runs (not 100% fallback), wall-clock recorded vs per-chunk baseline. Heavy docling inference; runnable here (full azure-cosmos-db.pdf + docling .venv present) but slow. NOT a build blocker; gates any future default flip.
- use_batched_worker default stays False this shipment (promoting bounded-batched to default is a separate decision gated on the cosmos numbers).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
DELIBERATED 2026-06-23 — design decided in docs/decisions/2026-06-23-bounded-sub-batching-deliberation.md (Option A: group-by-cumulative-page-count). Chosen MAX_BATCHED_PAGES=40 (named module constant), calibrated from the 032.001-T probe (single 30-page conversion peaks ~2GB RSS; batched accumulates -> the 1818-page OOM). Greedy bin-pack over existing range/chunk order (preserve doc order for splice-back); a single oversized range/chunk forms its own group. One fresh --batch worker per group reclaims torch memory between groups while amortizing model load within a group. Carry per-range/per-chunk do_ocr (036-S) into each group's manifest unchanged. Reuse 032.002-T per-chunk envelope gating so a mid-group crash preserves the group's written envelopes. Engages only when batched mode is active (use_batched_worker=True, N>=2, not serialize_docling); per-chunk default (033-S) unchanged. Sites: pdf_triage splice-jobs manifest builder + pdf_batch _run_chunks_batched manifest builder. cosmos 1818/40 ~= 46 subprocesses vs 86 per-chunk vs 1 unsafe-giant. Depends on 032.001-T (probe data, DONE) and 032.002-T (envelope gating, DONE).
<!-- END:implementation-notes -->