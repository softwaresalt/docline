---
artifact_type: task
created_at: 2026-08-29T05:15:00.0000000Z
dependencies:
    - 065.001-T
id: 065.002-T
labels:
    - chore
    - security
    - ssrf
    - fetch
    - sitemap
    - cgnat
    - code
    - stage-darkfactory
parent_id: 065-F
priority: high
references:
    - docs/plans/2026-08-28-sitemap-cgnat-ssrf-gap-plan.md
status: queued
title: Extend _is_unsafe_address with explicit CGNAT 100.64.0.0/10 check (green)
updated_at: 2026-08-29T05:15:00.0000000Z
---

Implement the explicit CGNAT `100.64.0.0/10` membership check in `_is_unsafe_address` (`src/docline/fetch/sitemap.py`) to green the 065.001-T harness. Code domain only (single module, <=1 function touched + one module-level constant). Depends on 065.001-T. See `docs/plans/2026-08-28-sitemap-cgnat-ssrf-gap-plan.md`.

<!-- BEGIN:acceptance-criteria -->
- Add a module-level constant `_CGNAT_NETWORK = ipaddress.ip_network("100.64.0.0/10")` (stdlib `ipaddress` already imported; no new dependency).
- In `_is_unsafe_address`, after the existing six-flag classification, reject (return `True`) when the parsed `ip` is contained in `_CGNAT_NETWORK` (e.g. `ip in _CGNAT_NETWORK`). Preserve the existing fail-closed `ValueError` handling and `_METADATA_IPS` check; the CGNAT clause is additive.
- All 065.001-T CGNAT rows pass (class-pin, IP-literal, resolved-host/rebinding, boundaries `100.64.0.0`/`100.127.255.255` rejected), and the boundary-accept rows (`100.63.255.255`, `100.128.0.0`, public-unicast) stay accepted — no over-rejection.
- All pre-existing `tests/fetch/test_sitemap.py` rows remain green (no behavior change for loopback/private/link-local/multicast/reserved/unspecified/metadata classes).
- Update the module docstring's SSRF-classification note (step 5) to include CGNAT `100.64.0.0/10` among the rejected classes, keeping the documented contract accurate.
- Quality gates green: `ruff check .`, `ruff format --check .`, `pyright src/`, `pytest tests/fetch/test_sitemap.py`.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green. Mirrors the §H6 fetch-path CGNAT membership check delivered by feature 064-F in `docline.fetch.url_policy` (same address class, different surface — 065-F is linked `related_to` 064-F). Keep the change tight: one constant + one `if ip in _CGNAT_NETWORK: return True` clause; do NOT refactor the classifier or consolidate with `url_policy` (out of scope — deliberation Option B, deferred). No change to `validate_sitemap_url`'s structure; the membership check runs inside `_is_unsafe_address` so both the IP-literal and resolved-address paths inherit it. Touches only `src/docline/fetch/sitemap.py` — disjoint from the 064-F/§H6 files, so no merge-order conflict with 055-S.
<!-- END:implementation-notes -->
