---
artifact_type: task
created_at: 2026-08-29T03:40:10.0000000Z
dependencies:
    - 064.032-T
id: 064.033-T
labels:
    - mcp
    - stdio
    - security
    - tests
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Author §H8 external-engine transport-mapping harness (dual-era -32602)
---

Author the RED §H8 transport-mapping harness (tests/parity/test_mcp_stdio.py). Tests domain only. Proves the stdio transport maps the adapter's typed ExternalEngineNotAllowedError (raised by 064.032-T's chokepoint) to a JSON-RPC -32602 invalid-params envelope over the wire, in BOTH protocol eras, so a hostile client's tools/call carrying a non-allow-list pdf_engine is refused end-to-end on a default server. Complements the adapter-policy pair (064.031-T/064.032-T): those prove the instance raises the typed error; this pair proves the transport turns it into the correct wire code identically for legacy and modern. Mirrors §H1's dual-era workspace_root -32602 reject funneled through ONE hardened dispatch. Verifies red until 064.034-T. See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Plan Hardening §H8; §H1 dual-era reject parity; H4 -32602 mapping).

<!-- BEGIN:acceptance-criteria -->
- Scenario (a) [legacy wire reject; green@064.034]: on a DEFAULT server over stdio, a legacy tools/call process with {pdf_engine:"mistral_ocr"} returns a -32602 invalid-params envelope (mapping the adapter's ExternalEngineNotAllowedError); the egress sentinel is NOT called. No-secret: the -32602 message/data names the engine only - no absolute paths / full-arguments echo (§H3) and no ambient credential value (AZURE_AI_FOUNDRY_KEY / AZURE_AI_FOUNDRY_ENDPOINT / MISTRAL_API_KEY) in the envelope or any captured log line.
- Scenario (b) [modern wire reject parity; green@064.034]: on a DEFAULT server, a MODERN (_meta-bearing, no prior initialize) tools/call process with {pdf_engine:"mistral_ocr"} returns -32602 IDENTICALLY - proving both eras funnel the mapping through the SAME hardened dispatch (no modern pre-handshake bypass), the §H8 counterpart to the §H1 dual-era guard-parity assertion.
- Scenario (c) [opt-in accept anchor - green at authoring from 064.032; stays green@064.034]: on an OPT-IN server over stdio, tools/call process {pdf_engine:"mistral_ocr"} is NOT rejected -32602 (the adapter does not raise on an opt-in server, so the transport dispatches normally; egress is STUBBED - never a real paid call). This is a regression anchor (green once 064.032 lands) that guards against the mapping over-rejecting when opted in; it is NOT a red-at-authoring scenario.
- Scenarios (a)/(b) observed RED at authoring (the transport has no -32602 mapping for ExternalEngineNotAllowedError yet, so the typed error would surface as -32603 or crash); scenario (c) is a green anchor. No production code changed (tests only).
- Scenario budget: 3 (legacy wire reject + no-secret; modern wire reject parity; opt-in accept anchor) - within the <4 heuristic.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: red-only transport harness authored before 064.034-T. Width-isolated: tests domain only (tests/parity/test_mcp_stdio.py). Drive the stdio dispatch (the same harness helpers 064.006-T/064.021-T use) with legacy and modern envelopes; assert the -32602 code + no-secret message on the default server and non-rejection on the opt-in server. Stub the mistral egress boundary so no real paid call occurs on any path. Depends on 064.032-T (the adapter raises ExternalEngineNotAllowedError). Greened by 064.034-T (the stdio.py -32602 mapping). See plan §H8.
<!-- END:implementation-notes -->
