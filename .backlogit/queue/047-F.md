---
artifact_type: feature
created_at: 2026-07-04T16:42:00.3321644-07:00
custom_fields:
    harness_status: pending
    source_stash_id: 3CFD945D
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: medium
    source_stash_text: 'Reduce CI build actions to only run on code changes, to conserve GitHub Actions usage limits. Two complementary strategies for .github/workflows/ci.yml: (1) add top-level paths-ignore: on the on: block so doc/backlog-only pushes skip the heavy matrix (e.g. .backlogit/**, docs/**, **/*.md, README.md); (2) add a PR-title-based guard on the build/test jobs, e.g. if: | !startsWith(github.event.pull_request.title, ''chore:'') && !startsWith(github.event.pull_request.title, ''docs:'') so chore:/docs: PRs skip build. NOTE: ci.yml is currently PAUSED (triggers only on tags/releases/manual dispatch) for cost control, and it already carries a ''PENDING - add paths-ignore'' comment (item 3). Fold this in when re-enabling PR/push CI. Keep the two strategies independent so either can ship alone.'
id: 047-F
priority: medium
status: queued
title: 'CI cost reduction: skip build actions on non-code changes'
updated_at: 2026-07-04T16:42:00.3321644-07:00
---

Reduce GitHub Actions usage by making CI act only on code changes. Two independent, complementary guards for .github/workflows/ci.yml: (1) top-level paths-ignore so doc/backlog-only pushes skip the matrix (.backlogit/**, docs/**, **/*.md, README.md); (2) PR-title if-guards on the heavy jobs (test matrix + build) to skip chore:/docs: PRs. CI is currently PAUSED (release/tags/dispatch only) and carries a PENDING paths-ignore comment (item 3) to fold in. Source: stash 3CFD945D.