---
artifact_type: task
created_at: 2026-06-04T00:03:41.0883666-07:00
dependencies:
    - 014.001-T
id: 014.002-T
labels:
    - processing
    - referentiality
    - tdd-green
    - stage-harvested
    - shipment-13
parent_id: 014-F
priority: medium
references:
    - docs/plans/2026-06-04-G3b-referentiality-plan.md
    - docs/decisions/2026-06-04-G3b-referentiality-plan-review.md
status: queued
title: Implement extract_section_title + docline namespace builder (TDD GREEN)
updated_at: 2026-06-04T00:03:41.0883666-07:00
---

Implement `extract_section_title(segment) -> str | None` in `src/docline/process/segment.py`. Extend `OutputDocumentPart` with `section_title: str | None = None` (additive frozen-dataclass field). Populate it in `build_output_document_parts` via `extract_section_title`. Add `_build_docline_namespace` and `_relative_sibling` helpers in `src/docline/app.py`; populate `base_data["docline"]` before `assemble_frontmatter_payload`. Reuse `_build_document_id(job_id, input_path, ingest_order=0)` for `parent_document_id` to ensure all parts of a source share the same id. All 10 referentiality tests + 3 helper tests pass.

<!-- BEGIN:AcceptanceCriteria -->
- `extract_section_title` returns the H1 text (stripped) or None
- `OutputDocumentPart.section_title: str | None = None` added (frozen dataclass, additive default)
- `build_output_document_parts` calls `extract_section_title` on each segment and passes the result to `OutputDocumentPart`
- `_build_docline_namespace` and `_relative_sibling` defined in `src/docline/app.py`
- `base_data["docline"]` populated for every processed part before `assemble_frontmatter_payload`
- All tests from 014.001-T pass (GREEN)
- `ruff format/check` and `pyright src/` clean on changed files
- Do NOT modify `assemble_markdown` signature default — keep at False for unit-test clarity
<!-- END:AcceptanceCriteria -->

<!-- BEGIN:ImplementationNotes -->
`parent_document_id`: call `_build_document_id(job_id, normalized_input_path, ingest_order=0)` for every part of a source — the 0 collapses per-part variance and yields a deterministic shared id. `prev_part`/`next_part`: compute from the list of `OutputDocumentPart.relative_output_path` for the source, returning the `.name` of the sibling at offset ±1 or None at boundaries. `section_title`: None when the segment came from char-bin fallback; the helper detects absence of H1.
<!-- END:ImplementationNotes -->