---
artifact_type: task
created_at: 2026-08-28T23:40:00.0000000Z
dependencies:
    - 064.029-T
id: 064.030-T
labels:
    - fetch
    - security
    - dos
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Debit redirect-hop fetch-attempts in redirect_request (post-validation)
---

Make 064.029-T green. Code domain only, <=1 file (src/docline/fetch/http.py). Cycle-12 (PR #166 Copilot cycle-3 Finding B): MOVE the per-hop MAX_FETCH_ATTEMPTS attempt debit OUT of the http_error_302 override (the rejected cycle-11 placement, which debited BEFORE delegating to super() and therefore charged an attempt even for redirects that stdlib scheme/loop validation or §H6 revalidation REJECTS before parent.open()) and INTO _ValidatingRedirectHandler.redirect_request - debiting exactly ONCE immediately BEFORE returning a non-None Request, i.e. AFTER the stdlib super().redirect_request scheme/same-origin build AND the §H6 address-pinned per-redirect revalidation, and BEFORE the hop's outbound I/O (parent.open()). Because redirect_request returns a non-None Request ONLY when the redirect will actually be followed, debiting at that point counts an attempt if-and-only-if a real outbound hop is about to occur; a redirect that stdlib rejects (redirect_request returns None) or that §H6 revalidation rejects (raises) consumes NO attempt. Reuses the SAME request-scoped RemainingByteBudget MAX_FETCH_ATTEMPTS allowance seeded by 064.026-T and ALREADY threaded into _ValidatingRedirectHandler.__init__ by 064.028-T - no new threading, no new constant, no new file. Raises FetchAttemptBudgetExceededError (a DoclineError subclass of AggregateBudgetExceededError) when a debit would cross MAX_FETCH_ATTEMPTS, from inside redirect_request before it returns the Request (so before outbound I/O); the four existing crawl.py except AggregateBudgetExceededError: raise clauses propagate it out of crawl() with NO crawl.py edit. The intermediate-fp closure on this attempt-breach path is provided by 064.028-T's existing http_error_302 closure guard (which catches AggregateBudgetExceededError and thus its FetchAttemptBudgetExceededError subclass) - no duplicate close logic here. handler.redirect_count stays observability-only, never post-hoc enforcement. This is the redirect-hop half of the §H7 item-4a request-amplification bound, DECOMPOSED into its own width-isolated pair (harness 064.029-T). Hardens BOTH interfaces (CLI docline fetch and the MCP fetch tool) - a cross-interface behavior change (see plan Rollback/Risks/SA-1). Does NOT change app_models.py or fetch/crawl.py. See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Plan Hardening H7 item 4a; cycle-12 placement remediation).

<!-- BEGIN:acceptance-criteria -->
- fetch/http.py: the per-hop MAX_FETCH_ATTEMPTS attempt debit is placed at the END of _ValidatingRedirectHandler.redirect_request, immediately before it returns a non-None Request - after the stdlib super().redirect_request build (scheme/same-origin) AND after the §H6 address-pinned per-redirect revalidation. The debit is REMOVED from http_error_302 (which retains only the bounded body drain + closure guard from 064.028-T). One attempt is debited per FOLLOWED hop; pre-I/O (before parent.open()).
- No debit on reject: the debit sits at the END of redirect_request, so (a) a redirect the stdlib http_error_302 SCHEME check rejects (raises HTTPError before redirect_request is called) never reaches the debit; (b) when super().redirect_request returns None (stdlib declines the method/code) the override returns None WITHOUT debiting; (c) when §H6 revalidation rejects newurl it raises BEFORE the debit. These common rejection cases consume NO fetch-attempt - closing the Finding-B over-count that the cycle-11 http_error_302-before-super() placement caused for ALL of them.
- Bounded CONSERVATIVE residual (documented, verified against CPython http_error_302 source): two paths debit before their parent.open() runs - (i) the stdlib LOOP-detection check (max_repeats / max_redirections) fires in http_error_302 AFTER redirect_request returns a non-None Request (hence after this debit) but before parent.open(); (ii) a per-response/aggregate body-drain breach on the current 3xx body fires after redirect_request but before parent.open(). Each over-counts by exactly one attempt on a terminating (or retried) hop. This is CONSERVATIVE for a DoS UPPER bound: the redirect_request placement NEVER UNDER-counts a followed hop (every followed hop is debited before its parent.open), so the over-count can only make MAX_FETCH_ATTEMPTS trip EARLIER, never allow more outbound work than the cap - the security invariant holds. It is bounded (at most one per such hop; retries add at most one phantom debit per retried pop), harmless, and unavoidable without reimplementing the stdlib method body (forbidden). Consistent with the existing cycle-10 "loop-detection/disallowed-scheme paths hold an unread fp" residual in plan Risks. The redirect_request placement is chosen over a body-proxy-close() placement (which would be exact/residual-free) to keep the attempt concern WIDTH-ISOLATED from 064.028's body-drain proxy per the cycle-12 decomposition.
- Attempt breach: a debit that would cross MAX_FETCH_ATTEMPTS raises FetchAttemptBudgetExceededError from inside redirect_request BEFORE returning the Request (before the crossing hop's outbound I/O); propagated out of crawl() by the existing except AggregateBudgetExceededError: raise clauses (no new crawl.py re-raise). The first debits up to the cap succeed; the crossing debit is refused pre-I/O.
- Intermediate-fp closure on attempt breach: the FetchAttemptBudgetExceededError raised from redirect_request propagates through http_error_302, where 064.028-T's closure guard (except AggregateBudgetExceededError: fp.close(); raise) releases the intermediate 3xx response fp - no leaked connection, no duplicate close logic added here.
- 064.029-T harness passes green (debit placement incl. no-debit-on-reject; attempt-breach refused before outbound I/O). The redirect-body drain + closure (064.027-T/064.028-T) and the direct-outbound-call boundary debit (064.025-T/064.026-T) remain green.
- The existing tests/fetch suite remains green: the attempt allowance defaults to unbounded/None for a standalone single fetch (no threaded crawl budget), so existing CLI single-fetch callers are unaffected. Typed errors only; no bare except.
- ruff check ., ruff format --check ., pyright src/ clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green (shared-code hardening) after 064.029-T. Single file: src/docline/fetch/http.py. Function/width budget (2-hour/<5-function guardrail, <=1 file): (1) add the one-line MAX_FETCH_ATTEMPTS attempt debit at the end of the existing _ValidatingRedirectHandler.redirect_request override, immediately before `return new_request`, after §H6 revalidation; (2) REMOVE the attempt debit from the http_error_302 override (leaving only the 064.028-T body drain + closure guard there). No new function, no new class, no new constant - the attempt allowance and FetchAttemptBudgetExceededError already exist (seeded/typed by 064.026-T) and the RemainingByteBudget is already threaded into the handler by 064.028-T. Width-isolated to the redirect-hop attempt-accounting surface (single skill domain: code), distinct from the redirect-body drain (064.028-T). Depends on 064.029-T; transitively relies on 064.026-T (MAX_FETCH_ATTEMPTS allowance) and 064.028-T (budget threaded into the handler + the http_error_302 closure guard that releases fp on the attempt-breach path) being in place upstream (chain 064.026 -> 064.027 -> 064.028 -> 064.029 -> 064.030). Feeds the MCP untrusted-fetch end-to-end boundary harness 064.014-T (re-pointed onto this task), completing the request-amplification bound across direct outbound calls AND redirect hops. Must NOT weaken the §H6 SSRF-by-resolution per-redirect revalidation - the debit is added AFTER H6 revalidation so validation still gates every followed hop.
<!-- END:implementation-notes -->
