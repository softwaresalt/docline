---
artifact_type: task
created_at: 2026-08-27T15:08:18.9128218Z
dependencies:
    - 064.016-T
id: 064.017-T
labels:
    - fetch
    - security
    - dos
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Retain raw body byte count and sum exact bytes in crawl
updated_at: 2026-08-27T15:10:37.5912827Z
---

Make 064.016-T green. Code domain only, <=2 files (fetch/http.py, fetch/crawl.py). Add FetchResponse.body_byte_count:int set from the length of the bytes read by the bounded reader BEFORE decoding (the body_bytes already materialized by the streamed read in 064.013-T), and make the fetch/crawl.py aggregate accumulator sum THAT exact value (never a character count or a re-encode) and abort once MAX_TOTAL_FETCH_BYTES is exceeded. Isolated from 064.013-T so the shared-model (FetchResponse) blast radius and byte-accurate accounting land in one bounded task. Cross-interface (CLI crawl too). See plan H7 item 3 / cycle-3 remediation.

<!-- BEGIN:acceptance-criteria -->
- FetchResponse gains body_byte_count:int, set from len(body_bytes) captured BEFORE body_bytes.decode(...) in fetch/http.py fetch_page (the raw wire byte count, not len(body) or len(body.encode())).
- fetch/crawl.py aggregate accumulator sums response.body_byte_count exactly and aborts the crawl when the running total exceeds MAX_TOTAL_FETCH_BYTES; the abort propagates out of the crawl loop and is NOT swallowed by the per-page retry except handler.
- Auxiliary fetches (robots.txt via _robots_allow, mdBook TOC scripts via _discover_toc_links) also accrue their response.body_byte_count to the SAME MAX_TOTAL_FETCH_BYTES running total, so oversized-but-under-cap auxiliary payloads cannot amplify transfer outside the budget (cycle-3 adversarial re-review P3).
- 064.016-T harness passes green (non-ASCII multibyte, invalid-byte, char-under-but-raw-over scenarios).
- Existing tests/fetch suite remains green (body_byte_count is additive; existing response.body consumers unaffected). Typed errors only; no bare except.
- ruff check ., ruff format --check ., pyright src/ clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green after 064.016-T. body_byte_count is derived from bytes the bounded reader already holds - no extra network read. Keep the field additive (default/derived) so the frozen dataclass and all response.body consumers stay valid. Width-isolated: code domain, <=2 files. Depends on 064.016-T.
<!-- END:implementation-notes -->