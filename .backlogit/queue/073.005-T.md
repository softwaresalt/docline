---
artifact_type: task
created_at: 2026-09-15T20:53:50.3793086Z
custom_fields:
    blocked_reason: 'Operator scope-correction (064-S/073-F): path-embedded secret redaction REJECTED. Stream C test task retired non-destructively alongside 073.006-T; not deleted.'
id: 073.005-T
labels:
    - security
    - test
    - elt-staging
    - rejected
    - superseded
parent_id: 073-F
priority: medium
references:
    - docs/plans/2026-09-14-elt-staging-credential-redaction-plan.md
status: blocked
title: Add tests for path-embedded secret redaction grammar
updated_at: 2026-09-16T02:59:10.2446974Z
---

**RETIRED — OPERATOR DECISION 2026-09-15 (064-S/073-F): REJECTED, NOT EXECUTABLE.** Path-embedded secret redaction is REJECTED as out of scope: Docline redacts ONLY structured access credentials (URL userinfo + recognized credential query params + typed secret config fields) and MUST NOT scan/rewrite source-document content or redact ordinary URL paths (that is an upstream DLP concern). This task is removed from executable shipment 064-S (status=blocked via shipment return-blocked) and retained NON-DESTRUCTIVELY for traceability only. The grammar described below is HISTORICAL and MUST NOT be implemented.

Stream C / Unit C1 (test domain, test-first). Add parametrized tests for the H2 + H2-C2 path-embedded secret grammar (see plan Unit C1 + deliberation H2 and Remediation cycle 2 / H2-C2) against _sanitize_url / sanitize_source with EXACT expected outputs. Cycle-1 (H2): /token/SECRET -> /token/<redacted>; /token=SECRET -> /token=<redacted>; case-insensitive /Token/SECRET -> /Token/<redacted>; percent-encoded /%74oken/SECRET -> /%74oken/<redacted>; new-marker /client_secret/hunter2 -> /client_secret/<redacted> (proves shared vocabulary from B2); interior /a/token/SECRET/b -> /a/token/<redacted>/b; benign /api/v1/data and /codec/data unchanged; trailing /token (no following value) unchanged. Cycle-2 decode-before-segmentation (H2-C2, finding #1): encoded-slash /token%2FSECRET -> /token%2F<redacted> (and lowercase /token%2fSECRET -> /token%2f<redacted>); encoded-equals /token%3DSECRET -> /token%3D<redacted>; nested /%74oken%2FSECRET -> /%74oken%2F<redacted>; multilayer /token%252FSECRET -> /token%252F<redacted>. Fail-closed (H2-C2): first-layer malformed escape /token%2/SECRET and /%ZZ/SECRET -> <path-redacted>; NESTED malformed escapes revealed only AFTER an iterative decode layer (cycle-3 finding F-02): /token/%252 -> <path-redacted> (`%252` decodes one layer to `%2`, a truncated escape) and /token/%25ZZ -> <path-redacted> (`%25ZZ` decodes one layer to `%ZZ`), proving percent escapes are validated AFTER EVERY decode layer, not only before the first decode; over-cap (>5 decode layers still transforming) -> <path-redacted>; unparseable/malformed URL -> <path-redacted>. /token/ trailing-empty (findings #1/#9): /token/ -> /token/ unchanged (no non-empty adjacent value); /token//SECRET -> /token//<redacted> (first non-empty value redacted). Acceptance criteria: (1) parametrized cases (incl. all H2-C2 encoded-delimiter, multilayer, malformed, over-cap cases, and the nested per-layer malformed cases %252/%25ZZ) FAIL on current HEAD (path secrets currently survive); (2) benign path cases (/api/v1/data, /codec/data, /token/, trailing /token) assert NON-redaction; (3) fail-closed cases (including the nested per-layer malformed cases %252/%25ZZ that only surface after an iterative decode layer) assert the <path-redacted> sentinel; (4) no production code changed. Execution posture: test-first. Files: one test file under tests/.

<!-- BEGIN:sizing -->
Size: S | Complexity: medium
<!-- END:sizing -->