---
artifact_type: task
created_at: 2026-08-27T07:45:56.6844944Z
id: 064.001-T
labels:
    - mcp
    - stdio
    - tests
    - test-first
    - security
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Author stdio MCP protocol handshake + discovery/parity harness (red)
updated_at: 2026-08-27T08:26:45.080748Z
---

Author the failing protocol handshake + discovery/parity test harness for the stdio MCP server in tests/parity/test_mcp_stdio.py (test-first / red). Tests domain only. First of four dependency-linked harness tasks split out of the original oversized T1 to honor the <4-scenario/2-hour granularity heuristic. Covers only the JSON-RPC initialize handshake and semantic tools/list discovery parity. Dispatch/error/notification coverage moves to 064.005-T; security gates H1-H3 to 064.006-T and H4-H6 to 064.007-T. See docs/plans/2026-08-27-mcp-stdio-server-plan.md (T1).

<!-- BEGIN:acceptance-criteria -->
- initialize returns pinned protocolVersion + capabilities + serverInfo [green@064.002-T transport].
- Instrumented serve() interactive-liveness assertion (cycle-16 round-2 — comment 3885888257; STRENGTHENS the transport handshake+discovery flow IN-PLACE, no new scenario): drive `serve(stdin, stdout, server)` in-process with an instrumented NON-GREEDY stdin fake and a flush-recording stdout fake over the SAME initialize -> tools/list frames this task already sends. The stdin fake exposes `read1()` as the injectable non-greedy primitive and WITHHOLDS the second frame (tools/list) until it observes the first frame's (initialize) response was FLUSHED to the stdout fake; a prohibited greedy `read(CHUNK_SIZE)` path on the fake fails/records so it cannot satisfy the driver (a greedy read cannot false-green). Assert: (i) serve() is actually invoked and dispatches the first short frame WITHOUT reaching EOF and WITHOUT blocking to fill a whole CHUNK_SIZE; (ii) the first response bytes are not observable until serve() calls `flush()`; (iii) the second input frame becomes available only AFTER that flush is observed, and its response is likewise flushed; (iv) EVERY response frame - including an error envelope - is flushed. Each blocking wait is bounded by a timeout and teardown cannot leave a blocked in-process worker after a failure (a deadlock FAILS deterministically, never hangs the suite). This is the IN-PROCESS red predecessor for serve()'s non-greedy-read + per-frame-flush liveness properties (RED here: serve() absent; green@064.002-T); the live subprocess/packaging smoke stays in 064.008-T.
- tools/list is derived from the adapter method DoclineMcpServer.list_callable_tools(); semantic parity (name + normalized schema, not byte-for-byte) vs the callable-filtered get_manifest().tools, normalizing the parameters<->inputSchema alias, the H1 workspace_root omission, AND the ingest_local_dir exclusion (compare against the callable allow-list, not the raw 4-tool manifest) [adapter-level assertion green@064.015-T; transport tools/list green@064.002-T].
- The legacy adapter method SERVER.list_tools() is asserted UNCHANGED: it still exposes the full four-tool shared manifest, so the existing tests/parity/test_manifest_parity.py::test_mcp_server_list_tools_exposes_shared_manifest stays green with no edit [green@064.015-T].
- §H8 cross-reference: this semantic parity normalization is EXTENDED by 064.031-T to additionally treat the external-PDF-engine omission (mistral_ocr, gated by the server opt-in) as a THIRD sanctioned MCP-only divergence once 064.032-T lands; no change is needed in THIS task's own red/green (at 064.015-T the default advertise still carries the full engine enum).
- Adapter invariant: the transport advertises ONLY the callable set (list_callable_tools()), never list_tools(); every advertised MCP tool is dispatchable via call_tool (no advertise-but-uncallable gap) [green@064.015-T].
- All new tests observed FAILING before implementation (red).
- Scenario budget: 3 (handshake [strengthened IN-PLACE with the instrumented serve() interactive-liveness assertion over the same initialize/tools/list frames], callable discovery/parity incl. list_tools()-unchanged + invariant, dispatchability) - within the <4 heuristic. The liveness assertion is an in-place strengthening of the transport handshake+discovery flow (no new scenario, same frames), so the scenario count stays 3 and the task stays tests-only / width-compliant.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: test-first (write, verify red). Reuse existing patterns in tests/parity/. Do not implement production code in this task (width isolation). Milestone model: adapter-level assertions (list_callable_tools parity, dispatchability, list_tools-unchanged, invariant) are greened by the adapter task 064.015-T; the transport-level assertions (initialize, tools/list frame) AND the instrumented serve() interactive-liveness assertion are greened by the transport task 064.002-T. The liveness driver injects fake stdin/stdout streams and therefore exercises serve()'s stream-level non-greedy primitive (`read1`) + `flush()` in-process (RED here because serve() does not yet exist); the real-fd end-to-end path is covered by the 064.008-T live subprocess smoke, so the two are complementary, not duplicative. Because 064.001-T is the first harness task (no dependencies) and 064.002-T is make-green, the liveness property is observed RED well before it is implemented, preserving the non-negotiable test-first order. Downstream: 064.005-T (dispatch/error incl. -32600), 064.006-T (H1-H3), 064.007-T (H4-H6 literal), 064.010-T/064.012-T (shared-fetch SSRF/caps), 064.014-T (MCP boundary end-to-end), 064.015-T (adapter surface).
<!-- END:implementation-notes -->