---
artifact_type: task
created_at: 2026-08-27T07:45:56.6844944Z
id: 064.001-T
labels:
    - mcp
    - stdio
    - tests
    - test-first
    - security
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Author stdio MCP protocol handshake + discovery/parity harness (red)
updated_at: 2026-08-27T08:26:45.080748Z
---

Author the failing protocol handshake + discovery/parity test harness for the stdio MCP server in tests/parity/test_mcp_stdio.py (test-first / red). Tests domain only. First of four dependency-linked harness tasks split out of the original oversized T1 to honor the <4-scenario/2-hour granularity heuristic. Covers only the JSON-RPC initialize handshake and semantic tools/list discovery parity. Dispatch/error/notification coverage moves to 064.005-T; security gates H1-H3 to 064.006-T and H4-H6 to 064.007-T. See docs/plans/2026-08-27-mcp-stdio-server-plan.md (T1).

<!-- BEGIN:acceptance-criteria -->
- initialize returns pinned protocolVersion + capabilities + serverInfo.
- tools/list == callable allow-list; semantic parity (name + normalized schema, not byte-for-byte) vs the callable-filtered get_manifest().tools, normalizing the parameters<->inputSchema alias, the H1 workspace_root omission, AND the ingest_local_dir exclusion (compare against the callable allow-list, not the raw 4-tool manifest).
- Every advertised MCP tool is dispatchable (no advertise-but-uncallable gap).
- All new tests observed FAILING before implementation (red).
- Scenario budget: 3 (handshake, discovery/parity, dispatchability) - within the <4 heuristic.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: test-first (write, verify red). Reuse existing patterns in tests/parity/. Do not implement production code in this task (width isolation). Turned fully green by the core impl 064.002-T (which builds tools/list from the allow-list: ingest_local_dir excluded, process inputSchema omits workspace_root). Downstream harness tasks: 064.005-T (dispatch/error/notification), 064.006-T (H1-H3), 064.007-T (H4-H6).
<!-- END:implementation-notes -->