---
artifact_type: task
created_at: 2026-08-27T07:45:59.4444393Z
dependencies:
    - 064.007-T
id: 064.002-T
labels:
    - mcp
    - stdio
    - code
    - security
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Implement core stdio JSON-RPC dispatch loop + call_tool allow-list
updated_at: 2026-08-27T08:26:49.09229Z
---

Implement the CORE stdio JSON-RPC dispatch loop (src/docline/mcp/stdio.py) and the manifest-driven DoclineMcpServer.call_tool allow-list to make the protocol/discovery/dispatch/error/notification harnesses (064.001-T + 064.005-T) pass green. Code domain only. Security hardening H1-H6 is split into its own successor task 064.009-T to honor the 2-hour/<5-function rule (the make-green work was too concentrated for one task). Depends on 064.007-T (the full red harness is authored first). See docs/plans/2026-08-27-mcp-stdio-server-plan.md (T2, Design).

<!-- BEGIN:acceptance-criteria -->
- DoclineMcpServer.call_tool(name, arguments) uses a static {name: bound_method} allow-list (no getattr); MCP tools/list derived from that same allow-list (advertised == callable), so ingest_local_dir is EXCLUDED.
- The per-tool inputSchema is built from the allow-list at BUILD TIME: the process inputSchema OMITS workspace_root (the runtime reject of a client-supplied value is 064.009-T's H1).
- src/docline/mcp/stdio.py provides pure dispatch(message, server) and serve(stdin, stdout, server=SERVER) reserving real stdout for frames; clean EOF exit.
- Frame reads are bounded at the byte level: fixed-chunk binary read up to MAX_FRAME_BYTES with bounded draining of an oversized/unterminated frame - NOT a post-read length check on an unbounded readline(). (The H2 acceptance test lives in 064.006-T; the bounded-read helper is core scaffolding delivered here.)
- initialize / tools/list / tools/call / ping / id-less notification handled per plan; success=False -> isError result mapping.
- 064.001-T (discovery/parity) and 064.005-T harness tests pass FULLY green; existing tests/parity/test_mcp_adapters.py and test_mcp_transport.py unchanged and passing. (Security harnesses 064.006-T/064.007-T remain red until 064.009-T.)
- ruff check ., ruff format --check ., pyright src/ clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green (core protocol/dispatch + build-time tools/list schema) after the four red harness tasks. No new runtime dependency (stdlib json/sys only). Keep transport tool-agnostic - tool identity lives only in the adapter allow-list. Runtime security guardrails H1-H6 (incl. dispatcher-level workspace_root reject) are delivered by successor 064.009-T. Depends on 064.007-T.
<!-- END:implementation-notes -->