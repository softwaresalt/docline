---
artifact_type: task
created_at: 2026-07-16T17:48:04.8295678-07:00
id: 056.010-T
labels:
    - cli
    - observability
    - test-first
parent_id: 056-F
priority: medium
references:
    - docs/plans/2026-07-16-console-verbosity-progress-plan.md
status: queued
title: Add TTY-aware ProgressReporter core module
updated_at: 2026-07-16T18:27:47.3930325-07:00
---

Unit 1. New module (src/docline/progress.py or src/docline/observability/progress.py) with a Verbosity enum, a small ProgressEvent, and a pure TTY-aware ProgressReporter that renders/throttles progress to a stream (default sys.stderr). Test-first. Files: the new module + tests/.../test_progress.py.

<!-- BEGIN:acceptance-criteria -->
- `Verbosity` enum (SILENT/NORMAL/VERBOSE) defined; caller resolves default NORMAL.
- `ProgressReporter.__call__(done: int, total: int, detail: str)` builds a `ProgressEvent` internally (P2-1: library seam emits positional primitives, reporter owns the event type).
- Percentage clamps `done/total` to [0,100]; `total` None/0 → count-only output (no ETA/percent).
- Stream `isatty()` true → carriage-return in-place update; false → newline-terminated line with no control chars.
- Rendering differs by mode (RC-5): NORMAL = throttled concise single percentage/count line (at most every ~1s or every N items); VERBOSE = one line per item with its detail (URL/path) + running percentage, NOT throttled (per-item lines never dropped); SILENT = nothing.
- `finish(detail=None)` emits the final line unconditionally, bypassing the throttle (RC-4): 100% when total known, else the final count — covers early completion where the last per-item update had `done < total`.
- Unit tests cover: percentage math + clamp + None-total; NORMAL throttling coalesces rapid calls; VERBOSE emits one detailed line per item; TTY vs non-TTY formatting; SILENT no-op; `finish()` emits the final line after a `done < total` last update.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: test-first. Deliberation O1/O3; plan Unit 1. Completion is signaled via ProgressReporter.finish() (called by the CLI after the run) since the per-page/per-file callback's last event may have done < total.
<!-- END:implementation-notes -->