---
artifact_type: task
created_at: 2026-07-16T17:48:04.8295678-07:00
id: 056.010-T
labels:
    - cli
    - observability
    - test-first
parent_id: 056-F
priority: medium
references:
    - docs/plans/2026-07-16-console-verbosity-progress-plan.md
status: queued
title: Add TTY-aware ProgressReporter core module
updated_at: 2026-07-16T21:08:54.8921094-07:00
---

Unit 1. New module (src/docline/progress.py or src/docline/observability/progress.py) with a Verbosity enum, a small ProgressEvent, and a pure TTY-aware ProgressReporter that renders/throttles progress to a stream (default sys.stderr). Test-first. Files: the new module + tests/.../test_progress.py.

<!-- BEGIN:acceptance-criteria -->
- `Verbosity` enum (SILENT/NORMAL/VERBOSE) defined; caller resolves default NORMAL.
- `ProgressReporter.__call__(done: int, total: int | None, detail: str)` builds a `ProgressEvent` internally (P2-1). `total: int | None` supports count-only reporting (N6).
- Percentage clamps `done/total` to [0,100]; `total` None → count-only output (no ETA/percent).
- Stream `isatty()` true → carriage-return in-place update; false → newline-terminated line with no control chars.
- Rendering differs by mode (RC-5): NORMAL = throttled concise single percentage/count line; VERBOSE = one line per item with detail (URL/path) + running percentage, NOT throttled (never dropped); SILENT = nothing.
- `finish(detail=None)` emits the final line unconditionally (bypassing throttle) but NEVER forces 100% (RC-4 revised / N4): renders the actual `done` — `done/total` (100% only when `done == total`, e.g. process known total) or a bare count when `total is None`/`done < total` (e.g. fetch ending early at 20 of a 50-page budget). Completion is a separate marker, not a synthetic 100%.
- Unit tests: percentage math + clamp + None-total count-only; NORMAL throttling coalesces; VERBOSE one detailed line per item; TTY vs non-TTY; SILENT no-op; `finish()` shows 100% only when `done == total`, else a bare count (never 100%) when `total is None` or `done < total`.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: test-first. Deliberation O1/O3; plan Unit 1. Completion is signaled via ProgressReporter.finish() (called by the CLI after the run) since the per-page/per-file callback's last event may have done < total.
<!-- END:implementation-notes -->