---
artifact_type: task
created_at: 2026-09-14T07:34:25.8060767Z
id: 073.003-T
labels:
    - security
    - test
    - elt-staging
parent_id: 073-F
priority: medium
references:
    - docs/plans/2026-09-14-elt-staging-credential-redaction-plan.md
status: queued
title: Add tests for expanded credential query-param name matching
updated_at: 2026-09-15T21:20:00.0000000Z
---

Stream B / Unit B1 (test domain, test-first). Add parametrized tests for _is_credential_param / sanitize_source AND the typed source_keys._remove_credential_query_params / _is_credential_name proving the FINAL credential query-param NAME grammar (Finding 1 + Finding 2). These tests gate the MERGED single production task 073.002-T (former stream-B code task 073.004-T is retired/merged into 073.002-T). Path-embedded secret redaction (former stream C / 073.005-T/073.006-T) is REJECTED/retired per the operator contract; not this task and not in executable scope.

EXPLICIT EXACT-MATCH VOCABULARY (Finding 1): the matcher recognizes an explicit, case-insensitive credential-name vocabulary by EXACT equality — the legacy `startswith`/prefix heuristic is REPLACED, not extended. Redacted (case-insensitive EXACT match): the individually-enumerated existing/legacy/cloud names preserved from current behavior (token, access_token, key, api_key, secret, auth, sig, signature, x-amz-credential, x-amz-signature, x-amz-security-token, x-goog-signature), the names previously caught only by the `startswith("auth")` prefix and now individually enumerated to preserve prior coverage (auth_token, authorization), the intended additional legacy/cloud names (refresh_token, apikey, client_secret, x-goog-credential, awsaccesskeyid), PLUS the newly added password, pwd, passwd, code, plus case-varied forms (Passwd/PASSWD/Refresh_Token/REFRESH_TOKEN/AWSAccessKeyId/X-Goog-Credential/etc.). Plan H1 holds the authoritative list.

HONEST COMPLETENESS (Finding 7): the vocabulary is NOT claimed universally complete. Add benign PREFIX-preservation fixtures asserting that names NOT enumerated in the vocabulary are INTENTIONALLY NOT redacted under exact match — including suffixed/prefixed variants of real credential names that the retired `startswith` heuristic used to catch but exact-match deliberately does not (e.g. `token_v2`, `access_token2`, `my_api_key`, `x-amz-credential-v4`) — proving exact-match semantics and recording unknown/variant names as accepted, intentionally-unredacted residual absent explicit product support.

BENIGN PRESERVATION (Finding 1) — these MUST NOT redact under exact match (they were FALSE POSITIVES under the retired startswith heuristic): tokenizer, keynote, secretary, authorship, signal, password_policy, pwd_length, client_secretary, codec, code_version, passwd_file, refresh_token_ttl, and case-varied variants of every one (Tokenizer, KeyNote, Secretary, Authorship, Signal, Password_Policy, PWD_Length, Client_Secretary, Codec, Code_Version, Passwd_File, PASSWD_HASH, Refresh_Token_TTL, REFRESH_TOKEN_EXPIRY).

BOUNDED DECODE — QUERY NAMES ONLY, BOTH PATHS (Finding 2): assert bounded percent-decoding is applied to the query-parameter NAME on BOTH the string sanitizer path and the typed sanitizer path; the vocabulary is re-checked after EACH decode layer, capped at 5 layers, and fails closed (treated as credential) if the name is still transforming past the cap. Cover: `%70assword`→password (1 layer), `%2570assword`→`%70assword`→password (2 layers), a
deeper still-valid layered case that resolves to a vocabulary name within the cap, and an
over-cap case (>5 layers still transforming) that fails closed. EXACT-CAP BENIGN BOUNDARY
(Finding P2): also cover a BENIGN query name whose bounded decoding reaches a STABLE benign value
at the exact 5-layer boundary (decoded == current observed within the cap) and is therefore
PRESERVED — proving the cap admits a benign name that stabilizes right at the boundary — while
RETAINING the over-cap credential-like/benign-looking case that is still transforming past 5
layers and fails closed (redacted, accepted false-positive residual). Assert NO decoding is
applied to URL PATHS or to query-parameter VALUES.

BENIGN QUERY BYTE-PRESERVATION — HELPER LEVEL (Finding P2): assert the string sanitizer's
credential-token removal is SURGICAL and preserves benign query parts BYTE-FOR-BYTE against
parse_qsl/urlencode normalization. Cover representative percent spelling (`%20` stays `%20`, not
`+`; literal `%25` intact), `&`-separated multi-param queries, and blank/valueless params (a
valueless `flag` does not gain `=`; `x=` stays `x=`), asserting only credential-named tokens are
dropped while every benign token's exact bytes survive. This is the helper-level proof that pairs
with the live default-stdout proof in 073.009-T.

TYPED PRE-PASS REGRESSION — 2026-09-24 IN-SCOPE P1: add test-first assertions that
DIRECTLY call `source_keys._strip_reversed_query_credentials` AND
`source_keys._sanitize_url_field` (or `sanitize_source_key(WebCrawlSource(url=...))`),
not only `staging.sanitize_source` or `_remove_credential_query_params`: the
pre-pass is invoked BEFORE staging and currently rewrites benign `;` and
repeated `&` even with zero credentials. Credential-free typed URLs MUST remain
byte-identical for `https://host/p?a=1;b=2`,
`https://host/p?a=1&&b=2`, `?flag&&x=&space=%20&literal=%25`,
and benign fragments with `;`, `&&`, and empty `?`/`#` introducers.
Include a raw-authority no-credential fixture
`https://HOST:080/p?a=1;b=2#note` at both helper and typed URL levels:
the string sanitizer currently rewrites the host/port even if the pre-pass
is fixed. In a separate mixed case, strip `user:pass@` but retain `HOST:080`
exactly. For the FIRST-token rule, pin `?token=SECRET;flag` → `?flag`
and `#token=SECRET&note=x` → `#note=x`, exercising the completed typed
sanitizer as well as the pre-pass.
Assert both helper and typed URL output explicitly so a staging-only fix cannot
make this gate pass; the first two fixtures MUST FAIL on current HEAD at the
pre-pass. In separate MIXED-CREDENTIAL cases, assert `token=SECRET` (including
`?detail=x?token=SECRET` and duplicated `?` boundaries), encoded recognized
names and userinfo are removed, while surviving benign raw spans retain original
percent spelling, `;`, repeated `&`, `flag` and `x=`. Exercise query AND fragment:
e.g. `?a=1;token=SECRET;b=2` → `?a=1;b=2`,
`?a=1&&token=SECRET;b=2` → `?a=1&;b=2` under the
preceding-delimiter rule, and `#note=x?token=SECRET&flag` →
`#note=x&flag`. In mixed cases do NOT claim identity of the entire query:
only the credential token plus ONE immediately adjacent separator is removed
(preceding preferred, following if first), with other separators and empty
tokens retained. If a sole credential token empties a component, its `?`/`#`
introducer may be dropped; untouched empty introducers stay intact. Cover
string sanitizer fragment preservation too; ensure `staging._sanitize_url`
and `source_keys._remove_credential_query_params` cannot erase benign fragments
or reserialize benign fields after the pre-pass. No path/query-value decoding or
document-body assertions.

LOCAL-PATH BYTE-PRESERVATION (Finding P1): assert sanitize_source returns a bare local filesystem
path (absolute POSIX `/home/...`, UNC `\\server\share`, Windows-drive `C:\...`) and a `file://`
input BYTE-FOR-BYTE unchanged — the `<local-path-redacted>` sentinel is GONE (compatibility is
bare URL only; local paths are preserved provenance per the operator contract). These fixtures
FAIL on HEAD (current sanitize_source redacts them to `<local-path-redacted>`) and the
contradicting `<local-path-redacted>` assertions in tests/fetch/test_staging.py and
tests/elt/test_source_keys.py are updated to the byte-preservation contract by 073.002-T.

Acceptance criteria: (1) new-name + case-varied + bounded-decode (1/2/deeper/over-cap) redaction cases FAIL on HEAD (currently unmatched); (2) ALL benign preservation cases (incl. tokenizer/keynote/secretary/authorship/signal and passwd_file/refresh_token_ttl and case-varied variants) assert NON-redaction; (3) the enumerated existing/legacy/cloud vocabulary (incl. the newly-enumerated auth_token/authorization/refresh_token/apikey/client_secret/x-goog-credential/awsaccesskeyid) still redacts by exact match (coverage parity/expansion for real credential names); (4) benign PREFIX-preservation fixtures (`token_v2`, `access_token2`, `my_api_key`, `x-amz-credential-v4`) assert NON-redaction, proving exact-match semantics and the intentionally-unredacted-variant residual (Finding 7); (5) bounded query-name decode is exercised on BOTH the string and typed sanitizer paths, with an EXACT-CAP BENIGN boundary case PRESERVED and an over-cap credential-like/benign-looking case FAIL-CLOSED; (6) no-credential typed `_strip_reversed_query_credentials` and `_sanitize_url_field` are byte-identical for `a=1;b=2`, `a=1&&b=2`, benign fragments and empty/valueless/percent-spelled fields (MUST fail on HEAD for the first two); benign uppercase host/zero-padded port remain byte-identical through string and typed sanitization; mixed query/fragment credentials, including reversed/duplicated `?` and first-token removal, are removed with only one adjacent delimiter and all surviving benign spans byte-preserved as specified above; same fragment/idempotence contract on the string path and `_remove_credential_query_params` — asserted at helper level so staging-only normalization fixes cannot pass; (7) sanitize_source preserves bare local filesystem paths (POSIX/UNC/Windows-drive) and `file://` input BYTE-FOR-BYTE (no `<local-path-redacted>` sentinel), FAILING on HEAD; (8) NO path or query-VALUE decoding, and NO path-embedded-secret assertions (path redaction is REJECTED/retired); (9) no production code changed. Execution posture: test-first.

<!-- BEGIN:sizing -->
Size: S | Complexity: low
<!-- END:sizing -->