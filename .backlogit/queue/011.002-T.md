---
artifact_type: task
created_at: 2026-06-03T13:12:07.3330825-07:00
id: 011.002-T
labels:
    - docs
    - onboarding
    - windows
parent_id: 011-F
priority: medium
references:
    - docs/plans/2026-06-03-docline-ci-workflow-plan.md
    - scripts/pre-commit-markdownlint.ps1
    - scripts/pre-commit-markdownlint.sh
status: active
title: Add `CONTRIBUTING.md` with local gates and Windows pytest noise note
updated_at: 2026-06-03T13:15:18.8922465-07:00
---

Create `CONTRIBUTING.md` at repo root with the following sections (in order):

1. **Local quality gates** — table listing the five gates and their `uv run` commands, mirroring `.github/workflows/ci.yml`.
2. **Running gates with uv** — paragraph on `uv sync --all-extras --dev` and `uv run` so contributors don't need globally installed ruff or pyright.
3. **Known Windows local-dev noise** — describes the `tmp_path` `PermissionError` lines emitted during `pytest` teardown on Windows (176+ entries per run), notes they do not affect pass/fail, gives filter examples:
   - PowerShell: `pytest 2>&1 | Select-String -NotMatch 'PermissionError'`
   - bash: `pytest 2>&1 | grep -v 'PermissionError'`
   - Points at CI (Linux) as the authoritative gate.
4. **Pre-PR checklist** — bulleted list reminding contributors to run all five gates locally before opening a PR.

Must cross-reference `.github/workflows/ci.yml` for authoritative gate definitions. Must pass the workspace markdownlint configuration (`scripts/pre-commit-markdownlint.ps1` / `.sh`).

This task bundles the Windows pytest noise documentation (`CE758832`) into the CI shipment per the deliberation. Commit scope: `docs`.

<!-- BEGIN:AcceptanceCriteria -->
- `CONTRIBUTING.md` exists at repo root
- Contains all four required sections in the order specified
- Local-gate table mirrors `.github/workflows/ci.yml` gate commands exactly
- Windows `tmp_path` PermissionError filter examples for both PowerShell and bash are accurate
- Cross-references `.github/workflows/ci.yml` for authoritative gate definitions
- Passes the workspace markdownlint configuration
<!-- END:AcceptanceCriteria -->