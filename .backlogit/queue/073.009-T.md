---
artifact_type: task
created_at: 2026-09-16T03:37:28.7668796Z
id: 073.009-T
labels:
    - security
    - test
    - integration
    - elt-staging
parent_id: 073-F
priority: high
references:
    - docs/plans/2026-09-14-elt-staging-credential-redaction-plan.md
status: queued
title: Integration test - live default-path stdout proves A2+B2 credential composition
updated_at: 2026-09-16T03:37:28.7668796Z
---

Stream A+B / Final composition gate (test domain, characterization-first). The single live default-path STDOUT integration test that proves the A2+B2 COMPOSITION (Finding 5). Invoke the LIVE `docline fetch` default (non-`--execute`) CLI entrypoint (project CLI test runner or subprocess) over a credentialed config of each URL-bearing source kind (WebCrawlSource, ManifestUrlSource, GitHubRepoSource, ManifestGitSource) whose URL carries BOTH URL userinfo AND each recognized NEW credential query-param name (password, pwd, passwd, client_secret, refresh_token, code) alongside a benign query param and an ordinary path. Assert the REAL stdout JSON at cli.py:381 contains NO userinfo and NONE of the new credential query-param values for all four kinds, while the benign query param, the ordinary path (byte-for-byte, incl. literal `%25`), and non-credential provenance are preserved.

This is the AUTHORITATIVE live-sink proof that userinfo redaction (A2) and the expanded exact-match credential-name vocabulary (B2) compose correctly at the real default-path stdout emission point. Written test-first (leaf, no upstream deps); it blocks BOTH 073.002-T (A2) and 073.004-T (B2).

SUBSET / FINAL-GATE HONESTY (Finding 5): on HEAD this test FAILS. After 073.002-T (A2) the userinfo assertions pass but the new-credential-name assertions remain red. After 073.004-T (B2) the new-credential-name assertions pass. FULL green requires BOTH A2 and B2 — this is the final composition gate, and neither code task may be marked done claiming full default-stdout coverage on its own.

Acceptance criteria: (1) test invokes the real CLI fetch entrypoint and captures actual stdout for all four URL-bearing source kinds; (2) FAILS on HEAD (userinfo AND new-name leak); (3) userinfo assertions green after 073.002-T; (4) new-credential-name assertions green after 073.004-T; (5) full test green ONLY after BOTH A2 and B2 land; (6) benign query param + ordinary URL path (byte-for-byte, literal `%25` intact) preserved — no path decoding/redaction; (7) no path-secret / document-content assertion; (8) no production code changed. Execution posture: characterization-first / test-first. Files: one test file under tests/.

<!-- BEGIN:sizing -->
Size: S | Complexity: low
<!-- END:sizing -->