---
artifact_type: task
created_at: 2026-06-04T00:03:40.9303988-07:00
id: 014.001-T
labels:
    - processing
    - referentiality
    - tdd-red
    - stage-harvested
    - shipment-13
parent_id: 014-F
priority: medium
references:
    - docs/plans/2026-06-04-G3b-referentiality-plan.md
status: queued
title: Write failing tests for referentiality frontmatter + chunk anchor default (TDD RED)
updated_at: 2026-06-04T00:03:40.9303988-07:00
---

Create `tests/process/test_referentiality.py` with all 10 referentiality scenarios + 3 helper scenarios from the plan's Test plan table. Also add an `extract_section_title` test scenario set inside `tests/process/test_segment.py`. Verify every test fails (RED) before any implementation lands in 014.002-T.

<!-- BEGIN:AcceptanceCriteria -->
- `tests/process/test_referentiality.py` exists with all 10 scenarios named per the plan
- `tests/process/test_segment.py` gains 3 tests for `extract_section_title`
- All new tests fail (collection-error or assertion-error) — no green tests
- Each test has a Google-style docstring naming the scenario
- Tests use the existing `_write_staging_job` + `_make_pdf_bytes_multi_page` + `_make_minimal_docx` helpers from `tests/elt/test_process_regression.py` where helpful (move helpers to a shared `tests/conftest.py` only if duplication grows; otherwise import-from-test or duplicate inline)
<!-- END:AcceptanceCriteria -->

<!-- BEGIN:ImplementationNotes -->
Mirror the table in `docs/plans/2026-06-04-G3b-referentiality-plan.md` § Test plan. For multi-part scenarios, use the `_make_pdf_bytes_multi_page` helper with H1-prefixed text streams (e.g. `# Chapter A`, `# Chapter B`, `# Chapter C`) per the 012-S regression fixture pattern. For the `parent_document_id` shared-across-parts assertion, load all part frontmatter blocks via yaml.safe_load and assert all entries[*].docline.parent_document_id are equal.
<!-- END:ImplementationNotes -->