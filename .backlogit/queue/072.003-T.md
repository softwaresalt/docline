---
artifact_type: task
created_at: 2026-09-12T20:07:26.6288103Z
dependencies:
    - 072.002-T
id: 072.003-T
labels:
    - security
    - elt
    - code
parent_id: 072-F
priority: high
references:
    - docs/plans/2026-09-12-source-key-credential-sanitization-plan.md
status: queued
title: Wire _execute_single_source metadata + ERROR-log sinks through sanitizer (GREEN)
updated_at: 2026-09-12T21:55:30.0000000Z
---

PLAN Unit 3 (code; GREEN). In _execute_single_source (src/docline/elt/execute.py), set metadata source to sanitize_source_key(config) and change the _log.exception argument from source_key to sanitize_source_key(config) (R4 typed-config contract; id + url sanitized; config is in scope alongside source_key = build_source_key(config)). KEEP job_id = make_job_id(source_key) on the RAW key (determinism invariant). If Unit 2's caplog.text assertion reveals the exc_info traceback re-leaks the raw URL (crawl exception embedding config.url), close it within this same ERROR sink by SCRUBBING the raw URL from the logged exception (sanitize/wrap the exception message); do NOT drop exc_info (Unit 2 asserts exc_info is not None, so the traceback must remain present but credential-free). Bounded to this file/ERROR path (D6E758F5 scope). Files: src/docline/elt/execute.py. Size: XS | Complexity: medium. Depends on 072.002-T. ACCEPTANCE CRITERIA: (1) metadata.source and ERROR log both routed through sanitize_source_key; (2) make_job_id still receives the raw source_key (job_id unchanged vs pre-fix for identical config); (3) Unit 1+2 tests green; (4) existing test_web_crawl_orchestrate_and_execute_share_job_key stays green.