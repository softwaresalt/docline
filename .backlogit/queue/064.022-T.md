---
artifact_type: task
created_at: 2026-08-27T15:09:31.9157622Z
dependencies:
    - 064.021-T
id: 064.022-T
labels:
    - mcp
    - protocol
    - dual-era
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Implement modern-era server/discover and _meta negotiation
updated_at: 2026-08-27T23:55:00.0000000Z
---

Make 064.020-T green. Code domain, <=2 files (src/docline/mcp/stdio.py + src/docline/mcp/server.py). Implement MCP 2026-07-28 modern-era negotiation AND the MODERN branch of the request-shape era classifier (detect per-request _meta -> route to modern handlers, stateless, no initialize). server/discover dispatch backed by the describe_server() accessor introduced by 064.015 (consume, do NOT re-introduce); per-request _meta metadata extraction + validation of BOTH io.modelcontextprotocol/protocolVersion AND io.modelcontextprotocol/clientCapabilities (version checked FIRST -> unsupported returns -32022; clientCapabilities validated only after version is accepted -> missing/malformed returns -32602 Invalid Params); modern result shape (resultType:'complete', serverInfo _meta, list ttlMs/cacheScope; the DiscoverResult itself also carries ttlMs/cacheScope as a CacheableResult). GUARD FUNNEL (blocking, cycle-4 arch/security remediation): the modern branch MUST route every tools/call/process through the SAME hardened dispatch path (H1/H3/H4/H5 from 064.009) as the legacy path, applying the modern envelope only as a post-return wrapper - guards-by-construction, never a parallel unguarded handler - so the stateless pre-handshake modern surface cannot re-open the H1 workspace_root P0 in the 064.022->064.023 build window. Successor 064.023-T then owns the LEGACY branch + no-drift verification (it VERIFIES guard parity rather than wiring it for the first time). See plan Protocol Era Model / cycle-3 + cycle-4 remediation.

<!-- BEGIN:acceptance-criteria -->
- server/discover dispatch returns a DiscoverResult sourced from the single DoclineMcpServer.describe_server() accessor (introduced by 064.015: supportedVersions for both eras, capabilities, serverInfo), resultType:'complete', AND cache metadata ttlMs + cacheScope (DiscoverResult is a CacheableResult per the 2026-07-28 schema).
- Modern branch of the era classifier: a request carrying modern _meta is detected and routed to modern handlers and served statelessly with NO prior initialize.
- Per-request _meta validation covers BOTH required members with pinned codes and ordering: io.modelcontextprotocol/protocolVersion is checked first (unsupported -> -32022 with data.supported (list) + data.requested); io.modelcontextprotocol/clientCapabilities is validated only after version acceptance (missing or malformed -> -32602 Invalid Params, since it is a _meta-carried params member).
- Guard funnel: the modern branch funnels tools/call/process through the SAME hardened dispatch that enforces H1 (workspace_root reject -32602), H3 (error non-disclosure), H4 (closed allow-list), H5 (stdout hygiene); the modern envelope (resultType/serverInfo/ttlMs/cacheScope) is applied only as a post-return wrapper. This greens the modern-path guard-parity scenario (064.021-T scenario c) HERE, so no build state exposes an unguarded modern path.
- Modern results carry resultType:'complete' and serverInfo in _meta; tools/list modern results carry ttlMs + cacheScope.
- 064.020-T harness passes green (discover shape incl. ttlMs/cacheScope, modern stateless serve with protocolVersion+clientCapabilities, -32022 for bad version + -32602 for bad clientCapabilities).
- ruff check ., ruff format --check ., pyright src/ clean; typed errors only.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green after 064.021-T (last dual-era harness authored first, mirroring the plan's harness-first ordering). Ownership boundary (cycle-3 arch remediation, refined cycle-4): THIS task owns server/discover + _meta negotiation (both members) + -32022/-32602 + the MODERN branch of the classifier routed through the shared hardened dispatch (so 064.020 scenario b AND 064.021 scenario c green here); 064.023 owns the LEGACY branch + no-drift VERIFICATION. describe_server() already exists (064.015) so there is no second identity source. Error code -32022 is in the MCP-reserved -32020..-32099 range per changelog.mdx #12. Function/width budget (2-hour/<5-function guardrail, <=2 files): (1) era classifier modern-branch detect+route, (2) server/discover dispatch (reads describe_server()), (3) _meta dual-member validator (version -> -32022, clientCapabilities -> -32602), (4) modern result-shape wrapper (resultType/serverInfo/ttlMs/cacheScope). SPLIT GUARD (mandatory if exceeded): if the dual-member validator + the modern result-shape wrapper cannot fit the 2-hour/<5-function envelope, split the modern result-shape wrapper into a successor impl task BEFORE implementing, moving the relevant 064.020-T result-shape sub-assertion green-ownership with it. Width-isolated: code domain, <=2 files. Depends on 064.021-T.
<!-- END:implementation-notes -->