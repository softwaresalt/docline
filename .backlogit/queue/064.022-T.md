---
artifact_type: task
created_at: 2026-08-27T15:09:31.9157622Z
dependencies:
    - 064.021-T
id: 064.022-T
labels:
    - mcp
    - protocol
    - dual-era
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Implement modern-era server/discover and _meta negotiation
updated_at: 2026-08-29T06:30:00.0000000Z
---

Make 064.020-T green. Code domain, <=2 files (src/docline/mcp/stdio.py + src/docline/mcp/server.py). Implement MCP 2026-07-28 modern-era negotiation AND the MODERN branch of the request-shape era classifier (detect the namespaced io.modelcontextprotocol/protocolVersion _meta member - by KEY MEMBERSHIP, not truthiness; equivalently io.modelcontextprotocol/clientCapabilities - plus the modern-only server/discover rule -> route to modern handlers, stateless, no initialize; a request carrying ONLY ancillary _meta such as _meta.progressToken with NO modern negotiation member is NOT modern and is left for the legacy branch / pre-init reject owned by 064.023 - cycle-16 round-1). server/discover dispatch backed by the describe_server() accessor introduced by 064.015 (consume, do NOT re-introduce), and - cycle-11 - server/discover is itself routed through the per-request _meta dual-member validator (it is modern-only and NOT exempt): only after BOTH members validate does it return the DiscoverResult. Per-request _meta metadata extraction + validation of BOTH io.modelcontextprotocol/protocolVersion AND io.modelcontextprotocol/clientCapabilities (version checked FIRST -> unsupported returns -32022; clientCapabilities validated only after version is accepted -> missing/malformed returns -32602 Invalid Params) applies to server/discover identically to tools/list/tools/call; modern result shape (resultType:'complete', serverInfo _meta, list ttlMs/cacheScope; the DiscoverResult itself also carries ttlMs/cacheScope as a CacheableResult). GUARD FUNNEL (blocking, cycle-4 arch/security remediation): the modern branch MUST route every tools/call/process through the SAME hardened dispatch path (H1/H3/H4/H5 from 064.009) as the legacy path, applying the modern envelope only as a post-return wrapper - guards-by-construction, never a parallel unguarded handler - so the stateless pre-handshake modern surface cannot re-open the H1 workspace_root P0 in the 064.022->064.023 build window. Successor 064.023-T then owns the LEGACY branch + no-drift verification (it VERIFIES guard parity rather than wiring it for the first time). See plan Protocol Era Model / cycle-3 + cycle-4 + cycle-11 remediation.

<!-- BEGIN:acceptance-criteria -->
- server/discover dispatch is itself routed through the per-request _meta dual-member validator BEFORE returning a result (cycle-11): a valid server/discover MUST carry BOTH io.modelcontextprotocol/protocolVersion (unsupported -> -32022 with data.supported + data.requested) AND io.modelcontextprotocol/clientCapabilities (missing/malformed -> -32602, checked after version), even though it needs no prior initialize (pre-handshake availability does NOT waive required _meta); only a validated server/discover returns a DiscoverResult sourced from the single DoclineMcpServer.describe_server() accessor (introduced by 064.015: supportedVersions for both eras, capabilities, serverInfo), resultType:'complete', AND cache metadata ttlMs + cacheScope (DiscoverResult is a CacheableResult per the 2026-07-28 schema).
- Modern branch of the era classifier: a request whose _meta carries a namespaced modern negotiation member (io.modelcontextprotocol/protocolVersion, equivalently io.modelcontextprotocol/clientCapabilities, by KEY MEMBERSHIP not truthiness) - and server/discover, which is modern-only - is detected and routed to modern handlers and served statelessly with NO prior initialize (and this modern classification WINS EVEN AFTER a legacy latch is set: a protocolVersion-bearing request following initialize is still served modern); a request carrying ONLY ancillary _meta (e.g. _meta.progressToken) with NO modern negotiation member is NOT classified modern (so a retained 2025-11-25 client's ancillary _meta is never misrouted to the modern validator and rejected - it is left to the legacy branch / pre-init reject owned by 064.023); a server/discover (or any modern method) arriving without valid _meta is rejected by the _meta validator (-32602 / -32022), never served as a bare pre-handshake call (cycle-11). This delivers the protocolVersion-keyed discriminator that greens the 064.021-T scenario (b) retained-legacy ancillary-_meta-stays-legacy and modern-wins-after-latch rows by NOT classifying ancillary _meta as modern (the latch-dependent assertions fully green once 064.023 lands the legacy latch - cycle-16 round-1).
- Per-request _meta validation covers BOTH required members with pinned codes and ordering: io.modelcontextprotocol/protocolVersion is checked first (unsupported -> -32022 with data.supported (list) + data.requested); io.modelcontextprotocol/clientCapabilities is validated only after version acceptance (missing or malformed -> -32602 Invalid Params, since it is a _meta-carried params member).
- Guard funnel: the modern branch funnels tools/call/process through the SAME hardened dispatch that enforces H1 (workspace_root reject -32602), H3 (error non-disclosure), H4 (closed allow-list), H5 (stdout hygiene); the modern envelope (resultType/serverInfo/ttlMs/cacheScope) is applied only as a post-return wrapper AROUND the SAME CallToolResult body produced by the shared result-shaping step (064.002): result.content stays a standards-valid ContentBlock[] (isError=true on failure, structuredContent mirrored when applicable) and is NOT re-serialized differently for the modern era. This greens the modern-path guard-parity AND CallToolResult wire-shape scenario (064.021-T scenario c) HERE, so no build state exposes an unguarded or raw-serialized modern path.
- Request-shape precedence (cycle-9): the shared hardened dispatch performs JSON-RPC request-shape validation - including MCP 2026-07-28 RequestId id-type validation (object/array/bool/null id -> -32600, id:null, never echoed) - BEFORE the modern branch extracts _meta, negotiates protocolVersion (-32022), validates clientCapabilities (-32602), routes the era, or dispatches the method (-32601). A malformed id short-circuits to -32600 for a modern (_meta-bearing) request identically to the legacy path and never surfaces as -32022/-32602/-32601 or a wrapped modern result. This greens the modern malformed-id row of 064.021-T scenario (c). No new function - an ordering constraint on the shared dispatch the modern branch funnels through (the modern-branch detect/route step must run after the shared request-shape guard).
- Modern results carry resultType:'complete' and serverInfo in _meta; tools/list modern results carry ttlMs + cacheScope.
- 064.020-T harness passes green (discover shape incl. ttlMs/cacheScope, modern stateless serve with protocolVersion+clientCapabilities, -32022 for bad version + -32602 for bad clientCapabilities, INCLUDING server/discover routed through the _meta validator - cycle-11).
- ruff check ., ruff format --check ., pyright src/ clean; typed errors only.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green after 064.021-T (last dual-era harness authored first, mirroring the plan's harness-first ordering). Ownership boundary (cycle-3 arch remediation, refined cycle-4): THIS task owns server/discover + _meta negotiation (both members) + -32022/-32602 + the MODERN branch of the classifier routed through the shared hardened dispatch (so 064.020 scenario b AND 064.021 scenario c green here); 064.023 owns the LEGACY branch + no-drift VERIFICATION. describe_server() already exists (064.015) so there is no second identity source. Error code -32022 is in the MCP-reserved -32020..-32099 range per changelog.mdx #12. Function/width budget (2-hour/<5-function guardrail, <=2 files): (1) era classifier modern-branch detect+route (keyed on the namespaced io.modelcontextprotocol/protocolVersion member - or server/discover - by key membership, NOT any per-request _meta presence, so ancillary legacy _meta is not misrouted to modern; a same-function refinement, no width change - cycle-16 round-1), (2) server/discover dispatch (reads describe_server()), (3) _meta dual-member validator (version -> -32022, clientCapabilities -> -32602), (4) modern result-shape wrapper (resultType/serverInfo/ttlMs/cacheScope). Cycle-11: server/discover dispatch (2) runs only AFTER the _meta dual-member validator (3) accepts both members (server/discover is modern-only and NOT exempt from _meta validation) - an ordering constraint, no new function, budget unaffected. SPLIT GUARD (mandatory if exceeded): if the dual-member validator + the modern result-shape wrapper cannot fit the 2-hour/<5-function envelope, split the modern result-shape wrapper into a successor impl task BEFORE implementing, moving the relevant 064.020-T result-shape sub-assertion green-ownership with it. Width-isolated: code domain, <=2 files. Request-shape parity (cycle-9): the modern branch adds NO id-validation of its own - it funnels through the SAME hardened dispatch whose request-shape guard already enforces MCP 2026-07-28 RequestId id-type validation (reject object/array/bool/null id with -32600 + id:null, never echoed) BEFORE _meta extraction / version negotiation / era routing / method dispatch; the modern envelope wraps only well-formed requests. This greens the modern malformed-id row of 064.021-T scenario (c). No new function (an ordering constraint on the shared dispatch); the <5-function/<=2-file budget is unaffected. Depends on 064.021-T.
<!-- END:implementation-notes -->