---
artifact_type: task
created_at: 2026-07-16T17:48:06.6895413-07:00
id: 056.012-T
labels:
    - process
    - mcp
    - test-first
parent_id: 056-F
priority: medium
references:
    - docs/plans/2026-07-16-console-verbosity-progress-plan.md
status: queued
title: Add per-file progress callback to execute_process
updated_at: 2026-07-16T23:39:13.9405931-07:00
---

Unit 3. Add optional `progress` (default None) to execute_process (app.py). Compute a GLOBAL total = sum of len(_ordered_staged_files(...)) across ALL completed jobs up front, then invoke progress(cumulative_files_done, global_total, detail) after each file — detail carries job identity/phase — so progress is monotonic across job boundaries (no per-job reset). No printing. Files: app.py + tests/test_execute_process_progress.py.

<!-- BEGIN:acceptance-criteria -->
- `execute_process` accepts optional keyword `progress: Callable[[int, int | None, str], None] | None = None`.
- Process-schema isolation (P2-2): `progress` is a standalone function parameter and is NOT added to the `ProcessRequest` Pydantic model, so the MCP manifest and terminal JSON result contract are unchanged. Regression test asserts `--manifest` output and the result JSON are unchanged.
- Global total (N7/N8): sum `len(_ordered_staged_files(...))` across ALL completed jobs up front; invoke `progress(cumulative_files_done, global_total, detail)` after each file so progress is monotonic across job boundaries (no per-job reset/regression).
- `detail` carries job identity/phase (e.g. "job 2/3: <rel_path>") so VERBOSE output is unambiguous.
- Callback invoked once per staged file with correct running cumulative `files_done`/`global_total` across a multi-job run.
- `progress=None` preserves current behavior (characterization).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: test-first. Global total across ALL completed jobs (cumulative files_done/global_total) for monotonic progress; job identity/phase in the detail (N7/N8). progress stays OUTSIDE ProcessRequest to preserve the MCP schema (P2-2). Plan Unit 3.
<!-- END:implementation-notes -->