---
artifact_type: task
created_at: 2026-07-16T17:48:06.6895413-07:00
id: 056.012-T
labels:
    - process
    - mcp
    - test-first
parent_id: 056-F
priority: medium
references:
    - docs/plans/2026-07-16-console-verbosity-progress-plan.md
status: queued
title: Add per-file progress callback to execute_process
updated_at: 2026-07-16T21:09:10.1633009-07:00
---

Unit 3. Add optional `progress` (default None) to execute_process (app.py); per completed job compute total=len(ordered_files) and invoke progress(files_done, total, rel_path) after each file. No printing. Files: app.py + tests/test_execute_process_progress.py.

<!-- BEGIN:acceptance-criteria -->
- `execute_process` accepts optional keyword `progress: Callable[[int, int | None, str], None] | None = None`; NOT added to the `ProcessRequest` Pydantic model (P2-2); test asserts `--manifest` + terminal JSON unchanged.
- Global total (N7/N8): sum `len(_ordered_staged_files(...))` across ALL completed jobs up front; invoke `progress(cumulative_files_done, global_total, detail)` after each file so progress is monotonic across job boundaries (no per-job reset/regression).
- `detail` carries job identity/phase (e.g. "job 2/3: <rel_path>") so VERBOSE output is unambiguous.
- Callback invoked once per staged file with correct running cumulative `files_done`/`global_total` across a multi-job run.
- `progress=None` preserves current behavior (characterization).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: test-first. Global total across ALL completed jobs (cumulative files_done/global_total) for monotonic progress; job identity/phase in the detail (N7/N8). Plan Unit 3.
<!-- END:implementation-notes -->