---
artifact_type: task
created_at: 2026-08-27T15:09:16.7747317Z
dependencies:
    - 064.020-T
id: 064.021-T
labels:
    - mcp
    - protocol
    - dual-era
    - tests
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Author legacy-era retention and era-routing harness
updated_at: 2026-08-27T23:55:00.0000000Z
---

Author the RED legacy-era retention + era-routing harness (tests/parity/test_mcp_stdio.py). Tests domain only. Proves the legacy initialize handshake still works AND the server routes eras correctly from how the client opens (modern _meta vs initialize). Verifies red until 064.023-T. See plan Protocol Era Model / cycle-3 remediation.

<!-- BEGIN:acceptance-criteria -->
- Scenario (a) [regression anchor - already green from 064.002 legacy base + 064.015 describe_server]: the legacy initialize handshake still returns capabilities + protocolVersion '2025-11-25' + serverInfo; notifications/initialized is silent; ping -> {} (legacy-only). This anchor asserts ONLY the legacy handshake surface (no cross-era comparison), so it is green at authoring and guards against era-routing regressions. The initialize-vs-discover no-drift equality is NOT part of this green anchor (server/discover does not exist until successor 064.022) and lives in scenario (b) as a genuinely-red assertion.
- Scenario (b) [era routing green at 064.023; no-drift sub-assertion green at 064.022]: (i) era routing - a tools/call carrying modern _meta is served under modern semantics with NO prior initialize (modern branch from 064.022), while the same method after initialize is served under legacy semantics (legacy branch from 064.023); AND (ii) no-drift equality - initialize and server/discover report the SAME identity/capabilities/supportedVersions from the single describe_server() source. The no-drift sub-assertion greens at 064.022 (once server/discover first reads describe_server()); the bidirectional era-routing sub-assertion greens at 064.023 (legacy branch); the scenario is fully green at 064.023. Both parts are RED at authoring because neither server/discover nor the legacy routing branch exists yet.
- Scenario (c) [green at 064.022 - guard parity is intrinsic to the modern branch]: modern-path guardrail parity (parametrized) - a modern (_meta, no initialize) tools/call process/fetch enforces H1 (workspace_root reject -32602), H3 (absolute-path sanitization in isError), and H5 (clean stdout) IDENTICALLY to the legacy path, proving the guards apply pre-handshake and era routing cannot branch around them. This greens at 064.022 because 064.022's modern branch funnels tools/call/process through the SAME hardened dispatch (guards-by-construction), not at 064.023 - no build state exposes an unguarded modern path.
- The genuinely-red scenarios (b)/(c) are observed RED at authoring; (a) is a legacy-only green regression anchor. Scenario (c) greens at 064.022 (modern guard funnel); scenario (b) is fully green at 064.023 (legacy branch), with its no-drift sub green at 064.022.
- No production code changed (tests only).
- Scenario budget: 3 (legacy-only green anchor; era-routing + no-drift; modern-path guard parity) - within the <4 heuristic.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: red-only harness authored before 064.023-T. Basis: versioning.mdx 'Backward Compatibility with Initialization-Based Versions' - a dual-era server selects behavior from how the client opens. Scenario (a) is the legacy-only green regression anchor (server/discover does NOT exist until successor 064.022, so the initialize-vs-discover no-drift equality cannot be green here and is placed in scenario (b) as a genuinely-red assertion). Scenario (c) closes the cycle-3 adversarial-review P1: because the modern surface is stateless/pre-handshake, a modern client could re-open the H1 workspace_root P0 if the modern branch skipped the guard - so guard parity is asserted here. Width-isolated: tests domain only. Depends on 064.020-T; greened by 064.023-T.
<!-- END:implementation-notes -->