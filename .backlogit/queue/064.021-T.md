---
artifact_type: task
created_at: 2026-08-27T15:09:16.7747317Z
dependencies:
    - 064.020-T
id: 064.021-T
labels:
    - mcp
    - protocol
    - dual-era
    - tests
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Author legacy-era retention and era-routing harness
updated_at: 2026-08-27T15:10:31.6639766Z
---

Author the RED legacy-era retention + era-routing harness (tests/parity/test_mcp_stdio.py). Tests domain only. Proves the legacy initialize handshake still works AND the server routes eras correctly from how the client opens (modern _meta vs initialize). Verifies red until 064.023-T. See plan Protocol Era Model / cycle-3 remediation.

<!-- BEGIN:acceptance-criteria -->
- Scenario (a) [regression anchor - already green from 064.002 legacy base + 064.015 describe_server]: the legacy initialize handshake still returns capabilities + protocolVersion '2025-11-25' + serverInfo; notifications/initialized is silent; ping -> {} (legacy-only); AND it reports the SAME identity/capabilities as server/discover (single-source describe_server() - no drift). Asserted so era routing cannot regress it.
- Scenario (b) [genuinely RED until 064.023 legacy branch]: era routing - a tools/call carrying modern _meta is served under modern semantics with NO prior initialize (modern branch from 064.022), while the same method after initialize is served under legacy semantics.
- Scenario (c) [genuinely RED until 064.023 guard-parity wiring]: modern-path guardrail parity (parametrized) - a modern (_meta, no initialize) tools/call process/fetch enforces H1 (workspace_root reject -32602), H3 (absolute-path sanitization in isError), and H5 (clean stdout) IDENTICALLY to the legacy path, proving the guards apply pre-handshake and era routing cannot branch around them.
- The genuinely-red scenarios (b)/(c) are observed RED at authoring; (a) is a green regression anchor. Fully greened at 064.023-T.
- No production code changed (tests only).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: red-only harness authored before 064.023-T. Basis: versioning.mdx 'Backward Compatibility with Initialization-Based Versions' - a dual-era server selects behavior from how the client opens. Scenario (c) closes the cycle-3 adversarial-review P1: because the modern surface is stateless/pre-handshake, a modern client could re-open the H1 workspace_root P0 if the modern branch skipped the guard - so guard parity is asserted here. Width-isolated: tests domain only. Depends on 064.020-T; greened by 064.023-T.
<!-- END:implementation-notes -->