---
artifact_type: task
created_at: 2026-07-16T17:48:02.8785701-07:00
id: 056.008-T
labels:
    - fetch
    - crawl
    - test-first
parent_id: 056-F
priority: medium
references:
    - docs/plans/2026-07-16-console-verbosity-progress-plan.md
status: queued
title: Thread optional progress callback into crawl()
updated_at: 2026-07-16T18:28:08.2465587-07:00
---

Unit 2a. Add an optional keyword `progress` callback to crawl() (src/docline/fetch/crawl.py) and invoke it once per processed page inside the BFS loop. Default None = exact current behavior. Test-first. Files: fetch/crawl.py + tests/fetch/test_crawl_progress.py.

<!-- BEGIN:acceptance-criteria -->
- `crawl()` accepts optional keyword `progress: Callable[[int, int, str], None] | None = None`.
- Callback invoked once per processed page with `(page_count, crawl_config.max_pages, current_url)`; counts are monotonic non-decreasing and ≤ max_pages.
- Per-page callback is a progress signal only (RC-4): when the frontier exhausts before `max_pages`, the last event has `done < total` and `crawl` forges no synthetic 100% — final completion is emitted by the CLI via `ProgressReporter.finish()` after `crawl()` returns.
- `progress=None` leaves the returned list of CrawlResult unchanged (characterization test).
- Test covers early frontier exhaustion (frontier empties before max_pages → last event `done < total`).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: test-first. Hook fires inside the BFS loop (per page) so progress tracks the slow network phase. Plan Unit 2a.
<!-- END:implementation-notes -->