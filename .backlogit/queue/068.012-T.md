---
artifact_type: task
created_at: 2026-08-30T05:00:49.7798872Z
dependencies:
    - 068.007-T
id: 068.012-T
labels:
    - crawl
    - frontier
    - observability
parent_id: 068-F
priority: medium
references:
    - docs/plans/2026-08-29-crawl-frontier-observability-plan.md
status: queued
title: A.T9 - Thread truncation into the manifest and the staging job
updated_at: 2026-08-30T05:00:49.7798872Z
---

Domain: src. Files: src/docline/elt/execute.py, src/docline/fetch/models.py. _fetch_url consumes CrawlOutcome and returns (staged_count, frontier_truncated). Outcome unpacking and the results iteration MUST remain inside the existing try: so the except BaseException completion-event path still fires. Manifest becomes {"pages": ..., "frontier_truncated": <real flag>} and is written BEFORE the staged_count == 0 guard per D8 - never a hard-coded true. StagingJob gains frontier_truncated: bool = False with an Attributes docstring entry; _execute_source sets it. Acceptance: three zero-staged regression cases - (a) no-refusal zero-staged path (print-page/exhausted short-circuit, max_frontier=0) false; (b) robots-denied or failed-start zero-staged crawl false; (c) reachable zero-staged crawl that did refuse a link true. _load_crawl_manifest still parses; existing metadata.json without the field still validates; the OSError still raises with the same message; pyright src/ 0 errors.