---
artifact_type: task
created_at: 2026-08-30T05:00:49.7798872Z
dependencies:
    - 068.007-T
id: 068.012-T
labels:
    - crawl
    - frontier
    - observability
parent_id: 068-F
priority: medium
references:
    - docs/plans/2026-08-29-crawl-frontier-observability-plan.md
status: queued
title: A.T9 - Thread truncation into the manifest and the staging job
updated_at: 2026-08-30T05:35:11.6297434Z
---

Domain: src. Files: src/docline/elt/execute.py, src/docline/fetch/models.py. _fetch_url consumes CrawlOutcome and returns (staged_count, frontier_truncated). Outcome unpacking and the results iteration MUST remain inside the existing try: so the except BaseException completion-event path still fires. Manifest becomes {'pages': ..., 'frontier_truncated': <the real flag>} and is written BEFORE the staged_count == 0 guard per D8 - never a hard-coded true. StagingJob gains frontier_truncated: bool = False with an Attributes docstring entry. Per D8, add CrawlStagedNothingError(OSError, DoclineError) carrying frontier_truncated and raise it in place of the bare OSError with the same message. IMPORTANT: _execute_source constructs the StagingJob only AFTER the try/except block (execute.py:227), so the flag CANNOT be set on job inside the handler. Hold it in a local (frontier_truncated = False initialised before the try), assign from the success tuple OR from CrawlStagedNothingError in the handler, and pass it into the later StagingJob(...) construction. Do not restructure _execute_source beyond that. Acceptance: three zero-staged regression cases - (a) no-refusal zero-staged path false; (b) robots-denied or failed-start zero-staged crawl false; (c) zero-staged crawl that did cost an eligible link true - WITH THE StagingJob REPORTING THE SAME VALUE IN EVERY CASE. _load_crawl_manifest still parses; existing metadata.json without the field still validates; the raised error still satisfies except OSError with the same message; pyright src/ 0 errors.