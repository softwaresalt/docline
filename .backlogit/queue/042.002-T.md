---
artifact_type: task
created_at: 2026-07-03T14:39:40.415889-07:00
custom_fields:
    source_stash_id: 24920EFF
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: low
    source_stash_text: '[review follow-up from 019-F] Validate weights_path in fidelity_scorer.load_weights against workspace containment when the path is supplied by an untrusted source (e.g. an MCP server caller). Today the CLI is the only caller and operator-supplied paths are trusted, so this is low priority. Becomes relevant when ProcessRequest is exposed via the MCP server and pdf_mode=triage is invoked remotely with a caller-controlled weights JSON path. Mitigation: reuse safe_workspace_path / validate_workspace_relative_path from docline.paths to constrain the loaded file under the workspace root.'
id: 042.002-T
parent_id: 042-F
priority: low
status: active
title: Constrain fidelity_scorer weights_path to the workspace root
updated_at: 2026-07-03T14:52:35.2423392-07:00
---

Validate weights_path in fidelity_scorer.load_weights (and load_pre_triage_weights) against workspace containment when supplied by an untrusted source (future MCP ProcessRequest caller with pdf_mode=triage + caller-controlled weights JSON). Reuse safe_workspace_path / validate_workspace_relative_path from docline.paths to constrain the loaded file under the workspace root; reject traversal with a typed FidelityScorerError. CLI-supplied operator paths remain trusted today. Source: 019-F review follow-up (stash 24920EFF).

<!-- BEGIN:acceptance-criteria -->
- load_weights and load_pre_triage_weights reject weights_path values that resolve outside the workspace root (raise FidelityScorerError).
- In-workspace paths and weights_path=None behave exactly as before (no regression to trusted CLI usage).
- Unit tests cover accept (in-workspace), reject (traversal / absolute-escape), and None cases.
- pytest green; ruff check/format clean.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
- load_weights @ src/docline/process/fidelity_scorer.py:539; load_pre_triage_weights @ :612 — both call weights_path.read_text() with no containment check.
- Helpers in src/docline/paths.py: safe_workspace_path(relative, workspace_root), validate_workspace_relative_path(relative), resolve_contained(path, workspace_root).
- Determine workspace_root consistent with existing callers; keep validation opt-in-safe so trusted CLI paths still resolve.
- Tests: extend tests/process/test_fidelity_scorer.py (the test tree mirrors src/docline/process/).
<!-- END:implementation-notes -->