---
artifact_type: task
created_at: 2026-08-27T08:05:39.0164696Z
dependencies:
    - 064.001-T
id: 064.005-T
labels:
    - mcp
    - stdio
    - tests
    - test-first
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Author stdio MCP dispatch + error-envelope + notification harness (red)
updated_at: 2026-08-27T08:08:05.9212639Z
---

Author the failing dispatch-parity + error-envelope + notification test harness for the stdio MCP server in tests/parity/test_mcp_stdio.py (test-first / red). Tests domain only. Second of four dependency-linked harness tasks split from the original oversized T1. Covers tools/call parity + CallToolResult wire-shape conformance (result.content is a ContentBlock[], isError, structuredContent-if-mirrored), JSON-RPC error envelopes, and notification silence. Security gates live in 064.006-T (H1-H3) and 064.007-T (H4-H6). See docs/plans/2026-08-27-mcp-stdio-server-plan.md (T1b).

<!-- BEGIN:acceptance-criteria -->
- tools/call parity + standards-valid MCP CallToolResult wire shape (parametrized over the three callable tools fetch, process, export_schema on success; plus the fetch/process failure->isError mapping): every successful tools/call (fetch, process, export_schema) returns a CallToolResult whose result.content is a non-empty ContentBlock[] (each block a valid typed block, e.g. a text block with a string body) - NOT a raw execute_fetch/execute_process FetchResult/ProcessResult (or export_schema str) serialized directly, which real MCP clients reject. fetch/process parity vs execute_fetch/execute_process holds (same behavior as the CLI); for fetch/process a validated-but-failed tool result (success=False) maps to isError=true with the sanitized error text carried in content. (export_schema has no domain success=False path; its only failure mode - non-empty arguments - is a -32602 invalid-params envelope covered by the error-envelope scenario, NOT an isError CallToolResult.) When the adapter mirrors structured output, result.structuredContent is asserted to mirror the domain result. This scenario asserts the LEGACY-era CallToolResult body (greens at 064.002-T); the MODERN-era wrapper (resultType:'complete' around the SAME CallToolResult body) is asserted by the dual-era harness 064.021-T scenario (c) and greened by 064.022-T, preserving the milestone model.
- Error envelopes asserted as one parametrized scenario: -32700 (parse error / invalid JSON), -32600 (invalid request), -32601 (method not found), -32602 (invalid params), -32603 (internal).
- -32600 request-shape validation (bounded parametrized cases): a non-object root (array, string, number, bool, null), a missing or non-"2.0" jsonrpc member, and a missing/non-string method each return -32600 Invalid Request - distinct from -32700 (bad JSON) and -32601 (well-formed request, unknown method).
- id-less notification (notifications/initialized) is silent (no response frame).
- All new tests observed FAILING before implementation (red).
- Scenario budget: 3 (dispatch parity + CallToolResult wire shape [parametrized over fetch/process/export_schema x success/failure], parametrized error envelopes incl. -32600, notification) - within the <4 heuristic. The wire-shape assertion strengthens the existing parity scenario in-place (no new scenario added), keeping the task test-only and the scenario count/width compliant.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: test-first (write, verify red). Depends on 064.001-T. Do not implement production code (width isolation). -32600 is REQUIRED for JSON-RPC 2.0 conformance (restored in cycle-2; the earlier "dropped -32600" scope trim was reversed). Implemented by 064.002-T request-shape validation in dispatch(). The CallToolResult wire-shape assertion is greened by 064.002-T's single result-shaping step (legacy tools/call CallToolResult body); the modern-era resultType wrapper around that SAME body is asserted separately in 064.021-T scenario (c) and greened by 064.022-T, so this harness stays legacy-era (fully green at 064.002-T). The export_schema wire-shape sub-case exercises server.call_tool's export_schema adapter (delivered by 064.015-T, which precedes 064.002-T in the execution order), so it greens at 064.002-T with the rest.
<!-- END:implementation-notes -->