---
artifact_type: task
created_at: 2026-08-27T08:45:10.0000000Z
dependencies:
    - 064.011-T
id: 064.012-T
labels:
    - fetch
    - security
    - dos
    - tests
    - test-first
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Author shared-fetch resource-cap harness (red)
updated_at: 2026-08-27T08:45:10.0000000Z
---

Author the failing shared-fetch resource-exhaustion harness in the fetch test suite (tests/fetch/) (test-first / red). Tests domain only. Cycle-2 review found FetchRequest.max_pages has no upper bound (src/docline/app_models.py:25) and each response is buffered with an unbounded response.read() (src/docline/fetch/http.py:123), so a small valid request can exhaust memory/network on the untrusted MCP surface. This harness pins the required page/work cap and streamed response-byte cap on the SHARED fetch code (affects CLI and MCP). See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Plan Hardening H7).

<!-- BEGIN:acceptance-criteria -->
- FetchRequest.max_pages above the hard upper bound is rejected at model validation (ValidationError / -32602 on the MCP boundary).
- Per-response byte cap (parametrized: initial response AND a redirected response): a body exceeding MAX_RESPONSE_BYTES is aborted without fully buffering it (streamed read; assert the read is chunked/bounded, not a single response.read()).
- All new tests observed FAILING before implementation (red) - no upper bound / no byte cap exists yet.
- Scenario budget: 2 (max_pages cap, per-response streamed cap [init+redirect parametrized]) - within the <4 heuristic. NOTE (cycle-3): the aggregate MAX_TOTAL_FETCH_BYTES budget moved to its own byte-accurate pair (064.016-T harness / 064.017-T impl) so this harness stays width-isolated to the per-dimension caps.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: test-first (write, verify red). Tests domain only (no production code - width isolation). Use a fake/stub HTTP response (large or chunked body) so the byte-cap tests are hermetic. Turned green by impl 064.013-T. The aggregate crawl-byte budget (byte-accurate) is a separate pair (064.016-T/064.017-T). The end-to-end MCP boundary proof (over-limit max_pages + oversized/aggregate response via tools/call fetch) lives in 064.014-T. Depends on 064.011-T (linear chain).
<!-- END:implementation-notes -->
