---
artifact_type: feature
created_at: 2026-06-03T13:11:06.3316335-07:00
custom_fields:
    harness_status: pending
id: 011-F
labels:
    - ci
    - ops
    - docs
    - stage-harvested
references:
    - docs/plans/2026-06-03-docline-ci-workflow-plan.md
    - docs/decisions/2026-06-03-docline-ci-workflow-deliberation.md
    - docs/decisions/2026-06-03-docline-ci-workflow-plan-review.md
status: active
title: docline CI workflow (GitHub Actions, Linux-only)
updated_at: 2026-06-03T13:15:18.431609-07:00
---

Establish automated quality-gate enforcement on every PR and `main` push by adding a single GitHub Actions workflow that runs the docline lint, format, typecheck, test, and build gates on a Linux runner (`ubuntu-latest`). Bundle a short `CONTRIBUTING.md` section that documents the known Windows local-dev `tmp_path` `PermissionError` noise so newcomers have a recovery path.

Harvested from stash entries `9C40BF99` (high — CI workflow) and `CE758832` (low — Windows pytest noise documentation). Combines both into one coherent shipment per the deliberation recommendation (Option C).

Constraints: Linux-only single matrix entry. No deploy, no PyPI publish, no Codecov, no Docker, no signing, no `pull_request_target`. Compliant with `.github/instructions/ci-security.instructions.md` and `.github/instructions/workflows.instructions.md`.

<!-- BEGIN:AcceptanceCriteria -->
- `.github/workflows/ci.yml` exists, parses as valid YAML, and `actionlint` reports zero errors
- All five gates (`ruff check`, `ruff format --check`, `pyright src/`, `pytest`, `python -m build`) run as CI jobs on `ubuntu-latest`
- All third-party `uses:` references pinned to full commit SHA with trailing semver comment
- Workflow-level `permissions: contents: read`; no job grants write
- `persist-credentials: false` on every `actions/checkout`
- Workflow-level concurrency group with `cancel-in-progress: true`
- Triggers limited to `pull_request` and `push` on `main`; no `pull_request_target`
- Zero `secrets.*` references
- `CONTRIBUTING.md` exists at repo root with sections: Local quality gates, Running gates with uv, Known Windows local-dev noise, Pre-PR checklist
- Probe PR shows all five jobs `conclusion: success`
- Closure document at `docs/closure/011-S-docline-ci-workflow.md` records job results and SHA pinning audit
<!-- END:AcceptanceCriteria -->

<!-- BEGIN:ImplementationNotes -->
See `docs/plans/2026-06-03-docline-ci-workflow-plan.md` for full plan with hardened controls. P2 finding from plan review: use commit scope `ops` for the workflow file and `docs` for `CONTRIBUTING.md`.
<!-- END:ImplementationNotes -->