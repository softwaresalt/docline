---
artifact_type: feature
created_at: 2026-06-04T09:53:08.1447768-07:00
custom_fields:
    harness_status: pending
id: 015-F
labels:
    - processing
    - readers
    - docling
    - sidecars
    - stage-harvested
    - shipment-14
references:
    - docs/plans/2026-06-04-G3c-docling-sidecars-plan.md
    - docs/decisions/2026-06-04-G3c-docling-sidecars-deliberation.md
    - docs/decisions/2026-06-04-G3c-docling-sidecars-plan-review.md
status: queued
title: docling PDF engine + image sidecars for layout-preserving markdown emission
updated_at: 2026-06-04T09:53:08.1447768-07:00
---

Wire docling as opt-in PDF engine and extract DOCX embedded images as media sidecars. Add `[project.optional-dependencies] pdf = ["docling>=2,<3"]`, introduce `layout_engine="auto"` with `_resolve_layout_engine` (uses existing `dependencies.pdf_available()`), thread `pdf_engine` through CLI + `ProcessRequest` + `build_output_document_parts`, add `PictureSink` protocol + `CountingPictureSink`, extend DOCX reader with `read_docx_blocks_with_media` that walks `<w:drawing>/<a:blip r:embed/>` and resolves via `word/_rels/document.xml.rels`, surface `media_files` in `OutputDocumentPart` and per-source `manifest.json` entries. Depends on 012-S + 013-S (both merged). Harvested from stash `351170C9`. Cross-repo `graphtor-docs` snapshot refresh remains a separate operator follow-up (already stashed as `752CA1E4`).

<!-- BEGIN:AcceptanceCriteria -->
- `[project.optional-dependencies] pdf = ["docling>=2,<3"]` in `pyproject.toml`
- `_SUPPORTED_LAYOUT_ENGINES` includes `"auto"`; `_resolve_layout_engine` exposed
- `read_pdf` / `read_pdf_pages` default `layout_engine="auto"`
- `src/docline/readers/picture_sink.py` with `PictureSink`, `MediaReference`, `CountingPictureSink`
- `read_docx_blocks_with_media(path, picture_sink)` extracts images + emits `![](media/figure-NNNN.ext)` at source position
- `OutputDocumentPart.media_files: tuple[str, ...] = ()`
- Per-source manifest entries gain `media_files: list[str]`
- CLI `--pdf-engine {auto,docling,heuristic}` default `auto`
- `ProcessRequest.pdf_engine: Literal["auto","docling","heuristic"] = "auto"`
- All 5 CI gates pass
- `docline export-schema` output unchanged
- Closure document `docs/closure/014-S-docling-sidecars.md`
<!-- END:AcceptanceCriteria -->

<!-- BEGIN:ImplementationNotes -->
Use `.\.venv\Scripts\python.exe` for local commands — bare `python` on this shell resolves to system-wide 3.14. CI is 3.12 (unaffected). Plan-review F1: wire `media_files` into `OutputDocumentPart.media_files` from DOCX branch (cast tuple); PDF branch passes `()` when no sidecar emitted. Plan-review F2: when ANY media is extracted for a source, force the multi-part output layout (`{source_basename}/part-0001.md` + `{source_basename}/media/figure-NNNN.png`) regardless of segment count so markdown `![](media/figure-0001.png)` reference is always sibling-relative.
<!-- END:ImplementationNotes -->