---
artifact_type: task
created_at: 2026-06-13T00:34:34.0693486-07:00
dependencies:
    - 029.002-T
id: 029.003-T
parent_id: 029-F
priority: medium
references:
    - docs/plans/2026-06-13-mistral-ocr-replace-adi-plan.md
status: active
title: T3 — Wire mistral_ocr through pdf_engine Literal + CLI + MCP + dispatch
updated_at: 2026-06-13T01:08:58.7973986-07:00
---

Wire mistral_ocr through the full pdf_engine surface:

1. src/docline/readers/pdf.py:
   - _SUPPORTED_LAYOUT_ENGINES = frozenset({"auto", "heuristic", "docling", "mistral_ocr"})
   - _resolve_layout_engine: auto path UNCHANGED (still docling > heuristic). Mistral never auto-selected pending T4 verdict. Document this in the docstring.
   - read_pdf_pages: dispatch "mistral_ocr" with MistralCredentialError fast-fail (re-raise, don't fall back) AND transient-error surfacing (re-raise as DoclineError with context — same explicit-request contract we established for the post-029-S azure_di removal, now obsolete but the PATTERN applies to mistral_ocr).

2. src/docline/app_models.py: pdf_engine: Literal["auto", "docling", "mistral_ocr", "heuristic"] = "auto". Docstring rewrites:
   - auto: prefer docling, fall back to heuristic. mistral_ocr is NEVER auto-selected pending empirical study verdict (see docs/closure/029-S-adi-spike.md for the same precedent applied to the now-removed ADI).
   - mistral_ocr: opt-in cloud peer via [mistral] extra + MISTRAL_API_KEY env var. Raises when SDK missing or credentials unset.

3. src/docline/cli.py: process subparser and ingest local-dir subparser both update --pdf-engine choices to include "mistral_ocr" (and exclude "azure_di" — already removed by T1).

4. src/docline/app.py: verify MCP manifest enum auto-surfaces correctly via Pydantic. The ingest_local_dir tool and process tool manifests should both list mistral_ocr in pdf_engine.enum.

TDD. Tests in tests/readers/test_pdf_engine_routing.py (extend) — 6-8 tests:
- test_explicit_mistral_ocr_passes_through (returns "mistral_ocr")
- test_auto_never_selects_mistral_ocr_even_with_creds (auto stays docling-first regardless of MISTRAL_API_KEY)
- test_auto_falls_back_to_heuristic_when_nothing_installed (no change from current; just sanity check after T1 cleanup)
- test_explicit_mistral_ocr_surfaces_credential_error (FakeMistral client with no MISTRAL_API_KEY → MistralCredentialError raised)
- test_explicit_mistral_ocr_surfaces_transient_error_loudly (FakeMistral client raising → DoclineError raised, no silent fallback)
- test_cli_process_accepts_pdf_engine_mistral_ocr (subprocess test of --pdf-engine mistral_ocr in --help output)
- test_manifest_includes_mistral_ocr_in_pdf_engine_enum (--manifest JSON parsing)
- test_process_request_pdf_engine_includes_mistral_ocr (Pydantic Literal validation)

Update tests/readers/conftest.py to add `install_fake_mistral_sdk` fixture (mirror the removed install_fake_adi_sdk pattern — module-level _FakeMistralClient and _FakeMistralError for class-identity stability).