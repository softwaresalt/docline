---
artifact_type: task
created_at: 2026-08-27T15:08:33.6160073Z
dependencies:
    - 064.015-T
id: 064.018-T
labels:
    - mcp
    - parity
    - correctness
    - tests
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Author fetch HTTP(S)-only advertising parity harness
updated_at: 2026-08-27T23:55:00.0000000Z
---

Author the RED parity harness (tests/parity/) proving the advertised fetch tool description is HTTP(S)-only and consistent with execute_fetch's rejection of non-HTTP(S) sources. Tests domain only. The shared description at app.py:465-468 currently claims 'a URL or file path' while execute_fetch (app.py:596-603) rejects every non-http(s) scheme, so clients are advertised an unsupported input mode. Verifies red until 064.019-T. See plan Design (fetch advertising) / cycle-3 remediation.

<!-- BEGIN:acceptance-criteria -->
- Scenario (a): the advertised fetch tool description (located by name as the existing manifest tests do - next(t for t in get_mcp_manifest().tools if t.name == 'fetch').description, NOT tools['fetch'] which raises TypeError because .tools is a list[ManifestTool]; reached via BOTH list_tools() and list_callable_tools()) states HTTP(S)-only and does NOT claim 'file path' / local-file support.
- Scenario (b): manifest-behavior parity - the advertised input contract matches execute_fetch's scheme rejection (execute_fetch fails any non-http/https source), so the advertisement cannot promise an input mode the executor rejects.
- Both scenarios authored and observed RED against the current 'a URL or file path' description.
- No production code changed in this task (tests only).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: red-only harness authored before 064.019-T. Author into tests/parity/ (alongside the manifest parity suite). get_mcp_manifest().tools is a list[ManifestTool], so locate the fetch entry by its name (next(t for t in ...tools if t.name == 'fetch')) exactly as tests/parity/test_manifest_parity.py already does - a subscript like tools['fetch'] raises TypeError. Assert on the located tool's description string and cross-check execute_fetch scheme behavior (it returns success=False for a non-http(s) source). Width-isolated: tests domain only. Depends on 064.015-T; greened by 064.019-T.
<!-- END:implementation-notes -->