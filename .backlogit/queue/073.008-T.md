---
artifact_type: task
created_at: 2026-09-15T21:20:00Z
id: 073.008-T
labels:
    - security
    - test
    - integration
    - elt-staging
parent_id: 073-F
priority: medium
references:
    - docs/plans/2026-09-14-elt-staging-credential-redaction-plan.md
status: queued
title: Integration test - live WARNING/error text drops new credential names
updated_at: 2026-09-16T05:14:56.2192523Z
---

Stream B / Unit BI (test domain, characterization-first). Live-sink integration test that removes B1's live-WARNING overclaim (cycle-2 finding #2) and proves execute.py error-output provenance preservation (error-output provenance finding). Drive the LIVE WARNING/error emission path — the execute.py exception/log composition (`_scrub_exception_for_logging` → `_scrub_exception_message` / `_exception_scrub_replacements` / `_sanitize_exception_text` / `_redact_query_param_fragments`, plus the shared source_keys._remove_credential_query_params typed/059-S surface), capturing the emitted `_log.exception(...)` text — for EACH of the four URL-bearing source kinds (WebCrawlSource, ManifestUrlSource, GitHubRepoSource, ManifestGitSource) — explicit all-four coverage matching the four source-kind rows the plan matrix attributes to BI/073.008-T (cycle-3 finding F-03; parametrized 4 kinds x 9 names stays within the 2-hour test-domain boundary, one test file), not a single source-kind-agnostic case — and assert (a) the FULL new credential vocabulary (password, pwd, passwd, client_secret, refresh_token, code, apikey, x-goog-credential, awsaccesskeyid) is dropped from the emitted text on each source kind's WARNING/error path, AND (b) the composition BYTE-PRESERVES the non-credential provenance fields GENUINELY COMPOSED INTO THIS SINK — source branch, path_glob, and manifest ID — even when those fields contain credential-looking text (e.g. branch `feat/token`, glob `**/*?token=*`, manifest id `tokenizer`), removing ONLY structured URL userinfo + recognized credential query-param NAMES + typed secret fields. SINK-SCOPE HONESTY (Finding P1, 2026-09-16 correction): the execute.py `_exception_scrub_replacements` WARNING/error composition emits ONLY url/repo_url, branch, path_glob, and manifest id per source kind (ManifestLocalSource composes ONLY config.id; LocalFileSource is not in the replacement set), so local filesystem PATH and INCLUDE patterns are NEVER composed into this live sink and MUST NOT be asserted here — that byte-preservation is proven at the executable typed-serialization / helper surface by 073.001-T (build_source_key/sanitize_source_key), NOT at the WARNING/error sink. This backs the live WE (WARNING/error) new-name invariant AND the provenance-preservation invariant with an executable test at the real emission point rather than the helper-level matcher unit test (that helper level is B1/073.003-T). Acceptance criteria: (1) test exercises the real execute.py WARNING/error emission path (`_scrub_exception_for_logging` composition + shared typed surface) for all four URL-bearing source kinds and captures the emitted `_log.exception(...)` text (not _is_credential_param directly); (2) FAILS on current HEAD across all four source kinds for the full new vocabulary (currently unredacted in that path) and/or because the current composition alters provenance fields; (3) after the merged production task 073.002-T lands, the FULL new vocabulary (password, pwd, passwd, client_secret, refresh_token, code, apikey, x-goog-credential, awsaccesskeyid) is dropped from the live WARNING/error text for all four source kinds; (4) the composed WARNING/error text BYTE-PRESERVES branch, path_glob, and manifest ID (provenance) — including credential-looking benign values — while removing only structured URL userinfo + recognized credential query-param NAMES + typed secret fields; local filesystem path and include patterns are NOT composed into this sink and are NOT asserted here (proven at the typed surface by 073.001-T); (5) existing 059-S WARNING-path behavior for pre-063-S markers unchanged; (6) no production code changed; (7) scope is STRUCTURED ACCESS CREDENTIALS ONLY (the recognized credential query-param names + URL userinfo + typed secret fields) — no path-embedded-secret or document-content assertion (2026-09-15 operator decision: path-secret redaction REJECTED, stream C retired). This is the stream-B live-WARNING coverage (full new vocabulary + provenance preservation; satisfied by the merged 073.002-T, which implements B2 and the execute.py provenance-preservation change; former separate code task 073.004-T is retired/merged into 073.002-T); the live default-STDOUT A2+B2 composition is proven separately by 073.009-T. Execution posture: characterization-first / test-first (073.002-T depends on this task). Files: one test file under tests/.

<!-- BEGIN:sizing -->
Size: S | Complexity: low
<!-- END:sizing -->
