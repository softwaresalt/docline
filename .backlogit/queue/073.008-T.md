---
artifact_type: task
created_at: 2026-09-15T21:20:00Z
id: 073.008-T
labels:
    - security
    - test
    - integration
    - elt-staging
parent_id: 073-F
priority: medium
references:
    - docs/plans/2026-09-14-elt-staging-credential-redaction-plan.md
status: queued
title: Integration test - live WARNING/error text drops new credential names
updated_at: 2026-09-16T05:14:56.2192523Z
---

Stream B / Unit BI (test domain, characterization-first). One existing test file,
e.g. `tests/elt/test_elt_real_execution.py`; exercise the REAL
`_execute_single_source` failure emission (`_log.exception` with formatted
`exc_info`, not just the helper). For each of WebCrawlSource,
ManifestUrlSource, GitHubRepoSource, ManifestGitSource, parametrize the full
nine-name NEW recognized credential vocabulary (password, pwd, passwd,
client_secret, refresh_token, code, apikey, x-goog-credential,
awsaccesskeyid). Check separately the two live-sink trust channels:
(A) typed `source_key=%s` log argument and `SourceMetadata.source` /
`metadata.json` preserve branch, path_glob and manifest id BYTE-FOR-BYTE
including `feat/token`, `**/*?token=*`, `src?token=BENIGN`, while typed URL
userinfo/query credential values are absent; (B) untrusted `error=%s` text
and rendered `exc_info` traceback redact a DISTINCT synthetic credential
from standalone `?token=SECRET` with NO full configured credentialed URL
and from exception cause/context/notes/ExceptionGroup child messages
when present. Do not reinsert raw
provenance-looking substrings into (B): same bytes may be untrusted secret
there. Assert error type and safe contextual diagnostics when safely
representable; ambiguous untrusted text may become a bounded generic typed
error, never a success-shaped fallback. Include an exception with ONLY a
secret-bearing `__notes__` (safe message), to catch the current
`_clone_scrubbed_exception` early return and verbatim notes copy; add a
clean-message `ExceptionGroup` with a child carrying `?token=SECRET`
(formatter prints children), plus a custom exception with a raising
`__str__` to test the bounded generic typed fallback; assert
`caplog.text` AND `record.getMessage()` / formatted `exc_info` contain no
synthetic secret. Cause/context are logged via `_log.exception` today;
assert both branch chains and notes in the live formatted log. Existing
`tests/elt/test_elt_real_execution.py` manifest-ID fixtures currently assert
global absence of `IDSECRET` even though the ID is typed benign provenance:
update those contradicting assertions to preserve that ID in controlled
`source_key`/metadata and use DISTINCT URL/error synthetic secrets for
absence tests (do not drop the URL/error protection).

Acceptance criteria: (1) real live failure log captured on all four URL
source kinds, full nine-name new vocabulary absent from untrusted text and
URL credential fields; (2) RED on HEAD for the new names and for typed
provenance corruption (`src?token=BENIGN` especially), while a standalone
recognized `?token=SECRET` in UNTRUSTED error/cause/context/notes remains
non-leaking after the production change even if the configured URL has no
secret; notes-only leak is RED on HEAD; (3) trusted typed `source_key` /
metadata preserve branch/path_glob/manifest id byte-for-byte at their actual
structured emission points, NOT arbitrary untrusted exception prose;
(4) capture formatted `caplog.text`, `record.getMessage()`, and traceback /
`exc_info` (which currently renders cause/context/notes/group children);
prove a throwing `__str__` yields a logged, incomplete job rather than
escaping the failure handler; retain typed
exception and useful safe context where possible, allow conservative
non-leaking generic typed failure for ambiguous prose; (5) preserve
existing 059-S WARNING behavior for old recognized markers; reconcile
manifest-ID-as-secret legacy assertions without weakening real credential
absence tests; (6) test-domain only, one test file, at most three logical
scenarios (four-kinds × nine-names parametrization, known typed provenance
versus standalone untrusted text, chain/notes/group/render-fallback as
one parametrized traceback-safety scenario); (7) no document-body or
URL/local path scanning, no claim local filesystem path/include is emitted
as structured WARNING provenance (its typed serialization is covered by
073.001-T). Execution posture: characterization-first / test-first;
073.002-T depends on this task; five-test-to-one-production DAG unchanged.

<!-- BEGIN:sizing -->
Size: S | Complexity: low
<!-- END:sizing -->
