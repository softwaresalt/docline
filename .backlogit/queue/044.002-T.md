---
artifact_type: task
created_at: 2026-07-04T01:28:02.5696625-07:00
dependencies:
    - 044.001-T
id: 044.002-T
parent_id: 044-F
priority: medium
status: queued
title: Stamp canonical_url into local-dir frontmatter (docline namespace)
updated_at: 2026-07-04T01:28:02.5696625-07:00
---

Load the source-root publish config once during local-dir ingestion; for each processed doc call `derive_canonical_url(config, source_path)` and stamp the result as `canonical_url` under the docline frontmatter namespace. When no publish config is present at the source root, or the doc matches no docset, omit `canonical_url` (graceful; existing frontmatter and output unchanged). Depends on the derivation function from 044.001-T.

<!-- BEGIN:acceptance-criteria -->
- Local-dir ingest of a synthetic repo containing `.openpublishing.publish.config.json` + a few `.md` files → each output doc's frontmatter carries `canonical_url` under `docline:`.
- Ingest of a repo WITHOUT a publish config → no `canonical_url` emitted; existing frontmatter/tests unaffected (no regression).
- pytest / ruff / pyright green.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
- Add `canonical_url: str | None` as a docline-namespaced field via `_DoclineNamespacedFrontmatter.DOCLINE_FIELDS` (src/docline/schema/library.py:19-27) on the family used for source-md (WikiFrontmatter), OR inject through `app.py::_build_markdown_with_frontmatter(..., docline_namespace=...)` (~line 200).
- `source_path` is on BaseFrontmatter (src/docline/schema/models.py:49).
- Local-dir flow: `elt/execute.py` + `ManifestLocalSource` (src/docline/elt/manifest_models.py:17); load the publish config from the source root once per source.
- Tests: end-to-end under tests/elt/ or tests/process/ with a synthetic repo fixture.
<!-- END:implementation-notes -->