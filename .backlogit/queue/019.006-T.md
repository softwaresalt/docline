---
artifact_type: task
created_at: 2026-06-06T15:45:03.2671254-07:00
id: 019.006-T
labels:
    - cli
    - triage
    - calibration
    - test-first
parent_id: 019-F
priority: high
references:
    - docs/plans/2026-06-06-triage-then-repair-plan.md
    - scripts/load_test.py
status: queued
title: U6 — `--triage-report-only` validation mode
updated_at: 2026-06-06T15:45:40.7666054-07:00
---



<!-- BEGIN:acceptance-criteria -->
- `triage_report_only(path, *, output_dir, report_tsv_path, scorer=None)` is a separate function from `process_pdf_triaged`, sharing only the Pass 1–2 helpers (plan-review P2 finding: avoid kwargs explosion on the main orchestrator).
- `report_only` path NEVER invokes the docling runner (assertable: `runner.call_count == 0` when injected).
- TSV is created at `report_tsv_path` with canonical columns and one row per page; rows sorted by `page_index` ascending.
- CLI: `docline process --pdf-mode triage --triage-report-only --report-tsv-path PATH` runs the report path.
- Returns a `TriageResult` with `engine_per_page = ['heuristic'] * total_pages` and populated `flagged_ranges` for downstream review.
- RED tests written first and fail; GREEN after implementation.
- Test scenarios: (1) `triage_report_only` never invokes runner; (2) TSV has correct columns; (3) TSV rows sorted by page_index.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Add a `triage_report_only()` sibling function to `src/docline/process/pdf_triage.py` that runs Passes 1–2 (heuristic + score) only, emits a per-page TSV `(page_index, signal_name=value..., aggregate, needs_docling, reason)`, and never invokes docling. Expose via `--triage-report-only` boolean CLI flag composable with `--pdf-mode triage`.

Purpose: empirical calibration of `_SIGNAL_WEIGHTS` thresholds against a curated corpus before locking weights for production use. First calibration target: the 12 completed cosmos chunks from the 2026-06-06 load test where docling output already exists for comparison.

Files:
- `src/docline/process/pdf_triage.py` (modified — add sibling function)
- `src/docline/cli.py` (modified — add `--triage-report-only` and `--report-tsv-path` flags)
- `tests/process/test_pdf_triage_report_only.py` (new)

Depends on: U3 (orchestrator) and U4 (CLI).
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
TSV format mirrors the existing `scripts/load_test.py` `_TSV_FIELDS` pattern (tab-delimited, header on first row, dict-writer).

This task PLUS U7 unlocks the calibration workflow documented in the plan's hardening section. Calibration itself (running this against cosmos and locking weights) becomes a follow-on stash entry after the shipment closes.
<!-- END:implementation-notes -->