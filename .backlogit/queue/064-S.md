---
artifact_type: shipment
created_at: 2026-09-14T07:36:58.0829952Z
custom_fields:
    items:
        - 073-F
        - 073.001-T
        - 073.003-T
        - 073.007-T
        - 073.008-T
        - 073.009-T
        - 073.002-T
id: 064-S
priority: high
status: queued
title: 'ELT staging credential redaction: default-fetch sink + coverage expansion'
updated_at: 2026-09-16T04:28:13.176172Z
---

## Description

<!-- BEGIN:description -->
Shipment scope: close the default (non-`--execute`) `docline fetch` structured
access credential sink and expand ELT staging credential-name coverage. Covering
feature 073-F over TWO width-isolated credential-redaction concerns delivered by FIVE
test tasks and ONE MERGED production task: (A) thread the typed
sanitizer into `create_staging_job`/`orchestrate_fetch` — keyword-only
`sanitized_source`, RAISING a typed non-leaking exception (no whole-key sentinel) on
compound-key omission, and preserving non-credential typed provenance fields
(branch/path_glob/manifest ID/local path/include, incl. LocalFileSource/
ManifestLocalSource) byte-for-byte (073.001-T helper test, 073.007-T live-stdout
userinfo-subset integration test);
(B) REPLACE the legacy `startswith` heuristic with an
explicit, case-insensitive, EXACT-match credential-name vocabulary (existing/legacy/
cloud variants individually enumerated + x-goog-credential/awsaccesskeyid; not
universally complete — unknown names intentionally not redacted) plus bounded
query-NAME decode on BOTH the string and typed sanitizer paths (073.003-T helper test,
073.008-T live-WARNING integration test). The SINGLE MERGED production task 073.002-T
implements BOTH concerns (A2 typed sink wiring + B2 exact-match vocabulary/decode) across
src/docline/fetch/staging.py, src/docline/elt/orchestrate.py,
src/docline/elt/source_keys.py, and src/docline/elt/execute.py (FOUR files — execute.py
added for error-output provenance byte-preservation on the WARNING/error composition). A shared final composition gate 073.009-T proves the
A2+B2 live-stdout composition (userinfo + all recognized new names); it and the other four
test tasks all go GREEN together when the single merged production task 073.002-T
completes. This is the 2026-09-15 P0 GREEN-GATE FIX: two separate code tasks could not
each green the composition gate (a per-task-green / test-first violation), so former
stream-B code task 073.004-T is RETIRED/merged into 073.002-T (status=blocked, edges
removed, out of manifest), retained non-destructively. Scope covers STRUCTURED
ACCESS CREDENTIALS ONLY (URL user-info, recognized credential query-param names, typed
secret config fields); ordinary URL paths and provenance are preserved byte-for-byte.
Stream C (path-embedded secret redaction, 073.005-T + 073.006-T) is REJECTED and
RETIRED per the 2026-09-15 operator contract: Docline MUST NOT scan/rewrite
source-document content and MUST leave ordinary URL paths byte-for-byte unchanged
(that is an upstream DLP concern). Stream C tasks are status=blocked (returned via
shipment return-blocked), their dependency edges removed, and removed from this
manifest — retained non-destructively for traceability. Security-sensitive
(credential-redaction correctness); ActionRisk high; operator-authorized under
DARK_MODE_ACTIVE scope 064-S with merge-preauthorization=false and
admin-fallback=false. Source: docs/plans/2026-09-14-elt-staging-credential-redaction-plan.md,
docs/decisions/2026-09-14-elt-staging-credential-redaction-deliberation.md.
Parent-first, dependency-ordered manifest (7 items: 073-F + 5 test tasks + 1 merged
production task); hand off to Ship. The 2026-09-15 operator contract also resolved the
three cycle-3 P1 blockers by REMOVING the unjustified path requirement (not by adding
path parsing); see the plan/deliberation FINAL Operator Contract sections. Stage owns
and COMMITS its own planning/backlog artifacts on chore/stage-064-s (Finding 8); the
Orchestrator coordinates review, the remote staging gate, and the Ship handoff (it does
not commit Stage artifacts).
<!-- END:description -->

## Items

<!-- BEGIN:items -->
- 073-F (covering feature)
- Test tasks (all leaf, characterization/test-first, no upstream deps):
  - 073.001-T (concern A helper test: in-memory sink + typed-field preservation incl. LocalFileSource/ManifestLocalSource)
  - 073.003-T (concern B helper test: exact-match vocabulary + bounded query-name decode)
  - 073.007-T (concern A live-stdout userinfo subset)
  - 073.008-T (concern B live-WARNING new-name subset)
  - 073.009-T (final A2+B2 live default-stdout composition gate)
- 073.002-T (SINGLE MERGED production task, code) — depends on ALL five test tasks; completing it greens the entire suite (A2 typed sink wiring + B2 exact-match vocabulary/decode across staging.py, orchestrate.py, source_keys.py, execute.py — FOUR files, execute.py added for error-output provenance byte-preservation)
- MERGED/RETIRED: former stream-B code task 073.004-T merged into 073.002-T (status=blocked, dependency edges removed, removed from this manifest) — retained non-destructively; P0 green-gate fix so the composition gate greens after one atomic code task
- Stream C: RETIRED (REJECTED by 2026-09-15 operator contract) — 073.005-T + 073.006-T removed from this shipment (status=blocked, dependency edges removed), retained non-destructively for traceability
- Manifest = 7 items (073-F + 073.001-T, 073.003-T, 073.007-T, 073.008-T, 073.009-T, 073.002-T)
<!-- END:items -->

