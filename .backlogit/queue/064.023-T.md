---
artifact_type: task
created_at: 2026-08-27T15:09:45.5753391Z
dependencies:
    - 064.022-T
id: 064.023-T
labels:
    - mcp
    - protocol
    - dual-era
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Implement dual-era routing with legacy retention
updated_at: 2026-08-27T23:55:00.0000000Z
---

Make 064.021-T green (dual-era routing completion + legacy retention + guardrail-parity verification). Code domain, <=2 files (src/docline/mcp/stdio.py + src/docline/mcp/server.py). Owns the LEGACY branch of the request-shape era classifier: an initialize request LATCHES a per-process legacy-era selection (initialize -> legacy), and metadata-free operations (tools/call/tools/list with NO _meta) received BEFORE that selection are REJECTED (error result), never served as legacy - so the process era is latched by initialize, not selected by an unadorned request shape. Completes dual-era routing on top of the modern-branch routing delivered by 064.022 (the modern branch stays request-stateless). Keep the legacy handshake/ping path (from 064.002) intact; identity/capabilities/versions are read from the single describe_server() accessor introduced by 064.015, so initialize (064.002) and server/discover (064.022) cannot drift by construction. GUARDRAIL PARITY (blocking, VERIFY not wire): 064.022 already routes the modern branch through the SAME hardened dispatch path as legacy (guards-by-construction); THIS task VERIFIES that both eras funnel every tools/call/process through that one hardened path so H1 (workspace_root reject -32602), H3 (error non-disclosure), H4 (closed allow-list), H5 (stdout hygiene) apply before the modern result wrapper - the era classifier changes only negotiation + envelope shape, never which guards run. 064.021 scenario (c) already greened at 064.022; this task must not regress it. See plan Protocol Era Model / cycle-3 + cycle-4 + cycle-5 remediation.

<!-- BEGIN:acceptance-criteria -->
- Legacy branch of the era classifier: an initialize request LATCHES a per-process legacy-era selection (initialize -> legacy); a request carrying modern _meta was already routed to modern semantics by 064.022 (request-stateless). A metadata-free operation (tools/call/tools/list with NO _meta) received BEFORE the initialize latch is set is REJECTED (error result), never served as legacy. Both branches coexist under one classifier.
- The legacy initialize/notifications/initialized/ping path is retained and unchanged for legacy clients.
- No drift: initialize and server/discover both read identity/capabilities from the single describe_server() accessor (introduced by 064.015), and initialize.protocolVersion (legacy singular) is contained in server/discover's supportedVersions (modern plural) - a containment check, not a singular-vs-plural field equality - so the no-drift property holds by construction.
- Guardrail parity VERIFIED (guards were applied by the modern branch in 064.022, not first wired here): a modern (_meta, no initialize) tools/call process with workspace_root is rejected -32602 (H1); modern-path isError content sanitizes absolute paths (H3); modern-path stdout stays clean (H5); H4/H6/H7 hold on the modern path. The §H8 external-PDF-engine reject (-32602 for a non-allow-list pdf_engine on a default server) likewise holds on BOTH eras because it funnels through the SAME hardened dispatch (adapter chokepoint 064.032-T + transport mapping 064.034-T); proven dual-era by 064.033-T. Both eras share ONE hardened dispatch path; this task confirms parity is not regressed by adding the legacy branch.
- 064.021-T harness passes green (legacy-retention regression anchor stays green; era routing both directions; modern-path guard parity - already green from 064.022 - stays green).
- ruff check ., ruff format --check ., pyright src/ clean; typed errors only.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green after 064.022-T. Boundary with 064.022: 064.022 owns the MODERN branch (detect _meta -> modern handlers) + server/discover + -32022; this task owns the LEGACY branch (initialize -> latch legacy) + the pre-initialize reject of metadata-free operations + guard parity + no-drift verification. The modern era is request-stateless (never depends on prior session state); the legacy era is latched per-process by initialize, and metadata-free operations before that latch are rejected (pre-initialize operation test), so a malformed modern request cannot fall through to legacy and bypass _meta validation. Guard parity closes the cycle-3 adversarial-review P1 (a stateless/pre-handshake modern branch that skipped H1 would re-open the workspace_root P0). Width-isolated: code domain, <=2 files. Depends on 064.022-T; feeds the subprocess smoke harness (064.008-T).
<!-- END:implementation-notes -->