---
artifact_type: task
created_at: 2026-08-27T15:09:45.5753391Z
dependencies:
    - 064.022-T
id: 064.023-T
labels:
    - mcp
    - protocol
    - dual-era
    - code
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Implement dual-era routing with legacy retention
updated_at: 2026-08-27T15:10:34.6567019Z
---

Make 064.021-T green (dual-era routing completion + legacy retention + guardrail parity). Code domain, <=2 files (docline/mcp/stdio.py + src/docline/mcp/server.py). Owns the LEGACY branch of the request-shape era classifier (an initialize request selects legacy semantics) and completes dual-era routing on top of the modern-branch routing delivered by 064.022. Keep the legacy handshake/ping path (from 064.002) intact; identity/capabilities/versions are read from the single describe_server() accessor introduced by 064.015, so initialize (064.002) and server/discover (064.022) cannot drift by construction. GUARDRAIL PARITY (blocking): both eras MUST funnel every tools/call/process through the SAME hardened dispatch path so H1 (workspace_root reject -32602), H3 (error non-disclosure), H4 (closed allow-list), H5 (stdout hygiene) apply before the modern result wrapper - the era classifier changes only negotiation + envelope shape, never which guards run. See plan Protocol Era Model / cycle-3 remediation.

<!-- BEGIN:acceptance-criteria -->
- Legacy branch of the era classifier: an initialize request selects legacy semantics; a request carrying modern _meta was already routed to modern semantics by 064.022. Both branches coexist under one classifier.
- The legacy initialize/notifications/initialized/ping path is retained and unchanged for legacy clients.
- No drift: initialize and server/discover both read identity/capabilities/versions from the single describe_server() accessor (introduced by 064.015) - the no-drift property holds by construction.
- Guardrail parity: a modern (_meta, no initialize) tools/call process with workspace_root is rejected -32602 (H1); modern-path isError content sanitizes absolute paths (H3); modern-path stdout stays clean (H5); H4/H6/H7 hold on the modern path. Both eras share ONE hardened dispatch path.
- 064.021-T harness passes green (legacy-retention regression anchor stays green; era routing both directions; modern-path guard parity).
- ruff check ., ruff format --check ., pyright src/ clean; typed errors only.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green after 064.022-T. Boundary with 064.022: 064.022 owns the MODERN branch (detect _meta -> modern handlers) + server/discover + -32022; this task owns the LEGACY branch (initialize -> legacy) + guard parity + no-drift verification. The classifier never depends on prior session state for the modern era. Guard parity closes the cycle-3 adversarial-review P1 (a stateless/pre-handshake modern branch that skipped H1 would re-open the workspace_root P0). Width-isolated: code domain, <=2 files. Depends on 064.022-T; feeds the subprocess smoke harness (064.008-T).
<!-- END:implementation-notes -->