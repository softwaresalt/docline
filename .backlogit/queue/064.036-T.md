---
artifact_type: task
created_at: 2026-08-29T03:40:25.0000000Z
dependencies:
    - 064.035-T
id: 064.036-T
labels:
    - mcp
    - stdio
    - security
    - config
    - packaging
    - stage-darkfactory
parent_id: 064-F
priority: high
references:
    - docs/plans/2026-08-27-mcp-stdio-server-plan.md
status: queued
title: Resolve external-engine opt-in from server env/flag in docline-mcp
---

Make 064.035-T green. Code domain only, <=1 file (src/docline/mcp/__main__.py). Wire the §H8 server-side opt-in: main() resolves external_pdf_engines_enabled from the DOCLINE_MCP_ALLOW_EXTERNAL_PDF_ENGINES environment variable (fail-closed: enabled ONLY for the exact token "1") OR the docline-mcp --allow-external-pdf-engine CLI flag, resolves it EXACTLY ONCE at startup, and constructs a FRESH DoclineMcpServer(external_pdf_engines_enabled=<resolved>) passed to serve(server=...). The module-level SERVER default is never mutated. This is the ONLY path that enables the external engine on the MCP surface; it is never derived from request data. Completes §H8 (advertise-omit 064.032 + dispatch-deny 064.032/064.034 + startup opt-in here). Depends on 064.035-T; predecessor of 064.004-T (docs). See docs/plans/2026-08-27-mcp-stdio-server-plan.md (Plan Hardening §H8; 064.003-T bootstrap).

<!-- BEGIN:acceptance-criteria -->
- src/docline/mcp/__main__.py main() resolves the opt-in fail-closed by RAW exact-token equality (NO .strip()/trim, NO case-fold, NO truthy/int coercion): enabled iff os.environ.get("DOCLINE_MCP_ALLOW_EXTERNAL_PDF_ENGINES") == "1" OR the --allow-external-pdf-engine CLI flag is present; every other env value - incl. "0", "false", "true", "", whitespace-only, AND any whitespace/newline-padded "1" (" 1", "1 ", " 1 ", "1\n") - resolves DISABLED (unset -> os.environ.get(...) is None, None != "1" -> DISABLED).
- main() constructs a fresh DoclineMcpServer(external_pdf_engines_enabled=<resolved>) and passes that instance to serve(server=...); it does NOT enable/mutate the import-time module-level SERVER singleton (which stays external-disabled). The stdio guard (TransportMode.STDIO) construction from 064.003-T is preserved.
- Resolution happens ONCE at startup; external_pdf_engines_enabled is never read from request arguments or _meta anywhere (guaranteed by 064.032-T's instance-only flag).
- No-secret logging: neither the resolved opt-in value nor any ambient credential env value (AZURE_AI_FOUNDRY_KEY / AZURE_AI_FOUNDRY_ENDPOINT / MISTRAL_API_KEY) is logged/emitted during resolution or startup.
- The docline-mcp entry point (064.003-T) and its subprocess smoke behavior (064.008-T) remain green; the default (no env/flag) invocation still constructs an external-DISABLED server.
- Greens 064.035-T scenarios (a)/(b)/(c); existing suites remain green.
- ruff check ., ruff format --check ., pyright src/ clean; typed only, no bare except.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Execution posture: make-green (startup config wiring) after 064.035-T. Single file: src/docline/mcp/__main__.py - add a minimal argparse flag (--allow-external-pdf-engine) + the exact-"1" env check, a single fail-closed resolution, and pass external_pdf_engines_enabled into the DoclineMcpServer(...) constructor that main() already builds (064.003-T). No new module, no request-side reads, no SERVER mutation. Relies on serve() accepting the constructed server instance (established by 064.002-T/064.003-T). Depends on 064.035-T; feeds 064.004-T (README documents the flag/env + default-deny + upload/cost warning). See plan §H8.
<!-- END:implementation-notes -->
