---
artifact_type: feature
created_at: 2026-06-04T11:31:39.2993034-07:00
custom_fields:
    harness_status: pending
id: 016-F
labels:
    - processing
    - readers
    - docling
    - hygiene
    - stage-harvested
    - shipment-15
references:
    - docs/plans/2026-06-04-post-g3-hygiene-plan.md
    - docs/decisions/2026-06-04-post-g3-hygiene-deliberation.md
    - docs/decisions/2026-06-04-post-g3-hygiene-plan-review.md
status: queued
title: Post-G3 hygiene — CRLF normalization + docling tuning with PictureSink wiring
updated_at: 2026-06-04T11:31:39.2993034-07:00
---

Address two low-priority hygiene follow-ups from the G3 segmentation + docling arc: (1) normalize CRLF and CR line endings on entry to `segment_markdown` so paragraph detection survives Windows-extracted PDF text (review F2 advisory from 012-S, stash `F50AD7E6`); (2) configure docling `PdfPipelineOptions(do_table_structure=True, table_structure_options.do_cell_matching=True, generate_picture_images=True, images_scale=2.0)` in `_read_pdf_docling_pages` and wire docling-rendered pictures through the existing `PictureSink` from 014-S (plan-review F3 advisory from 014-S, stash `5ADD558F`). No graphtor-docs writes — operator confirmed read-only access. Both follow-ups are independent at the file level but share a thematic context (post-G3c hardening for the docling-enabled production path).

<!-- BEGIN:AcceptanceCriteria -->
- `segment_markdown` normalizes `\r\n` and `\r` to `\n` on entry
- `_read_pdf_docling_pages` accepts optional `picture_sink: PictureSink | None = None` kwarg
- Docling pipeline configured with `do_table_structure=True`, `table_structure_options.do_cell_matching=True`, `generate_picture_images=True` (only when sink provided), `images_scale=2.0`
- `read_pdf` and `read_pdf_pages` accept and forward `picture_sink`
- `build_output_document_parts` PDF branch passes `picture_sink` through to `read_pdf`
- 6 new tests pass (3 CRLF unconditional + 3 docling-side skip-gated on `pdf_available()`)
- All 5 CI gates green
- `docline export-schema` output unchanged
- Closure document records the new behavior
<!-- END:AcceptanceCriteria -->

<!-- BEGIN:ImplementationNotes -->
Plan-review F1 (P2) refinement: implementation (016.003-T) must verify the docling 2.x picture-iteration API (`document.pictures` with `.image` PIL attribute is the assumption from docling docs). If the attribute name differs at runtime, log warning and skip picture emission rather than raising — markdown still flows through. PNG serialization via BytesIO + PIL `image.save(buf, format='PNG')`. Plan-review F3 advisory (docling `do_ocr=True` follow-up) should be stashed during closure. NOTE: Operator clarified — do NOT write to graphtor-docs directory even though access is in the allow list.
<!-- END:ImplementationNotes -->