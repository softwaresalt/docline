---
title: "Instinct clustering: 060-S session (scope=recent)"
date: 2026-09-09
scope: recent
observation_source: .autoharness/continuous-learning/observations/2026-09-09.jsonl
---

## Candidate instincts

| Pattern | Evidence count | Confidence | Suggested next action |
|---|---|---|---|
| A non-canonical/non-editable docline install (not the documented `uv sync`/`uv run` workflow) requires `pip install -e . --no-deps` as a repair step before local `src/` edits are visible to tooling | 1 | low | keep observing — single-session evidence only, scoped to a non-canonical environment; watch for recurrence in a similarly stale environment before promoting |
| `uv run` is this repository's documented, CI-matching environment (README.md; `.github/workflows/ci.yml`); a bare `python -m` invocation may resolve to a different, non-project toolchain in a session not set up via that documented workflow | 1 | low | keep observing — single-session evidence; if this recurs, promote toward a documented environment-setup instruction |
| POSSIBLE only, not yet established: Copilot review may re-engage on every push and surface new documentation-accuracy findings unrelated to the latest commit's diff | 1 | low | keep observing — single-PR (#186), single-shipment (060-S) evidence; requires corroboration from another shipment before promotion beyond "possibility" |

None of the three observed patterns has reached the promotion threshold (3 corroborating
instances). No instinct is promoted to `evolve` in this session. All three remain provisional,
single-evidence observations per the `learn` skill's quality criteria (do not turn a weak hunch
into a hard rule).
