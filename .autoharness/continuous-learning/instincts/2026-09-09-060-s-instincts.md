---
title: "Instinct clustering: 060-S session (scope=recent)"
date: 2026-09-09
scope: recent
observation_source: .autoharness/continuous-learning/observations/2026-09-09.jsonl
---

## Candidate instincts

| Pattern | Evidence count | Confidence | Suggested next action |
|---|---|---|---|
| docline requires `pip install -e . --no-deps` before local `src/` edits are visible to tooling | 1 | low | keep observing — single-session evidence only; watch for recurrence across future Ship sessions in this workspace before promoting |
| `uv run` is the canonical, CI-matching environment for quality gates in this workspace (bare `python -m` resolves to a different toolchain/Python version) | 1 | low | keep observing — single-session evidence; if this recurs in a future session, promote toward a documented environment-setup instruction |
| Copilot review re-engages on every push and can surface new documentation-accuracy findings unrelated to the latest commit's diff, even after prior rounds look clean | 1 | low | keep observing — single-shipment evidence (060-S/PR #186); if a future shipment shows the same cascading-cycle pattern on docs-heavy PRs, this is a candidate for a PR-lifecycle planning note (budget N+1 review rounds for doc-heavy shipments) |

None of the three observed patterns has reached the promotion threshold (3 corroborating
instances). No instinct is promoted to `evolve` in this session. All three remain provisional,
single-evidence observations per the `learn` skill's quality criteria (do not turn a weak hunch
into a hard rule).
