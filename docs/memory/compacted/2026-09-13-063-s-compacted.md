# 063-S / 072-F Compacted Memory — Sanitize credential-bearing source_key on ELT error/persistence paths

**Compacted:** 2026-09-13 (post-merge closure, P-020)
**Sources compacted** (moved to `docs/archive/memory/2026-09-13/`):
- `063-s-build-checkpoint.md`
- `063-s-build-checkpoint-round3.md`
- `063-s-halt-review-loop-report.md`

**Not compacted / not archived** (explicit carry-forward preservation directive for this
closure session — left exactly in place, untouched, uncommitted):
- `docs/memory/2026-09-14/063-s-bounded-extension-round8-9-checkpoint.md`

## Outcome

Shipment `063-S` (feature `072-F`, tasks `072.001-T`–`072.004-T`) shipped a typed-config
credential sanitizer (`sanitize_source_key`/`sanitize_source_id` in
`src/docline/elt/source_keys.py`, wired into `src/docline/elt/execute.py`) that prevents
credential-bearing `source_key` values from being persisted to `metadata.json` or logged
on ELT error paths, including through exception-chain scrubbing.

* PR #198 (original) → superseded/closed after a non-converging Copilot review-comment
  loop (7 rounds) plus an operator-authorized one-cycle bounded extension (commit
  `c547f93`, fixing `E462E1F0`/`E89DC095`).
* PR #199 (replacement, single corrected commit) → merged to `main` at
  `2026-09-14T03:02:32Z`, merge commit `3933dfc335e19bcd602bf104e82728268ae83156`,
  reviewed HEAD `f1f5f8fd82fe83cc8095e210eac5ba47ccaf60cb`. All 8 CI checks green, P-018
  copilot-review `SATISFIED` (0 unresolved threads).

## Key decisions and rationale

* 3 full local adversarial-review rounds (pre-PR) plus multiple PR-review (Copilot)
  rounds were run; all HIGH/CRITICAL credential-leak findings were fixed in-cycle under
  P-021 C1/C3 same-contract-surface rules. Every fix was independently re-verified by
  Ship (line-by-line diff review, direct Python reproduction, full gate re-run) rather
  than trusted from subagent/bot reports alone.
* The review-fix-cycle circuit breaker (3) was reached at PR-review round 3; rounds 4-7
  findings were deferred via P-021 C2 capture rather than reactively fixed, except for a
  single operator-authorized bounded one-cycle exception (commit `c547f93`) that fixed
  two HIGH-priority same-contract-surface regressions (`E462E1F0`, `E89DC095`)
  discovered in that fix's own helper surface.
* A provably non-converging documentation-staleness nitpick pattern (round 7) was
  deliberately NOT chased further after one bounded correction, per explicit operator
  instruction, to avoid an unbounded push/poll loop.
* 14 total P-021 deferred-scope-expansion stash entries remain open across the shipment's
  full review history (5 pre-PR local-review, 5 PR-review rounds 3-6, plus 4 more from
  the round-3/round-8-9 bounded-extension work): `0F1A653C`, `06A59B1D`, `95BD0DC7`,
  `709BDB53`, `6076A65E`, `4CEE1EA5`, `1B5CEF80`, `A6D7EEB9`, `96E6C3F2`, `7D7222E3`,
  `E462E1F0`, `E89DC095`, `BF028CAE`, `12925B3B`. None are P0/P1; all require Stage
  triage/deliberation per their `requires_deliberation` flags. Full detail in the
  post-merge closure narrative:
  `docs/closure/2026-09-13-sanitize-source-key-elt-error-paths-closure.md`.

## Carry-forward artifact handling

The pre-existing `.backlogit/stash.jsonl` fractional-seconds timestamp-normalization diff
(2 lines, on entries `0F1A653C`/`06A59B1D`) was carried forward, byte-identical, across
every commit and branch switch in this shipment's full lifecycle (build → PR → merge →
post-merge closure) via targeted `git stash push -u -- <path>` / `pop`, verified via
`git hash-object` at each checkpoint. It remains uncommitted as of this compaction.

## Final disposition

Shipment closed (`shipped`/archived) via the P-015 verified-fully-covered-root cascade
close path. See `.backlogit/reconcile/063-S-safe-close-20260913T200543Z.md` for the full
cascade-close evidence trail.
